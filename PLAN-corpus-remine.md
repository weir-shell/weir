# weir — the corpus re-mine

Status: LANDED 2026-07-22 (blessed same day). One session,
time-boxed per the original mining precedent. Owed since the tuples
session and four feature waves richer: TUPLES, LITERAL PATTERNS,
COMPOSITION, RAW STRINGS each retired divergence rows whose shapes
the original mechanical filter EXCLUDED.

Completion addenda (2026-07-22):
- Finding zero: the FIRST mine's extraction/filter never survived its
  session — only the report did. tools/corpus-mine.py is now the
  committed artifact, calibrated to the published denominator
  (4253 extracted vs 4256; base 76 vs 78), wave-rejects explicit.
- Prize 1: ZERO GOLD over the widened set. Prize 2: disagreements
  24 -> 18 while the set grew 76 -> 102 (+34%); agree-accepts 4 -> 9.
  All 18 bucketed; residue after naming: zero.
- Seven new rows (no-record-update the headline; ctor-pattern-
  scrutinee the predicted unnamed find; column-zero-statements
  explained the five ///-lookalike snippets); four rows corpus-
  tagged; no-elif upgraded to demand-exists.
- Prediction scorecard: tuple/raw residue shapes did not survive the
  filter; the >=1 unnamed absence arrived; the headline find was
  unpredicted.
- Full report: tests/fidelity/corpus-report-5928e91-remine.md.

[Plan text as blessed follows.]

## Pre-made decisions

- DECIDED — Same commit, new filter (5928e91 pinned; the filter DIFF
  is a committed artifact).
- DECIDED — The prize metric stated up front, two numbers: zero-gold
  over the widened set; the disagreement triage yield (existing row /
  unnamed divergence / weir bug; gold = stop-everything).
- DECIDED — Triage mechanical-first; residue prediction on record:
  (a) tuple-adjacent bounded forms, (b) raw edges, (c) >=1 unnamed
  absence from the new rule families.
- DECIDED — Time-box: one day inside the session.
- DECIDED — Findings feed the standing ledgers: rows + SKILL lines
  in-session; weir bugs get pins + own sessions unless trivial;
  corpus-confirmed rows gain a `corpus: 5928e91` tag.
- DECIDED — Report includes the coverage denominator; environment
  verified FIRST (the tuples-session skip must not recur silently).

## Parked (unchanged)

- Refresh to a newer dotnet/fsharp commit — reopens only on a
  fidelity incident suggesting staleness.
- Mining other corpora — the ComponentTests vein stays the single
  source until exhausted.
