{
  "name": "weir-site",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "postbuild": "pagefind --site dist && node scripts/check-search.mjs",
    "preview": "astro preview"
  },
  "dependencies": {
    "@astrojs/markdown-remark": "^7.3.0",
    "astro": "^7.3.1"
  },
  "devDependencies": {
    "pagefind": "^1.5.2"
  }
}
