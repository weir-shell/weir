module Jira2
/// generated from 'jira2' on 2026-09-02
/// source: help+subs — a scraped surface may be incomplete (partial
/// by default; add `let exhaustive = true` once verified by hand)
/// flat surface: flags checked across the whole line — split into
/// subcommand records by hand if the tool warrants it
let version = "(Version=\"1.7.0\", GitCommit=\"Homebrew\")"
type Cmd = {
    /// string    Config file (default is /Users/queil/.config/.jira/.config.yml)
    [<Short "c">]
    config: bool
    /// Turn on debug output
    debug: bool
    /// help for jira
    help: bool
    /// history
    history: bool
    /// string   JQL
    [<Short "q">]
    jql: bool
    /// Plain
    plain: bool
    /// string   Jira project to look into
    [<Short "p">]
    project: bool
}
