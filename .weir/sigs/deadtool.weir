module Deadtool
/// generated from 'deadtool' on 2026-09-02
/// source: help (walked 1 subcommand help(s), 0 answered, none yielded flags) — a scraped surface may be incomplete (partial
/// by default; add `let exhaustive = true` once verified by hand)
/// flat surface: flags checked across the whole line — split into
/// subcommand records by hand if the tool warrants it
let version = "t 1.0"
type Cmd = {
    /// top flag
    top: bool
}
