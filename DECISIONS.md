# Decision index

Keyed one-liners. The STORY lives where the pointer says — NOTES.md
(section title), SEMANTICS.md, tests/fidelity/divergences.md (row id),
PROCESS.md — this file never duplicates it. Code comments cite keys as
`[D:key]` plus a half-line local why; dates live here, not in comments.
F#-border decisions REUSE their divergence-row id as the key (one name
per decision). Append-only: a reversed decision gets a new entry naming
the old key, never an edit-in-place.

| key | date | decision | story |
|---|---|---|---|
| one-pipeline | 2026-07-21 | every consumer (runner, REPL, -e, check, lsp) dispatches through Script.checkStatement; consumers render, never re-derive | NOTES: "One pipeline — the mirror incident's fix" |
| check-lsp-chain | 2026-07-21 | weir check [--json] is the agent-facing diagnostics core and the LSP's payload generator; no evaluation by construction | NOTES: "weir check + weir lsp — the chain lands" |
| lsp-v1 | 2026-07-21 | stdio JSON-RPC on System.Text.Json (DOM reader / Utf8JsonWriter, AOT-safe); whole-file recheck inside the 10-11ms license | NOTES: "weir check + weir lsp — the chain lands" |
| assume-resolver | 2026-07-21 | check-only consumers assume command-shaped heads (cmd-not-found WARNINGS); the runner keeps hard resolution; the parse resolver is built per-statement from the current env | NOTES: "Live-testing receipts: check assumes commands; the resolver goes per-statement" |
| clean-parse-dump | 2026-07-21 | parse errors show the ORIGINAL source line + caret, never assembled text; FParsec snippet blocks stripped, embedded positions translated to physical | NOTES: "Parse errors show the unassembled source" |
| missing-command-diagnosis | 2026-07-21 | on parse failure, retry under the assume-resolver; if it parses, name the missing command heads precisely instead of dumping the parser | NOTES: "Runner missing-command diagnosis; a masking confession" |
| assembly-recovery | 2026-07-21 | tooling paths drop the line that breaks assembly (≤10, each kept as an `assembly` diag) instead of erasing document knowledge | NOTES: "fmt field-drift + assembly recovery" |
| repair-completion | 2026-07-21 | completion REPAIRS the broken statement (blank filler for a dangling prefix, closers appended) and typechecks the repair for cursor-exact results | NOTES: "Error-recovery completion — the park opens on a user push" |
| hole-completion | 2026-07-21 | unbound lowercase names bind as fresh TVars before inference, so pipelines-with-holes still type and `t.` completes | NOTES: "Error-recovery completion — the park opens on a user push" |
| open-row-compat | 2026-07-21 | an open row whose fields fit inside a declared record completes the record's FULL field set | NOTES: "Open rows meet nominal records; cursor-local repair" |
| declared-fields-fallback | 2026-07-21 | an UNRESOLVABLE `x.` head offers every declared record's fields rather than nothing | NOTES: "Completion for params — the declared-fields fallback" |
| completion-textedit | 2026-07-21 | completion items carry explicit textEdit ranges (bare labels double-inserted after dots and were prefix-filtered inside parens) | pinned in tests/lsp/lsp-e2e.py |
| json-relaxed-escaping | 2026-07-21 | JSON payloads use UnsafeRelaxedJsonEscaping — LSP/CLI output, never HTML; default " escapes tripped micro's plugin | pinned at frame level in tests/lsp/lsp-e2e.py |
| owned-line-editor | 2026-07-21 | the ReadLine package is replaced by an owned editor: bash key semantics (Ctrl+C cancels line, Ctrl+D EOF, word nav) | NOTES: "Three out-of-band asks: exit, Ctrl+D, usage" |
| exit-rename | 2026-07-21 | Exit.code -> `exit n` (F#-parity); `fail` keeps message-carrying exit 1 | NOTES: "Three out-of-band asks: exit, Ctrl+D, usage" |
| squiggle-on-binder | 2026-07-21 | binder-name spans are re-derived from statement text (SLet carries a bare string); casing squiggles sit on the name, not the RHS | pinned in ci/e2e.sh |
| colored-diagnostics | 2026-07-21 | errors/warnings colored per-stream on TTY only; NO_COLOR and TERM=dumb respected — pipes/CI see plain text by construction | pinned by every e2e capture being non-TTY |
| lowercase-binds | 2026-07-21 | the casing law: binding names start lowercase; uppercase is types/modules/constructors | divergences.md row; NOTES: "The casing law — lowercase binds, uppercase declares" |
| exhaustiveness-hard-error | 2026-07-18 | a non-exhaustive match is a hard error (F# warns) | divergences.md row; NOTES: "Amendment: exhaustiveness is a hard error" |
| unreachable-arm-hard-error | 2026-07-21 | an arm below an unguarded catch-all is a hard error AT the catch-all, with a constructor hint for variable binders | divergences.md row; SEMANTICS: branching |
| tuples-reversal | 2026-07-21 | tuples land (literals, types, patterns, binders); Pair {Fst;Snd} retired; `*` claims tuple type syntax | NOTES: "REVERSAL: tuples land; \"records are the product\" retires" |
| bare-comma | 2026-07-21 | comma is the tuple constructor at F#'s precedence; weir-only cell: below `;` | NOTES: "Pattern binders + the bare-comma amendment" |
| pattern-binders | 2026-07-21 | irrefutable patterns destructure in let/params (parens required on params); refutable binders are check errors | divergences.md row no-pattern-binders; NOTES: "Pattern binders + the bare-comma amendment" |
| literal-patterns | 2026-07-20 | int/string/() literal patterns; literals never complete a match alone (F#'s rule) | NOTES: "Literal patterns + () thunks" |
| let-param-sugar | 2026-07-20 | `let f x y = e` desugars to nested lambdas — the corpus mining's top yield | NOTES: "let f x = ... parameter sugar" |
| inferred-type-classes | 2026-07-20 | closed {Eq, Show, Ord}, compiler-owned, machine-regime-only, fully erased; sentinels retired (Sessions A-C), the sole runtime type check died | NOTES: "Type classes Session A/B/C" entries |
| child-env-overlay | 2026-07-20 | cmdEnv/runEnv inject per-child env as an OVERLAY (set those names, inherit the rest, parent untouched); one spawn path via Proc.linesWith | NOTES: "Child-env injection — the shEnv receipt lands" |
| env-sugar-layers | 2026-07-20 | sigils take an env slot glued to the glyph ($e(...) / !e(...)); line-end `!name` makes an env district | NOTES: "Env sugar Layers 1+2 — the seam pays out" |
| one-scanner | 2026-07-20 | foldOutsideStrings is the ONE string-state primitive; a second inline quote machine is a review flag | NOTES: "Assembler formalization — the boundary question" |
| structured-parse-failure | 2026-07-20 | parse positions travel as DATA (ParseFailure.Col), never regexed out of FParsec message text | NOTES: "Assembler formalization — the boundary question" |
| comment-transparency | 2026-07-20 | comment-only lines are transparent to assembly (F#-faithful; they used to end statements) | NOTES: "Fix round: transparent comments, parse-error attribution" |
| fmt-v1 | 2026-07-18 | fmt normalizes leading indent + trailing ws ONLY; the result must re-assemble identically or fmt refuses to write | NOTES: "weir fmt — the canonical formatter, v1" |
| fmt-depth-model | 2026-07-20 | indent = open levels, deepest first (any deeper line opens, same-level returns, col-0 resets) — preserves every assembler comparison | NOTES: "Hardening sweep — the postmortem pays out" |
| fmt-brace-plus-2 | 2026-07-21 | record fields align at open-brace column + 2 (house style; the depth model had drifted them to depth*4) | NOTES: "fmt field-drift + assembly recovery" |
| bracket-heads-expression | 2026-07-18 | `[` never heads a command (a line-head list would resolve to /usr/bin/[) | NOTES: "Review amendments: [ heads nothing; the sh builtin is gone" |
| typed-env | 2026-07-20 | the Env module: Env.get / Env.vars / Env.load Config (typed, one aggregated error) | NOTES: "Typed Env — Env.load Config" |
| prefix-minus | 2026-07-21 | prefix minus with F#'s adjacency rule; the oracle overturned the `f -1` subtraction folklore mid-landing; retires no-unary-minus | divergences.md retirement note; oracle pins in tests/Weir.Fidelity/Pins.fs |
| composition-operators | 2026-07-22 | `>>`/`<<` compose functions at the PIPE's precedence (the oracle refuted the tighter-than-pipe folklore: `xs \| f >> g` needs parens, F#'s rule); command-line `>`/`>>` stay argv words WITH File.write/File.append hint warnings | divergences.md rows redirect-argv + no-heredoc; oracle pins in Pins.fs |
| regex-pattern | 2026-07-22 | the bespoke `Regex "lit" binder` match form — literal-only, check-time compiled (shared literal-keyed cache), arity-typed binders, refutable; Str.isMatch/rmatch carry computed patterns; NOT user active patterns (door stays closed). Its positional-raw literal was superseded same week by [raw-strings] | divergences.md row regex-pattern; SEMANTICS: branching; TRANSCRIPTION addendum |
| masking-mechanized | 2026-07-22 | build stamps (publish embeds git hash; `weir --version`) + HARD stamp/mtime gates in every executing harness + waitpid-truth process census (tests/lib/harness.py) — the fifth masked-failure incident proved remembering fails; mechanisms don't | PROCESS.md: harness truth; the selftest pin |
| lambda-core | 2026-07-22 | the five lambda-typing arms share one assembly core (Flag 7 discharged early — the ELambdaPat twin miss showed the duplication already biting); zero behavior change, zero pin edits | TRANSCRIPTION consolidation note |
| elif | 2026-07-22 | `elif` chains land as pure spelling for `else if` (parse desugar to nested if; the assembler treats elif lines as else-family); precondition-probed first — chained else-if already worked, no hidden gap | divergences.md retirement note; oracle pins |
| splice-default-last | 2026-07-22 | splice/hole scalar defaulting is a FINALIZATION step at the statement boundary, not an eager bind — fixes the only wrong-rejection on the books (`1 \| (fun k -> $"{k}")`); still-unresolved holes default to string, non-scalars keep the original rejection | SEMANTICS: the soundness condition, re-verified; TRANSCRIPTION addendum |
| fmt-respace | 2026-07-22 | fmt v2: intra-line respace (collapse space runs, pad record braces, tidy `;`) under a PARSE-SHAPE guard — each statement must sexpr-match its original under Script.assumeResolver or it reverts; string interiors, leading indent, and pre-comment alignment gaps untouched | SEMANTICS: fmt; the sexpr renderer moved to Ast (shared with the parse pins) |
| exit-reifiers | 2026-07-23 | `\| succeeds` (bool, ExitCode==0 exactly, silent) and `\| orFail "msg"` (unit-or-raise, msg + exit code, stderr replayed) join complete's family — one fold, one single-segment rule, env twins; unit became printable-as-nothing so orFail sits in !()/districts (the decided cell); bool-valued command statements join the discard family | PLAN-exit-reifiers; SKILL: the exit-zero sentence |
| paramful-rhs | 2026-07-23 | param-ful lets take a command RHS; params shadow PATH in their own RHS (bindings-beat-PATH's scope — `let f x = x` stays identity, pinned against a real PATH x); spliced params ride boundary defaulting; the FIRST feature enabled by a bug fix (splice-default-last was the wall) | PLAN-paramful-rhs; SEMANTICS: soundness note 3rd edition |
| seq-fold | 2026-07-22 | Seq.fold lands (state-first folder, FCS-probed; strict; constraint-free); the check-mode nested-lambda push-through fixed en route (the canonical piped sum rejected); Env.pair/ofPairs ride (inline-env, NOT an anon-records case) | PLAN-fold; SEMANTICS: library |
| fun-sugar | 2026-07-22 | `fun a b ->` desugars via curryParams — one rule, two positions with let-param sugar; duplicate params reject in BOTH (the probe caught let-sugar accepting them; F# rejects) | PLAN-fold; oracle pins |
| record-update | 2026-07-22 | `{ r with F = v }` lands (flat/multi/nested I.X/general sources); result type IS the source's type — a row source keeps its own row var, so updaters generalize; update never adds fields; bare match/if sources need parens (FCS-probed); paths are field-only (row update-path-plain) | PLAN-record-update; divergences.md retirement note |
| raw-strings | 2026-07-22 | F#'s two raw kinds land: `@"..."` verbatim and `"""..."""` triple, single-line only (row raw-single-line); rawness is a KIND property, never positional — and the Regex position is raw-ONLY by rider (ordinary strings rejected there, kind-not-content). Interpolated-raw parked (row no-interpolated-raw) | PLAN-raw-strings; SEMANTICS: literals + branching |
| attributes | 2026-07-23 | `[<Name arg>]` on record fields — F#-parity SYNTAX (FCS parses the adopted shapes), invisibly divergent SEMANTICS (check-time erased data, no reflection); closed registry Short/NoShort/Doc/Positional, unknown names check-error with did-you-mean; validate-at-attachment/bind-at-consumption (unconsumed = legal-and-inert); consumers pending typed argv | PLAN-attributes; SEMANTICS: types; divergences row attributes-registered |
| typed-argv | 2026-07-23 | `Args.load T` over records (flags) and unions (subcommands) — kebab derivation, unambiguous-first-letter shorts with attribute override (derivation yields to declaration), strict collect-then-raise, `--help` pre-validation from the declaration; the attributes consumers activate here (Short/NoShort/Doc bound, Positional not-yet); no positionals (zero organic receipts; named-over-positional) | PLAN-typed-argv; SEMANTICS: processes (the boundary family); NOTES entry |
| seq-choose | 2026-07-23 | `Seq.choose` lands (F#-parity signature, lazy, qualified-only, constraint-free) — the match-or-skip idiom replaces sentinel-empty map-then-filter; the Regex pattern's natural stream consumer. `Seq.choose id` NOT ridden (`id` does not exist; the identity-lambda spelling stands, no receipt beyond cosmetics) | PLAN-choose; SKILL/GUIDE idiom blocks; flagship statusRefs |
| multiline-brackets | 2026-07-23 | type decls and list literals continue across lines — ONE widened mechanism (bracket stack replaces the brace depth; innermost kind picks the separator rule: `=`-fields / `:`-fields+attrs / every-line-an-element-unless-dangling); cross-bracket closers error naming both sides; preceding-line attributes ride free; fmt aligns under the first field/element (brace+2, bracket+1); parens stay parked | PLAN-multiline-brackets; SEMANTICS: types (bracket continuation); attributes plan finding resolved |
| fmt-match-arms | 2026-07-23 | fmt aligns match arms under the m (a match-head stack; the first pipe after a head anchors the arm set's indent) — deeper-indented arms pull back; union cases, chain stages, and arm-body pipes are not arms and keep their depth | user request; Fmt.fs [D:fmt-match-arms] comments; fmt pin battery |
| fmt-stroustrup | 2026-07-23 | Stroustrup is the HOUSE bracket style (user call, citing the fantomas-repo poll ~70%): dangling opener at the head line's end (or a `{ .. with` header), entries at opener-indent+4, closer alone at the opener's indent; fmt canonicalizes each style's indentation but does no line surgery, so the aligned style stays accepted; closer lines never take a separator. F# offside-rejects the col-0 closer in UPDATE position only (probe-refuted Same claim → the record-fields-ignore-indent row) | Fmt.fs [D:fmt-stroustrup]; SKILL house-style note; flagship swept |
| blank-in-brackets | 2026-07-23 | REVERSES the blank-inside-bracket error (the record-continuation session's rule, extended by multiline-brackets): blanks inside open `{`/`[` are TRANSPARENT, the comment-line class's second member. Archaeology honesty: comment transparency PREDATED the original decision — this is a re-weighing on new receipts (Stroustrup grouping, encodeSubref, user call), not new information. The statement-head guard (col-0 `let `/`type ` while a bracket is open errors naming the opener) bounds the unclosed-bracket runaway. RESIDUE: the bound is keyword-anchored — command-heavy scripts can swallow many lines before a `let` appears; command heads cannot join the guard (`[foo bar]` is a legal element). WATCH: a long-swallow incident reopens the guard's keyword set | PLAN-blank-lines; divergences row blank-line-ends-statement (narrowed); NOTES entry |
| body-blanks | 2026-07-23 | REVERSES blank-line-ends-statement's CORE (and subsumes [D:blank-in-brackets], same day): blanks are transparent while a statement pends — bodies, arms, districts, brackets, one rule; statements end at the next col-0 line or EOF (the column-zero-statements law absorbed the boundary role). The founding multi-line divergence RETIRES — the row left the ledger. Strays after a gap join and the checker catches them, identically to the blank-free spelling (FCS-probed: F# rejects both). noBodyBlank retired; the bracket statement-head guard stays | PLAN-body-blanks; divergences retirement note; NOTES: the founding arc |
| repl-echo | 2026-07-23 | the REPL/-e echo renders bounded (10 elements + "…", 120-char string clip, depth 8) with a way-out hint naming the count when known (materialized lists) and "?" when counting would force; ONE renderer, limits threaded — `show` keeps its shipped lossy contract byte-identical (20 + "..."; unclipped strings). fsi-parity in direction, constants ours. Plan-premise corrections on record: the echo already bounded forcing (no hang existed), and show was already DOCUMENTED-lossy (not total) — the decided behaviors were executable as written, the reasoning amended | mini-plan (session message); pull-count pin (caught a double-force in review) |
| repl-color | 2026-07-23 | REPL input-line coloring, lexical grade: the colorizer RIDES the one scanner (inStringMask) + stripComment + the parser's keyword set — the one highlighter correct by construction (micro/TextMate drift by hand-audit; this one cannot); the fish trick colors the head by live resolver verdict (bold known / blue PATH / red unresolved; `^` forces PATH-only); paint-only architecture — buf/pos never hold escapes, so the planned strip-width helper DISSOLVED (cursor math never sees ANSI); NO_COLOR/piped byte-clean, harness-guarded; the transparency property (strip∘colorize=id) is the load-bearing pin; fixed palette, no theming — RED-FAMILY EXCLUSIVITY (first live receipt: keywords were magenta, which renders near-red in some themes and read as errors; keywords moved to blue, PATH heads to bold blue; 31 is exactly one signal: a head that would fail) | PLAN-repl-color; tests/repl/repl-color.py; error carets pre-existed (colored-diagnostics session) |
| compound-paren-prune | 2026-07-23 | compounds record the paren depth at open (scanner-aware parenDelta); a piece whose closers drop below that depth PRUNES the compound — the user's own `)` already bounds it, so the offside wrap never crosses a paren boundary. Found by the full subrepo port: a match inside a multi-line lambda got its outer pipeline stage wrapped INTO the lambda (legal-parse-wrong-meaning class) when a block-let was pending | Script.fs; the pinned assembled-text test |
| seq-append | 2026-07-23 | `Seq.append` lands (F#-parity, lazy, qualified-only) — the full-port receipt: variable-length argv construction (commit-tree parents) had NO concat spelling at all | SKILL library line; the port's commitTreeAs |
| seq-force | 2026-07-23 | the materializer is `Seq.force`/bare `force` (shortest honest word, zero F# collision — `toList` named a type weir will never have, per the no-split decision); `Seq.collect` RESERVED unbuilt for flatMap (F#'s meaning — later is breaking, now is a grep); Seq.cache-shaped lazy memoization NOT built (different tool); retired names teach via one table at both lookup sites; the Option bundle rode: `defaultTo` → `defaultValue` + `defaultWith` (thunk), discharging the parity audit's known backlog | PLAN-seq-force-patterns; SKILL when-do-I-force lines |
| seq-patterns | 2026-07-23 | seq patterns land (Part 2's design verbatim): F#'s list spelling, seq semantics, bounded force, memoize-once via Seq.cache (effects once TOTAL incl. rest consumption — the double-force class unrepresentable); [] + irrefutable-cons complete (flat v1); refutable → match-only; the cons level is the pattern grammar's one infix; @-patterns/mid-list rest/arrays stay out until their own receipts | PLAN-seq-force-patterns Part 2 + addenda; divergences row seq-patterns; SEMANTICS: patterns |
| pipe-hint | 2026-07-23 | a bare `|` after a completed expression names the spelling ("'|' chains commands; pipe expressions with '|>'") instead of dumping the token set — the whitelist investigation's recovered receipt (commandCallable was never a stage gate; stages are full expressions since session-3; the cliff was the error channel); three sites (statement, let-RHS, destructuring RHS), the consumed-`|` fatal wins FParsec's furthest-error rule | NOTES: the investigation entry; Parser.fs barePipeHint |
| block-let-cmd | 2026-07-23 | block-let command RHS lands (the param-ful law one scope deeper): ONE grammar, one more position — the spine flag (ThreadLocal, TRUE only along topLet's RHS + its let-in chain) holds the original in-swallow park's boundary BY CONSTRUCTION (parens, lambda bodies, and single-line let-in stay expression-only, pinned); block names shadow PATH via ambient-resolver extension (failing-first: SPAWNED observed guard-dropped); the in-stop reaches reifierEnd (found by the depth battery: `| succeeds in` demoted the reifier); the port's workaround wrappers un-hoisted. RIDER: `function` reserved with a teaching hint; `^function` reaches PATH; FCS Same-reject | PLAN-block-let-cmd; e2e depth battery |
| pipe-alignment | 2026-07-24 | sibling pipes align exactly (hard error, F# warns — the strictness family, opened on the flagship's live off-by-one `| Init`); arms anchored at-or-right of their match head (F#-parity narrowing: col-0-arms-under-deeper-match now rejects); shallower arms offside-CLOSE nested compounds — the nested-match return became legal (was a flat-join mis-association weir REJECTED; F# accepts). Zero unit-pin movement; the |-inertness law narrows to: indentation-blind WITHIN an aligned group | divergences row pipe-alignment; SEMANTICS: multi-line rules; probes-first (FS0058 is a warning — the premise's "error in F#" folklore-corrected) |
| field-alignment | 2026-07-24 | sibling bracket entries align exactly with the FIRST entry (measured, not offset arithmetic) — F#-parity (probes: F# errors on off-by-one fields everywhere once the snippet carries its type; the first "F# tolerates literals" read was a missing-type artifact, corrected by the typed probe); update headers anchor on their first field, not the source; trailing-`;` and closers exempt; the records-ignore-indent row NARROWS to its update/closer edges. First catches: the flagship's own gitrepoHeader drift (latent since the port) and fmt's +1/+2 offset arithmetic disagreeing with a `[ x`-spaced list — fmt now uses the measured anchor, agreeing with the assembler by construction | divergences row (narrowed); SEMANTICS: multi-line rules |
| fuzz-harness | 2026-07-24 | the assembler fuzzer lands (Session 1): FsCheck generator over a line-shape combinator grammar (valid-by-construction; trivial bodies; unique effect markers), metamorphic equivalence on the AOT binary (blank/comment insertion any gap, whole-block re-indent, and their COMPOSITION — (rc, stdout, stderr) byte-identical), totality of analyzeLines on generated programs + mutated neighbors (delete/perturb/duplicate/swap, stacked), dependency-closure shrinking to minimal statement subsets; stamp-gated runner; pinned-seed smoke (~5s), env-driven deep runs. Coverage denominator committed (tests/fuzz/GRAMMAR.md); equality detector has a graded positive control | PLAN-fuzzer Session 1; tests/Weir.Fuzz; NOTES: the first deep runs |
| multiline-lambda | 2026-07-24 | `(fun ... ->` dangling at EOL opens a body block closed by its own `)` (the parens-spanning park's lambda face — receipts at two): a Lambdas stack member on Pend (open line, opener indent, depth-before, prior sibling level), popped by paren balance; the col-0 law suspends while open (closer-alone lines join at any indent; a line LEFT of the opener is a named leak error — weir-stricter, divergence lambda-body-offside; body AT the opener indent is a continuation, F#-parity); on pop the sibling level restores to min(opener, prior) so a block sibling gets `;` while a fold's init still applies (the fuzzer's first new-grammar catch + the e2e counter-catch, both pinned); lambda bodies inherit the letCmdOk spine and params extend the ambient resolver (the test-counts regression pinned); sigil-interior premise CORRECTED: multiline continuation inside $() was already legal — the single-LOGICAL-line law holds | PLAN-multiline-lambda; divergences row lambda-body-offside; fuzzer grammar + lambdaSingle transform |
| shared-flags | 2026-07-24 | shared CLI flags by CONTAINMENT: Args.load's field law gains "at most one union-typed field" — the subcommand slot (name immaterial, derives no flag); scalar siblings are shared flags recognized anywhere on the line (float), the first non-flag token anchors the case, payload flags bind after it. Cross-tier name/explicit-short collisions reject at CHECK (declared once — reject-don't-guess at the schema); derived shorts run per case scope over the UNION of tiers (contested derives for neither); validation collects across both tiers into one boundary error with tier-aware did-you-mean; --help is two-tier and case-scopes. [<Inherit>]-style per-payload marking REJECTED (preserves the repetition); nested hierarchies parked (one anchor level is the budget). Existing record/union shapes zero-diff | PLAN-shared-flags; the port's extraction matches deleted; TRANSCRIPTION addendum |
| arm-commit | 2026-07-24 | a consumed arm-`|` commits to its arm (the consumed-separator law's second instance; one line, the attempt retired): a failing arm RHS reports at ITS site — the bare-pipe fatal never receives the counterfeit "completed expression" the backing-out arm list manufactured; weir now beats FCS on the repro (F# reports the NEXT arm's line). Probes corrected one premise: a bare `|` in an arm RHS is an ARM SEPARATOR (F#-parity), command chains in arms ride `$()`; or-patterns confirmed NOT a feature (F# accepts) — divergence row or-patterns filed. GRADUATION: strict spans default-ON in the fuzzer; the two span pins flipped to true-site. The first strict deep run found the law's THIRD instance same-day — the record literal's whole-attempt rewound deep field junk into the update alternative's dump; fixed in-session (the literal commits on its `ident =` head, `==`-guarded; updates keep their path, trailing `;}` was already a reject) | SEMANTICS: the consumed-separator law (one sentence, all instances); divergences or-patterns; strict-spans graduation |
| seq-commit | 2026-07-24 | a consumed `;` COMMITS to its element (the consumed-`|` fatal's sibling; the attempt-wrapped `; elem` retired): a failing element can no longer un-consume the `;` and re-parse OUTSIDE its let-in scope — the backtrack door check's assume-resolver monetized into the phantom-command verdict split (the fuzzer's find; blessed as "the assume-resolver fix", DIAGNOSIS CORRECTED in-session: the resolver's known-name refusal was already right, the door was the sequence backtrack; probes: tenv-known junk errored correctly all along, the block/`in` spellings both swallowed, scope legitimately spans `;`). Check≡run on the class; the district span class fixed FOR FREE (primary now anchors on the junk); deep fuzz green; the law's second instance is [D:arm-commit] | fuzzer verdict-split pin FLIPPED to fixed; district span pin flipped to the gain; PLAN-semantic-tokens gate |
| semantic-tokens | 2026-07-24 | the mode boundary made visible: textDocument/semanticTokens/full over the checked pipeline — mode spans ONLY (weirCommandHead/weirArgv/weirSplice; expression land emits nothing, TextMate keeps lexical coloring — the minimal legend is the minimal claim); the synthetic-span rule (emit only where the logical slice sits VERBATIM at its translated physical home — district wrapper glyphs and joins emit nothing); reified chains recognized through their desugar spine; sigil-origin heads scanned past the glyphs; error-state files emit partial truth; bareword argv only (quoted/raw/interp args stay lexical strings); case-scoped-help precedent extends: whole-file recompute per request (no delta/range, parked). Client: semanticTokenScopes to standard TextMate scopes (argv = string.unquoted — the fish/nu inert-words convention). REPL rider: the same three-way tint from the same resolver (dim argv, stage un-dims at `|`) | PLAN-semantic-tokens; park CLOSED (trigger: the verdict-split receipt); lsp-e2e token probes |
| reifier-family-complete | 2026-07-24 | the family closes under ONE law — output goes where the meaning goes: succeeds silent/bool (predicate), complete captured/record (inspection), orFail STREAMS/unit-raise (assert — the capture-and-replay retired as accident-grade: no pin held the silence, only the message desugar was pinned), exitCode STREAMS/int never-raises (control flow; NEW — the old park opened on its own criterion: complete carries the code CAPTURED, the missing cell was the code STREAMED). Streaming reifiers refuse capture/discard contexts at parse with teaching errors (sigil/bang/statement/district — four cells pinned); single-segment family rule holds; `!(cmd)` ≡ `cmd | orFail msg` stream-byte-identical (pinned); env twins exitCodedEnv/expression builtins follow the completed precedent; the match-on-130 idiom is the taught spelling | PLAN-reifier-family; SEMANTICS: the reifier output law; both NOTES-agent frictions closed |
| spawn-spec | 2026-07-24 | the internal spawn spec: ONE record (Prog/Args/Env/Input) + one starter behind the whole command-value family; the output axis is the CONSUMER function (linesOf/streamCodeOf/completedOf — the reifier law restated in code), public wrappers keep their signatures as thin constructors. Behavior-preserving by contract: ZERO pin edits across the rebase, both byte-identity pins (run ≡ cmd\|>print, !() ≡ orFail-default) re-ran unchanged as the structural proof. `feed`/`feedEnv` land as the seventh and eighth constructors — cmd + stdin, the family's first data-LAST member (its data is the pipeline's subject: `snips \|> feed "sha256sum" []`); input pulls LAZILY on the writer task (pull-count pinned — the laziness rule's first INPUT customer), stdin closes on exhaustion (EOF pinned). A user-facing spec type stays PARKED with the design banked — the internal spec makes that session an EXPOSE, not a build | PLAN-spawn-spec; NOTES: the family-audit arc |
| default-attr | 2026-07-24 | `[<Default v>]` moves the RESTING POINT (Args.load consumer): string/int fill the literal (missing-required shrinks; help shows `default: v`); `Default true` on bool mints the `--no-X` twin (resting on; `--no-x` sets false; positive idempotent; both polarities error naming both; minted names join COLLISION checks and did-you-mean but never SHORT derivation — ruled at bless); rejection cells with teaching text (false-on-bool redundant, Default-on-Option contradictory, literal≠field, the subcommand slot); Positional's not-yet WINS the composition. CASHES the attributes session's field-defaults door prediction — the LANGUAGE door (defaults in type declarations) stays shut, its customers served by the attribute at zero grammar cost; Env.load consumption is the gated follow-up (no twin there — env bools are text) | PLAN-default-attr; PLAN-attributes door entry (the pointer added); fuzz.weir the receipt |
| env-default | 2026-07-24 | Env.load consumes `[<Default>]`: an absent var fills the literal (the resting point sits BELOW the whole overlay stack — any set var wins, incl. runEnv/fromFile overlays becoming the child's env; four-layer e2e); missing-required shrinks; NO twin mints (env bools are true/false TEXT — no presence semantics; the pre-note confirmed). THE FLIP, pinned both sides: `Default false` on bool is LEGAL at Env (absent→false is a real statement) and REJECTED at Args (presence rests there already) — the sharpest demonstration that validation is the CONSUMER'S arm; Option-contradiction and literal-mismatch cells re-derived with the same teaching text. Receipt: bicep-deploy's BOTH defaultValue dances deleted (detachAll, the auth toggle's 0) — user pushed the toggle to a literal default too; the absent-is-data living example is fuzz.weir's computed seed | PLAN-env-default; the second field-defaults customer served — the language door stays shut |
| script-path | 2026-07-24 | `scriptPath : string` — the running script's ABSOLUTE path, resolved at STARTUP against the invocation cwd (before any cd — pinned), symlinks unresolved (bash-$0; realpath via the command when needed); script-only like args/stdin with its own teaching refusal in the REPL/-e; shebang scripts get the SCRIPT's path (three-invocation e2e: relative/dot/absolute one answer, PATH-shebang the script not the interpreter). One primitive — the dirname idiom is `\|> Path.dir` (already shipped; the plan's Path.parent verify resolved to it), no scriptDir builtin. OPENED BY SEQUENCING, not receipt count (Path.glob's customers are script-relative; recorded per the user-fiat precedent); fuzz.weir deliberately keeps git-toplevel | PLAN-script-path; the $0 friction half closed |
| path-glob | 2026-07-24 | RECATEGORIZATION first: the injection-safety exclusion was about BAREWORD EXPANSION IN ARGV — nothing expands, ever; `Path.glob : string -> seq<string>` is a typed, visible function outside that rationale (the no-globs LAW amends, not retires: bare `*.txt` in argv stays a literal word). Semantics: the standard subset (`*` `**` `?` `[abc]`/`[!abc]`), bash's dotfile law (`*` skips dotfiles, `.`-segments match), sorted per level, LAZY against the cwd at enumeration (the cd seam taught with `\|> Seq.force`), empty seq on no match (the match-`[]` idiom taught), unreadable dirs SKIP (discovery not assertion), `**` NEVER traverses symlinked dirs (bash ≥4.3 globstar — the plan's follow-with-visited-set bracket was STALE bash; premise corrected, loop-immunity by law) while explicit segments follow links. HAND-ROLLED: FileSystemGlobbing fights the dotfile SPEC (its `*` matches dotfiles, no per-segment surface) and is unrestorable offline — the fallback clause taken, reported. No brace expansion (bash-ism, parked). The argv-splat park gains its second receipt (glob-into-git-add) without opening | PLAN-path-glob; SEMANTICS amended; the dbt reshape unblocks |
| seq-distinct | 2026-07-24 | `Seq.distinct : Eq a => seq<a> -> seq<a>` — lazy, first-occurrence-wins, remembers only what it has yielded (pull-count pinned: first-2 over a counted dupe source pulls exactly to the second novel element); the Eq constraint rides the class machinery (functions/seqs rejected at use with the standing teaching); pulled forward from the stashed miner proposal by the glob product cell it unblocks (overlap dedupe, e2e'd). FINDING for the proposal: Seq.groupBy ALREADY EXISTS — its "no countBy/groupBy" premise was stale | the miner proposal loses another item (feed, distinct now landed) |
| argv-splat | 2026-07-24 | `$@name`/`$@(expr)` splices N argv words (bash `"$@"` semantics — each seq<string> element ONE word, never re-split/re-joined; `$*`-rejoining unrepresentable). The one-splice-one-word law SHARPENS (loses its silent asterisk): `$x` one word, `$@xs` N words. Empty seq → ZERO words (replaces bash's conditional-flag idiom, pinned by argc AND behavior). Type-demand seq<string> exactly, twin teachings (scalar → `$x`; seq<int> → map show); enumerated ONCE at spawn (splat forces by necessity — the when-do-I-force line). REJECTED at the head (N heads incoherent — computed-head park named) and mid-word (`--flag=$@xs` — N words can't build one), each naming its fix. `$@"` reserved for the parked interp-verbatim opener (lookahead-decided, both sides pinned so the park inherits a held boundary). PARK CLOSED (proposed→parked→two receipts→opened by call). Fuzzer owed: the splat↔inline law (`$@([a;b])` ≡ inline words) rides a deep run; tokens color the form; POSITIONS swept | PLAN-argv-splat; SEMANTICS the argv word law; the splat park closes |
| range-probe | 2026-07-25 | the range-vs-list probe fails FAST on a `[`/`{` opener (a range endpoint is never a list/record literal): without it, `rangeBody`'s `attempt (rangeTerm .>> dotdot)` descends into nested brackets, fails at the missing `..`, backtracks, and the list path RE-PARSES — O(2^n) on `[[[…]]]` (measured: each +1 depth doubled the time, 607ms→9s over depths 18–22). The one-line `notFollowedBy (anyOf "[{")` commits to the list path on the opener (the consumed-separator law's shape for range detection). Cost: a list/record as a range START (`[[1]..[3]]`, nonsensical, type-error before) is now a parse error — unpinned, no legit range starts with a bracket | safe-by-design totality find #3; the bracket hang; unit Depth-guard + fuzz depth axis |
| depth-guard | 2026-07-25 | unbounded expression depth was a SAFETY bug — a memory-unsafe SEGV (rc 139) in a safe-by-design language. TWO overflow seams, ONE ceiling (500, ~45x the corpus max of 11, ~10x below the ~6000 crash floor): the recursive-descent parser dies on deep NESTING (parens/brackets), so `deepen` (a ThreadLocal depth counter on `atom`) THROWS `DepthExceeded` past FParsec's error protocol (a failFatally there gets swallowed by the surrounding attempt/choice backtrack) → parseLineFull catches it as a located diagnostic; the checker/evaluator's tree-walk dies on a deep left-SPINE (`1 + 1 + …` parses shallow, builds a deep AST), caught by a post-parse ITERATIVE (explicit-stack, crash-proof) depth gate in parseLineFull. Diagnosis corrected the plan's "one parse-depth guard" premise: the operator segfault is in CHECK, not parse (fmt=0 at 20k, check=SEGV), so the seam is post-parse not in-parser. Acceptance: no-crash + diagnose pinned in unit (499 ok / 501 rejected / message) AND the fuzzer's totality invariant gains a DEPTH axis (the three exact fixtures as seeds — the honest denominator the review named, its generators favored breadth) | safe-by-design totality finds #1+#2 → CONFIRMED; SECURITY.md Property-3 flip; the no-crash safety invariant |
| diag-arbitration | 2026-07-25 | which error the user sees: ONE policy stated (SEMANTICS "Diagnostics"), ratifying the mechanism the commit law already produced rather than building a new arbiter. "weir reports the error at the furthest point the parser REACHED" — furthest-reached, NOT latest-in-file (DIAGNOSIS CORRECTION: two junks in one statement report the FIRST; the plan's "deeper wins" acceptance was wrong, caught by the new two-error fuzz property failing then flipped to first-reached-owns-the-report). Position always PHYSICAL by construction: the consumed-separator law [D:seq-commit][D:arm-commit] removed the backtracks that rewound deep failures into shallow synthetic-anchored ones. Two DISTINCT channels, NOT unified (the bypass-generalization EVALUATED and REJECTED): teaching hints (failFatally) override the MESSAGE at their own furthest position — barePipeHint KEEPS its fatal, it is the correct same-position override, not a workaround (archaeology: its misfire was a backtrack, fixed by the law); the depth guard's THROW [D:depth-guard] escapes discardable speculation (fires inside attempt/choice that backtracks) — reserved for that, never generalized (throwing out of a should-retry branch would break the grammar). One pipeline (analyzeLines) → check/--json/LSP one policy. Zero behavior change (the instances were already true-site); zero pin movement | the two invariant-3 repros were ALREADY true-site (plan premise stale — seq/arm-commit flipped them); noteHit fuzz hatch retired (dead post-commit-law); new two-error arbitration property; SEMANTICS Diagnostics section |
| rmatch-all | 2026-07-25 | `Str.rmatchAll pat s : seq<seq<string>>` — the plural of `rmatch`, every match's group seq LAZILY (Match/NextMatch walk, a match computed only on pull); no Option (absence IS the empty seq — the plural needs none). Same contract as rmatch otherwise: any string pattern, groups as strings, `(?s)`/`(?m)` inline flags cover DOTALL/MULTILINE so no options API grows. Str.-qualified only (not a bare alias, like rmatch/isMatch). The whole-file-as-string question RESOLVED to the existing `Str.join` (`File.read \|> Str.join "\n"` composes) — `File.readText` stays parked (no receipt). ACCEPTANCE: corpus-mine.weir (the last python tool, retired) reproduces the miner's published numbers on the committed corpus EXACTLY — base extracted=4253/unique=3700/kept=76, wide kept=102, kept content-SET byte-identical in both modes; the three receipts exercised (rmatchAll all-matches, Seq.distinct dedup-as-pipeline-stage, feed value→sha256sum stdin). Note: feed writes lines so sha256sum sees `src\n` — filenames differ from python's `sha256(src)` by that trailing newline, but dedup partition and the content-set are unaffected (dedup is by content via Seq.distinct, not by hash) | PLAN-miner-receipts DONE (feed/distinct/scriptPath already shipped ahead); tools/ python-free; SKILL scrape idiom; rmatchAll pull-count pin |
| drop-positional | 2026-07-25 | `[<Positional>]` UNREGISTERED — dropped from the attribute registry, its not-yet consumer deleted; `[<Positional>]` in source is now the unknown-attribute error with did-you-mean over the remaining names (Short/NoShort/Doc/Default). It was registered as a pre-scoping not-yet, but the positionals park is dropped without opening: its ONE receipt (git-subrepo `config <key> [<value>]`) was CONTRACT-MIMICRY from model-authored code reproducing git's CLI, positional CLIs are what the training distribution overwhelmingly contains (argparse/click/getopt/man pages), and NO weir script was ever blocked (config was skipped, nothing broke). The names-beat-positions house rule (records-over-tuples, extended to argv) stood; three weeks of real scripts produced zero organic demand. A registered name reserving syntax for a feature whose only receipt was model idiom is SQUATTING, not pre-scoping. `--` never landed (flag-termination was only planned with positionals) so it goes unmentioned. Re-askable against a receipt from HAND-WRITTEN weir that a flags reshape genuinely cannot serve. The typed/declared/help-generating path is dropped, NOT the ability — the untyped floor (`args`, `Args.flag`/`Args.value`) remains | not-yet + registry-membership pins flipped (one now asserts unknown-attribute); PLAN-attributes four-name clause = 3 bound/1 dropped; PROCESS receipt-provenance lens; no PLAN-positionals.md existed |
| value-headed-pipe | 2026-07-25 | `xs | prog args` — the bare spelling of `feed`, a value piped into an external command's stdin (`snips | sha256sum` ≡ `snips |> feed "sha256sum" []`, byte-identical). RESOLUTION DECIDES, extended one step: after an EXPRESSION LHS, a bare `|` whose head resolves EXTERNAL is the value-headed pipeline; a known-binding/library head keeps the barePipeHint teaching (`'|' chains commands; pipe with '|>'`) — the hint SHARPENS, firing exactly on expression-into-expression. KEY FINDING (diagnosis): `EPipe(seqExpr, ECmd)` ALREADY type-checks (arg bound seq<string>) and evals to `Proc.linesWith ... (Some stdin)` — the whole feature is a PARSER change (seed the command-chain fold with the LHS expression), no new runtime, no feed-desugar needed. `feed` STAYS (computed program names). LHS demand seq<string> exactly with twin teachings (scalar → `[x]`/show; seq<int> → map show), shared by command chains and value-headed (one EPipe-into-ECmd arm). SCOPE (session-1 of the feature): streaming pipelines (single + multi-external) at expression positions (let-RHS, statement, captures) SHIPPED. DEFERRED as separable chunks: reifier-with-stdin (`xs\|grep\|complete` — needs stdin-carrying reifier builtins; today gives the same "single external segment" rejection as a multi-external reifier, no new law), sigil-interior `$()` value-headed (redundant — a value-headed pipe is already an expression), semantic tokens for the head (recognizer doesn't walk EPipe-into-ECmd), fuzzer value-headed shapes, district message polish. The parked fzf shape (`$(git branch)\|fzf`) works for the streaming case. SESSION 2 (2026-07-25) CLOSED the deferrals: reifier-with-stdin (internal stdin twins completedIn/succeededIn/exitCodedIn/orFailedIn — public spellings ZERO-DIFF; `xs\|grep\|complete` reifies WITH stdin; multi-external still rejects, no new law); semantic tokens ALREADY worked (the walk recurses childExprs — session-1 "empty" was a bare-statement DISCARD, a diagnosis-not-fix finding); fuzzer gained `xs\|prog` ≡ `xs\|>feed` (dedicated property, GRAMMAR invariant 5); district/sigil value-headed now TEACHES (the `[`-head fail message, which only surfaces in command-only contexts); sigil `$()` value-headed confirmed NON-parsing (redundant — reported not fixed). Spawn-park pressure noted: the reifier row's stdin column is now populated by internal twins (no new user-facing NAMES, park stays shut; a TENTH pressure makes the expose-not-build session overdue) | park OPENED by a hand-written receipt (the miner's `feed "sha256sum"` — provenance: hand-written, the lens says weight it); corpus-mine.weir sha256 site rewritten to `\| sha256sum`; e2e + unit pins |
| drop-command-builtins | 2026-07-25 | The expression-position command tier RETIRED — `cmd`/`run`/`cmdEnv`/`runEnv`/`feed`/`feedEnv` (six of the [D:spawn-spec] constructors) removed; every command head is now a LITERAL program name, resolved at CHECK time (no computed-head tier). ARGUMENT: the tier's sole job was a COMPUTED head (a program name held in a variable); a GREP-GATED audit of the whole corpus (e2e + tools + tests) found ZERO computed heads — every real use had a literal head that reads better as a command line (`$(sh -c ...)`, bare `git status`, value-headed `xs \| sha256sum`). Tool substitution by runtime condition BRANCHES the whole line (`if hot then rg pat else grep pat`), clearer than a computed head anyway. NO retirement hints (experimental phase — pure subtraction, not a teaching machine, per user); dead impls left in place (no FS warning). SCOPE — the six "clean" names NOW; the REIFIER family (`completed`/`succeeded`/`orFailed`/`exitCoded` + Env/In twins) STAYS: they are dual-use desugar targets emitted by foldChain (`\| complete` etc.), so their drop is an internal rename, its OWN session (session 2 of the drop). This retires the head-tier half of [D:spawn-spec] and the `feed` clause of [D:value-headed-pipe] — the value-headed pipe ITSELF stays (it needs no feed; GRAMMAR invariant 5's feed-equivalence law goes with feed). SPAWN-PARK reframed: the reopen axis is no longer "a ninth NAME" but SCOPES-VS-ARGUMENTS — a value that must vary the child's ENV or STDIN still routes through internal spawn-spec twins (no user-facing name); the park reopens only if a value must vary the HEAD and cannot be branched. RE-ASK: a hand-written weir script whose computed head is genuinely open/unbounded at author time (the head set cannot be enumerated into a branch). | NOTES: "Dropping the computed-head tier"; SEMANTICS the sugars ledger + command-line + child-env entries; e2e + unit + fuzz pins |
| keep-reify-builtins | 2026-07-25 | Session 2 of [D:drop-command-builtins] (hiding the reifier builtins `completed`/`succeeded`/`orFailed`/`exitCoded` + Env/In twins behind un-typeable internal names) REJECTED at execution — the plan's premise was WRONG: it mapped the tier by NAME, not capability. `completed "git" argv` has a LITERAL head; what's computed is the ARGV, which the head law never spoke to — hiding them buys zero law-level gain and removes the only computed-argv reification path. `\| complete` cannot take that role: `$@` is confined to argv assembly, and a reified chain's arguments desugar OUT of argv — receipt: the flagship's commitTreeAs (`completedEnv author "git" argv`, variable-length parent list; unrewritable by construction). The builtins keep their user-facing role; the `complete`/`completed` homonym stays (distinct jobs — chain stage on a literal command vs function reifying a computed argv; one SKILL line, no rename). RIDER (found by the session's own probe): splat-in-reifier was an UNHANDLED CRASH (the checker's ESplat unreachable arm) — now a located parse rejection at foldChain naming the confinement and the builtin escape, all three routes (plain/env-sigil/value-headed); the let-RHS bare-command route backtracks to a generic parse error, family-uniform with the multi-external rejection. Second rider: the head-splat teaching still named the retired `run`/`cmd` — now names the literal-head law (branch the whole line). RE-ASK: only with a splat-through-reifier design that preserves argv-assembly soundness. | NOTES: "Session 2 dies on its own gate"; SEMANTICS the sugars ledger + the argv word law; unit pins |
| splat-reifier-chains | 2026-07-25 | `$@` rides reifier chains: `$author(git $@argv \| complete)` — literal head, splatted argv, sigil env, one syntax (the [D:keep-reify-builtins] re-ask answered same day, WITH the demanded soundness argument). DIAGNOSIS FIRST (stated before coding): implementation gap, NOT a law change — a reified segment IS a spawn with an argv; the builtins' `argv : seq<string>` maps element→one word (Seq.map asString → Proc), the SAME boundary as spawn-argv-build, so the splat was already semantically in-scope and the old crash was representational (EList has no N-word slot). FIX: foldChain desugars mixed literal+splat argv to the seq VALUE it denotes — non-splat runs chunk into list literals, splat interiors splice whole (keeping the full `$@...` span), folded with Seq.append (the range-literal desugar's node). ESplat never leaves argv in the AST — the confinement pin holds LITERALLY; zero checker/eval changes; splat-free chains keep the identical old node (zero pin movement). The interim splat-ride rejection (the keep-reify rider) RETIRES, replaced by behavior. Products: all four reifiers × env sigils × districts × value-headed × empty splat (zero words); THE safety pin re-run through the reifier path (adversarial words argc=3, unit + e2e); fuzz invariant 6 (splat ≡ inline words, byte-identical, dedicated generator); tokens color splatted chains (Lsp walks the append fold; In twins joined reifierHeads — pre-existing gap, rider). Part 0 finding: `\|> _.Stdout` already parsed in pipe position (docs-only win). Flagship commitTreeAs rewritten to the sigil spelling — the argv-assembly line DELETED. Nuance on record: a wrong-typed splat interior in a reified chain gets the generic unify error, not argv's twin teachings. PART-2 INVENTORY (report, no code): the flagship was the corpus's ONLY builtin call site and it converts; remaining uses are docs demonstrations. Every site converts ⇒ the hide/drop question REOPENS on its merits for a SEPARATE bless (homonym resolves, family shrinks, internal targets take un-typeable keys). | NOTES: "The splat rides"; SEMANTICS the argv word law + sugars ledger; GRAMMAR invariant 6; unit + e2e + fuzz pins |
| drop-reify-builtins | 2026-07-26 | The reify builtins (`completed`/`succeeded`/`orFailed`/`exitCoded` + Env twins) RETIRED as user-callable names; the desugar targets renamed to un-typeable `\|`-prefixed keys (`'\|completed'` etc. — identifiers are `[A-Za-z_]..`, so the chain grammar alone reaches them; foldChain re-aimed, suggestion/completion pools filter via Types.isUserName). THE ARCHAEOLOGY, three parts, because this is the third pass at these names: (1) THE WRONG REASON, dead — session 2 believed they were the computed-HEAD escape hatch; they were not (literal head; the tier was mapped by NAME not capability); it executed, the battery killed it at the flagship, reverted whole [D:keep-reify-builtins] — do not revive that argument. (2) THE RIGHT REASON, live — `$@` rides reifier chains [D:splat-reifier-chains], so the computed-argv job the builtins uniquely held spells `$author(git commit-tree -m $msg $@parentArgs $tree \| complete) \|> _.Stdout`; the builtins are REDUNDANT, not wrongly-scoped, and the `complete`/`completed` homonym dies with them. (3) THE INVENTORY, total — the splat session's Part-2 sweep: flagship commitTreeAs was the corpus's ONLY call site and already converts; tools zero; residue was docs demonstrations (converted here). NO retirement hints — PHASE-SCOPED, not permanent policy: in the experimental phase teaching retirements teaches a fiction; the first external user makes removals teachable again, `retiredBare`'s original three are the precedent to extend, and the phase is about to change. Zero behavior change to the chains (targets renamed, not re-specified; four routes re-pinned byte-identical); un-typeability PINNED (binding/calling `'\|completed'` is a parse error; no `\|`-leak in didYouMean). | NOTES: "The names go, third pass, right reason"; SEMANTICS sugars ledger + the complete entry (homonym clause retired); unit + e2e pins |
| lsp-requests | 2026-07-26 | textDocument/formatting + textDocument/definition land in `weir lsp` (the editor arc's Session 1). FORMATTING: runs on CLIENT-SENT text only (the buffer is the truth; the SECURITY non-claim stays exact); the SAME Fmt.formatLines pipeline as `weir fmt` — one whole-document TextEdit; editor options (tabSize) IGNORED, fmt is canonical. Verified in-session: formatLines is total-ish on broken buffers (unparseable statements ride VERBATIM, indent still normalizes — format-on-save on a broken file stays useful); only an ASSEMBLE failure refuses → the LSP answers no edits. Both paths pinned. DEFINITION v1: top-level `let`/`let`-pattern bindings only, NULL otherwise — params/match binders/block-lets carry no binder spans in the checker, and never-wrong beats sometimes-wrong; builtins null (no source). Mechanism: the hover plumbing (toLogical → nodeAt → TEVar) + last-binder-above scan (shadowing falls out) + textual binder column bounded by the first `=` + Script.translate back to physical; factored as pure `definitionFor` (the semanticTokensFor precedent — handlers are not unit-testable). PARKED with a three-customer criterion: binder spans in Check are the shared prerequisite for full definition AND rename AND references — one medium session, reopened when any of the three is demanded by a real user. End-to-end: nvim headless (edit byte-identical to `weir fmt`; definition range lands on the binder) + helix pty (`:format` rewrite, `gd` jump proven by an insert-marker at the landing site); the helix false-alarm was the DRIVER (helix's `w` leaves the selection head on trailing whitespace — the server's null-for-a-space was correct; gd sends the selection HEAD). EXTENDED same window (user follow-up): record FIELDS (access via TEField + literal field names via word-at-cursor on TERecord) and union CASES (expression-position TEVar-uppercase AND match-PATTERN position — Pattern carries PSpan, so no new machinery) resolve to the KType declaration site (textual member search bounded by the decl's first `=`; joined text spans multi-line decls, translate maps back). Payload binders stay null (local binders — still the binder-span park; the park's scope SHRINKS to params/local lets/payload binders). Builtin types (Completed/FileRow/...) null — no declaration in the script. | NOTES: "LSP requests"; docs/editors.md matrix; unit pins ×2 + the nvim/helix rigs |
| depth-stack-probe | 2026-07-26 | The depth guard [D:depth-guard] gains a STACK PROBE: `deepen` also raises DepthExceeded when `RuntimeHelpers.TryEnsureSufficientExecutionStack()` reports <~64KB headroom (the primitive System.Text.Json's recursion guards use — NativeAOT-safe). ORIGIN: the first cross-platform receipt — `dotnet test` on macOS (Debug, arm64, test-host worker thread) STACK-OVERFLOWED at ~420 of the 500-deep at-ceiling fixture; the guard's "margin for smaller stacks" was a constant measured on Linux 8MB stacks, and a measured constant is not a contract. DIVISION OF LABOR, restated: the probe owns SAFETY (any depth, any stack, any platform); the counted ceiling is now purely a COST + PORTABILITY bound — deterministic same-verdict-everywhere above 500, bounded ASTs for the probe-less downstream walks (check/eval/fmt/Lsp recurse over the parsed tree with no probes; the parse gate is their single choke point). The 500's only load-bearing consumer is SPINE/PIPELINE length via the post-parse gate (spines parse shallow, produce deep ASTs — a probe cannot help there); nesting-wise the corpus max is ~11. The user's could-it-be-lower question answered: ~200 would be defensible but no receipt forces it — RE-ASK: a real script rejected by the gate; the change is one line with its own ledger entry. Capacity BETWEEN the probe floor and the ceiling is platform-dependent BY DESIGN — the over-pinned "499 must parse" flipped to the law (parse or diagnose located; a crash is the only wrong answer). Second macOS finding, same run: BSD `wc -l` left-pads — the into-test pinned GNU formatting, not weir plumbing; now `wc -l \| tr -d ' '`. | NOTES: "The first macOS receipts"; unit small-stack pin (512KB thread, would have crashed pre-fix) + e2e AOT pin (ulimit -s 512 → located diagnostic, rc<128) |
| vacuous-probe-audit | 2026-07-26 | The harness-wide sweep for probes that test nothing — verification-only, zero src/ moves. ORIGIN: the zombie pin was VACUOUS FROM BIRTH (GNU `ps --ppid` on BSD errored → `grep -c Z` → 0 → pass), the worst masked-failure genus: nothing broke, the claim was just never measured. THE BAR (now in PROCESS): every shelling-out probe is PORTABLE, LOUD (its own failure = a named test failure, never a benign value — zero must never be both the pass and the probe's failure mode), and POSITIVE-CONTROLLED (shown to fail on deliberately-bad input, committed; else flagged "loud but uncontrolled" in writing). DENOMINATOR: 18 probe sites/groups examined; 6 vacuity matches — `survivors` (pgrep-missing → benign 0; now exit-9-loud + a live-marker control), `defunctChildren` (the origin; now `defunctChildrenOf` + a COMMITTED python fork-no-reap zombie control — .NET auto-reaps our own children, so the control targets another parent; bash reaps bg jobs, so the sh-zombie shape does NOT work as a control), deep-lock's unreadable/garbage lock (was silently CLEARED-as-stale — the exact race it closes, reopened on EACCES; now exit 3, lock preserved, e2e-pinned), publish.sh's check consumers ×2 (probe-broken rc read as FREE → proceed; now any rc≠1 refuses), the Tests.fs:1499 marker check (asserted a marker no process ever carried — self-labeled "trivially true"; the marker now rides the command), e2e's own /etc/hostname (the unit fix's twin, landed via the fork f6ce026). CLASSIFIED SOUND: check-fresh (fail-safe — probe failure lands STALE-side), skill-doc (zero-blocks loud + weir-error blocks are built-in positive controls), harness.py (harness-selftest IS a control suite: real zombie vs kill(0), stale stub), fuzz Runner gate (incident-controlled this window — the 12-failure stale event), timing (loud under -e; GNU date %%N on the macOS pile), fuzz equality detector (graded control at bring-up), e2e structure (set -euo pipefail + two-sided errout&&fail), timeout polyfill (controlled at introduction). LOUD BUT UNCONTROLLED, stated: survivors' exit-9 path (no committed pgrep-breaking input); the tree-sitter corpus bar is UNCOMMITTED (session-run; if it becomes standing it takes the template). NEWLY-RED PROBES: none — the instruments were wrong, the claims happened to be true. Cross-platform-as-instrument recorded in PROCESS. | NOTES: "The vacuous-probe audit"; PROCESS the bar; e2e garbage-lock + the two unit controls |
| argv-rules | 2026-07-26 | The Eval argv runtime twins unified — the dedupe sweep's order-observable finding, executed oracle-first: `argvDup` (polarity/given-twice), `argvParseValue`, `argvFill` (the resting-point fill) each had ONE verbatim copy per twin (argvParseRecord + argvLoadShared); now one implementation each, the accumulators passed as PARAMETERS (never closure captures) so problem ORDER stays each caller's own — the twins kept their loops untouched via partial-application aliases. THE ORACLE (before any edit, against the pre-extraction binary): four exact-string e2e pins — record twin scan-then-fill order, polarity conflict, two-tier token-order scan crossing tiers, fill order shared-then-payload — all byte-identical after. ORDER VERDICT: the twins already AGREED (scan problems in token order, fills in declaration order, shared tier before payload) — no canonicalization needed. PLACEMENT: all three rules in EVAL (run-time token parsing + Value construction); the Default POLICY they consume (Argv.defaultOf) stays check-side in Argv.fs beside the flip — the line unchanged. OBSERVED FINDING, preserved not fixed: a bad int yields TWO problems (not-an-int at scan + missing-required at fill — the value stays unset); pinned as-is, a message-dedup would be a behavior change. Net lines ≈ 0 (explicit accumulator signatures + comments ate the ~60 estimate) — the win is single-implementation at the collect-everything boundary, not size. | NOTES: "The argv twins"; the four e2e order-oracle pins |
| capture-buffer | 2026-07-26 | `\| complete` capture representation: ONE fixed-size segment store (4MB byte[] chunks, no doubling/no final copy — a growable array's doubling+trim TOUCHED ~3x the text and MaxRSS keeps LOH pages) + two-pass EXACT (start,len) int arrays; `Completed.Stdout/Stderr` are lazy VIEWS decoding a string per pull. Observable type unchanged (already seq<string>); zero pin movement across the battery. ORACLE-FIRST: the exact old rules pinned against the OLD binary BEFORE the change — stdout = ReadLine (\n/\r\n/lone-\r, empties kept, unterminated tail); stderr DIFFERS (\n-only split, empties DROPPED, \r retained); UTF-8 BOM stripped; invalid byte → one U+FFFD (per-line GetString ≡ streaming decode: newline bytes are hard UTF-8 boundaries); UTF-16/32 BOM SWITCHES decoding (StreamReader detect) — preserved via a rare-path StreamReader fallback. NUMBERS (seq-1..4M fixture, 29.5MB text): 555MB→82MB MaxRSS (18.4x→2.7x incl. baseline+enumeration; capture-held 2.13x — the ~2x target; this fixture is offset-pathological at 7.7B/line), wall 0.92s→0.42s (2.2x FASTER), runs under a 100MB GC hard ceiling. DECIDED: int offsets (single capture caps ~2GB — the old repr met OOM at a fraction of that text); lifetime = a view holds its buffer while referenced (same data the old lists held); re-enumeration STRENGTHENS (fixed buffer: stable, cheap, cannot become effectful); NOT interning; NOT bounding (the SECURITY non-claim stays, its number now ~2x). | NOTES: "Capture representation"; SKILL + SECURITY numbers updated; the 4 oracle pins |
| keyword-completion | 2026-07-26 | The dedupe sweep's keywords finding, VERDICT: DRIFT — Complete's hand-kept suggestion list dated to Spike 6 (2026-07-12), BEFORE if/then/else (07-18), elif and when existed; completion silently under-offered control flow for six weeks, and no comment or row ever claimed the exclusion was curated. FIX: completion DERIVES from `Parser.keywords` (one source of truth — the same-policy-twice pattern closed by derivation, not by synchronizing two lists) minus an explicit reasoned exclusion set: `rec`/`mutable` (reserved words with no meaning) and `function` (reserved for the parked match-lambda sugar) — offering any suggests a spelling whose only outcome is the reserved-word teaching error. `in` stays OFFERED (the explicit let-in form is legal exactly where completion lives — the REPL and -e). THE DURABLE HALF: the inventory pin — the offered/excluded SPLIT is pinned as data, so a new grammar keyword FAILS the test until its completion decision is recorded (the inventory-match pattern from the highlighter grammars, applied to completion; derivation alone would auto-offer silently). One pre-existing pin legitimately gained an entry, named: suggest "whe" now yields when+where. | Complete.fs the exclusion reasons; the inventory pin in completionTests |
| env-enums | 2026-07-26 | Env.load accepts 0-ARITY UNIONS as enum fields (LOG_LEVEL: Level) — the boundary conversion the field law was missing: a typo becomes a check... runtime boundary error WITH CANDIDATES ("expected one of: Debug, Info, Warn" + a hint) instead of a wrong branch later. Field law clause: monomorphic union, every case 0-arity; Option<enum> rides. THE MATCHING DIVERGENCE, deliberate and stated: Env matching is CASE-INSENSITIVE (env convention is uppercase — =DEBUG/=debug/=Debug all select Debug) while Args subcommands stay LOWERCASE-VERBATIM (`c.ToLowerInvariant() = tok` — `git subrepo clone`, never CLONE); two conventions, two rules, adjacent by [D:key]. Check-time rejections ×3, each teaching: payload-carrying case ("env values are single tokens... case 'Carry' of LvlB carries a payload"), same-cased pair collision ("collide as env value 'debug'" — the Args collision's env sibling), Default-on-enum (attribute literals are string/int/bool — "spell the resting point Option<Lvl> with Option.defaultValue"; the bare `[<Default Case>]` form is already unparseable, verified — the attribute grammar takes literals). EMPTY value = a miss with candidates (the int rule's precedent, verified: `PORT=` fails TryParse), never None. The hint compares case-insensitively (the matcher's rule) but names the DECLARED spelling. TEEnvLoad grew a payload (the packed case lists — eval has no env.Types); TRANSCRIPTION addendum. Args.load ZERO-DIFF (battery). | tests: the two [D:env-enums] pins; e2e enum block (casing/candidates/overlay); SKILL + GUIDE the LOG_LEVEL idiom |
| binder-spans | 2026-07-27 | THE PARK OPENED AS WRITTEN: its criterion — a real user demanding definition/rename/references on locals — fired 2026-07-26, the author's first hour of VS Code dogfooding (recorded per the arc's bless note; the criterion mechanism working exactly as designed). THE SPANS: `ELet` gains nameSpan (the let-in ident, parsed `spanned`), `ELambda` gains paramSpan (curryParams hands each param-pattern's PSpan through; the `_` shorthand keeps its atom span); TypedKind mirrors both; PATTERN binders needed nothing — PSpan always carried them. THE RESOLVER: definitionFor's `localDef` walks the typed tree carrying the enclosing binder scope (TELet/TELetPat/TELambda/TELambdaPat/TEMatch arms) — the use resolves to the INNERMOST binding, top-level scan as fallback; cursor ON a binder stays null (definition of a definition). The park's two conservative-null pins FLIPPED to jumps — the named movement that IS the acceptance. Threading was contained: 4 parser constructions, ~10 Check sites, 2 Eval, 1 Lsp, 7 test matches; oracle re-run green (AST shape grew, no semantics moved). RENAME/REFERENCES: now unlocked, NOT built — each is its own bless riding these spans. | PLAN-diagnostics-arc C; unit pins (param/inner/payload/shadowing both directions) + the nvim protocol jumps |
| row-provenance | 2026-07-27 | Cross-statement row-field provenance: `t.BicepPath2` in a function body no longer errors at the CALL 45 lines away — the no-field discharge POSITIONS at the access, with the meet as the message note ("T has no field 'BicepPath2'. Did you mean 'BicepPath'? (the value becomes a T at 5:1)"). DIAGNOSIS (the bless note's question answered): within-statement was already origin-exact (ctx.Rows carries Ty * Span and dischargeRow errs at fspan); provenance died at the generalize→instantiate boundary — schemes carried no spans, and instantiate re-registered fields with the instantiation span. NOT a rider: the fix is representation. Scheme gains RowOrigins (row var -> (field, physLine, physCol, len)) — PHYSICAL positions, because a logical span is only meaningful through its OWN statement's line map; recorded at generalization (both paths: generalizeLet in-checker, and typecheckWith exporting origins WITH the constraint residue for Script's SLet generalizeWithOrigins), rehydrated at instantiate into ctx.RowOrigins, merged in mergeRows (r2 wins per field), chained through re-generalization (a prior origin outranks the local span). The translator is AMBIENT (Check.toPhys, ThreadLocal): Script.checkStatement sets `translate ll` around the check in try/finally; Complete/tests/REPL-direct see None and provenance simply does not record — zero signature churn on the typecheck entries. TypeError gains Origin ((line, col, len) option, physical): the ONE err helper defaults None; Script's typed diag builder honors it, bypassing translate. SCOPE HELD: no-field only — the field-type MISMATCH sibling (right field, wrong type, cross-statement) still reports at the meet; same mechanism applies when blessed. Within-statement inner-let discharge also moved to the access (intended class, pinned direct-access unchanged). | tests: the [PLAN-diagnostics-arc D] pin pair (cross-statement moves, direct access does not); e2e prov.weir block |
| sibling-sentinel | 2026-07-27 | The diagnostics-arc Session E successor (Option B, blessed). DEFECT: inside a multi-line body, command mode over-consumed the assembler's sibling `;` — a command-first body (`cmd` then `let x = … in body`) mis-parsed and failed at the JOINED line's EOF with a raw FParsec expecting-list (a space at 78:8 reported at 86:73). The position was a SYMPTOM; the mis-parse the defect. FIX: the assembler joins STATEMENT-siblings with a MACHINE SENTINEL (U+001F), not `;` — a new `JStmtSibling` join, SAME 3-char width as `" ; "` so every span mapping through the segment table is byte-identical (`translate` unchanged; strict-spans deep run 4000+ cases green as the acceptance). Command mode's `cmdWordChar` STOPS at the sentinel; `seqExpr`'s separator ALSO accepts it (compositional sequencing at every nesting level — lambda/let-in/match bodies); topLet's RHS command branch gains a sentinel-separated tail so `cmd ⟨sib⟩ let…in` folds a real ESeq(ECmd, …) instead of over-running. NOW: the shape parses; the repro reports AT THE COMMAND HEAD (the ordinary seq-unit discard rule — `git status` is a discarded non-unit), no EOF note, no expecting-list. WHY B not A: Option A (command args stop at a literal `;`) is simpler but trades away a working teaching error — `git status ; echo hi` (user ';', one line) must still parse as ONE command with a `;` argv word and warn "';' does not chain" (the bash prior-bleed hint); B keeps user-`;` BYTE-IDENTICAL (pinned both ways) because the machine sentinel is a DISTINCT token from a user `;`. Rejected C (diagnostics-only): would leave `cmd ; let…in` unwritable for no reason. UNPRODUCEABILITY (2 pins, the '|'-key precedent): assemble REJECTS any source line carrying the sentinel ("illegal control character"), so it reaches the grammar only from the assembler; and it NEVER surfaces — cleanParseDump scrubs it to ';', and fmt output is built from the original physical lines (the assembled `ll.Text`-with-sentinel is only an internal structural oracle: the fmt equality check + the respace shape guard, which parses the sentinel as a separator and renders a sentinel-free sexpr). FMT QUESTION ANSWERED: fmt CAN see the sentinel (in ll.Text) but it dies at the assemble→check boundary — never rendered. SCOPE: only the statement-sibling join changed; bracket field/element `;` (JSibling) and district-internal `;` (JDistrictSibling, no inner let possible) stay literal `;`. The consumed-separator law [D:seq-commit][D:arm-commit] is NOT renegotiated — the sentinel rides the same non-attempt `many (sep >>. …)` commit. BARE-PIPE PARK does NOT die (see SEMANTICS): its narrow question is a `|`-fatal POSITION law; this is a command-mode BOUNDARY law — common ancestor (furthest-reached arbitration), disjoint fixes. | PLAN-diagnostics-arc E (diagnosis) + PLAN-sibling-sentinel; Tests "Sibling sentinel" (6 pins) + the ~20 assembler-text pins rewrapped via asmSib; e2e command-first block; GRAMMAR.md invariant-1 sentinel note |
| builtin-fields-lowercase | 2026-07-27 | Builtin record fields obey the casing law — the five defs renamed: FileRow name/bytes/readOnly, Change status/staged/unstaged/path, Completed exitCode/stdout/stderr, Group key/items, EnvVar name/value ("named exactly as a user's record would name them"; type names stay uppercase — they declare). BOTH HALVES, ONE DECISION: field names ARE the wire contract (to json writes them verbatim, from json matches them verbatim), so choosing lowercase fields IS choosing lowercase keys — there was no second decision to bless. The wire argument stands alone too: weir emits into a lowercase/camelCase ecosystem (jq, gh, kubectl, az); PascalCase keys read as .NET escaping into a shell tool; user records already speak lowercase to the wire (you declare kind/metadata/spec to match kubernetes) — builtin rows were the only PascalCase speakers, same inconsistency one layer out, fixed by the same rename. NO convention knob: verbatim-key matching means the declaration is the naming; a casing setting would be a mode. Per-field override PARKED on its trigger — an external key that isn't a legal weir identifier (x-request-id, _id). Phase note: done BEFORE publication deliberately; post-publication this is a breaking wire change with a migration story. Ordinal order preserved per def (lowercasing moved no field past another), so show/to json emission order is byte-stable modulo the names. Zero behavior moved except names: 3 pins moved SEMANTICALLY (both did-you-mean probes respell Bytse→bytse — distance to 'bytes' is 3, the hint threshold is 2; completion prefix f.B→f.b), the rest textually — roundtrip coherence pin moved with both sides. Retirement hints: NONE, phase-scoped per [D:drop-reify-builtins]; the accident pays anyway — 'has no field ExitCode … Did you mean exitCode?' teaches the rename for free (distance 1). | NOTES: "Builtin fields go lowercase"; the gate-stop entry above it; unit+e2e+skill-doc sweeps |
| anchor-before-read | 2026-07-27 | A parser that fails AFTER consuming its trigger attributes the error to wherever the stream DRIFTED (trailing ws crosses physical lines in assembled statements), so the caret lands off the token. FIX SHAPE: capture the trigger's position BEFORE consuming and raise there — `failFatallyAt`/`failFatallyAtCol` (consume to clear the competing "expected" errors at that spot, then Seek back to the anchor; a plain lookAhead-restore keeps the fatal NON-consuming so `<|>` merges the competitors back into a dump). PART 1 — the bare-pipe caret [D:pipe-hint]: the parked "where should a bare-`\|` fatal point when the LHS spans lines?" question DISSOLVED on its receipts — the multi-line record LHS behaves IDENTICALLY to the flat pipeline LHS, so there is no position law, just one off-by-one (caret on the ws after the `\|`, everywhere). Now the caret sits ON the `\|` in all three shapes (single-line, multi-line pipeline, multi-line record); message TEXT unchanged, the three customer sites re-run. PART 2 — the sweep (anchor-before-read's SECOND sighting after caseDecl, the two-instance threshold): every consume-then-fail site tabulated (NOTES). FIXED (caret drift → anchored): bare-pipe, int-out-of-range ×3 (expr/neg-context/pattern), measure ×2 (expr/type), range-step-zero, duplicate-param (the 2nd binder, not the `=`). CLEAN already (anchored on their token): the `;`-does-not-chain + `>`/`>>` redirect warnings, discarded-exitCode, cmd-not-found `^head`, the depth-guard (node span). FINDINGS (reported, not fixed — out of the caret scope): (a) neg-int-out-of-range — anchoring at the `-` drops the fatal into the unary-minus operator's contested spot and MERGES its expecting-list, so the clean message wins and the caret drift stays, coupled to (b); (b) MESSAGE-DOMINATION class — splat head/mid/bare ×3 and the reserved-word errors (function/rec/mutable) have CORRECT carets but their non-consuming `fail`/`failFatally` merges competitors into a buried dump; (c) multi-external/reifier foldChain — caret drifts to the chain end but the anchor is ambiguous (which segment) and foldChain carries no positions. Ground rule held: zero message TEXT moved, only positions; strict-spans deep run green. | PLAN-anchor-before-read; the two caret pins (bare-pipe three shapes + the anchor sweep) [D:anchor-before-read]; NOTES sweep table + park retirement; PROCESS anchor rule |
| message-domination | 2026-07-27 | Finding-class (b) from [D:anchor-before-read] closed: teaching errors whose caret was CORRECT but whose message was BURIED under a raw FParsec expecting-list ("Other error messages: …"). ROOT: a NON-consuming `fail`/`failFatally` (a lookAhead guard, or a `notKeyword` inside an attempt) fails where sibling alternatives also failed, so FParsec merges their expected-set into a dump — the anchor sweep's FIXED sites consume their trigger first, which clears it. FIX: consume the trigger to clear competitors, then `failFatallyAt` back (same helper). SPLAT family (splat head, splice/splat mid-word ×2): a `$@`/`$` in these spots is ALWAYS an error (no fall-through), so consume-then-anchor dominates cleanly. RESERVED-WORD in the BINDER name (`let function/rec/mutable`): the CRUX — the shared `ident`/`notKeyword` MUST stay non-fatal, because a keyword there falls through to its own parser (`many1 postfixAtom` stops at `in` for `1 in y`; `let x = if …` reaches ifExpr). So a dedicated `letKeywordGuard` gates the domination to the `let <name>` slot and fires OUTSIDE topLet's `attempt` (which would otherwise BACKTRACK the fatal — FParsec's attempt swallows fatals); the peek engages only when the name is reserved, so every real binder falls through. Each site pinned with BOTH the exact line:col AND the ABSENCE of "Expecting:" (the burial is the bug), plus the two fall-through parses (`let x = if …`, `let y = 1 in …`). Message TEXT unchanged — the session SURFACES the existing teaching, never reworded. FINDINGS left: (i) neg-int-out-of-range — contested between negIntLit and the unary-minus operand (both int-oor, cols 9 vs 10), neither dominates, so the clean message wins over the caret; (ii) reserved word in PARAM/FIELD position — distinct sites giving a generic "expecting '='", not the keyword teaching, out of the binder-name scope. Strict-spans deep run green. | PLAN-message-domination (diagnosis table); Tests "message domination" pin (6 sites + 2 fall-through) [D:anchor-before-read]; NOTES finding-(b) retirement |
| anchor-residue-ab | 2026-07-27 | The A+B session of PLAN-open-findings (the anchor residue). PROPERTY recorded (LEXICON + PROCESS, owed by the first session to run): a fatal inside an `attempt` is NOT a fatal — FParsec backtracks fatals too, so a teaching `failFatally` in speculative attempt/choice is silently swallowed (the worst failure: looks implemented, passes a not-worse test); a teaching that must survive needs an anchor OUTSIDE the attempt, an exception channel, or a commit point ahead (sightings: arm-commit, depth-guard, the reserved-word gate). A (foldChain reifier drift, finding-(c)): the sweep's "ambiguous anchor" was WRONG on re-read (a finding is a claim, not a fact) — foldChain holds each segment's span; its ONE real error branch (reifier not after a single external command) is owned by the MARKER (mspan). Widened `Result<Expr, string>` → `Result<Expr, string * Span>`; both callers (cmdLineWith, value-headed tail) raise `failFatallyAtCol`; the marker had no surviving competitor so it dominates clean. B (reserved word in name-required slots): PARAM keyword now dominates via `letKeywordGuard` EXTENDED to scan the name + simple-ident params (it already fires outside topLet's attempt — the load-bearing boundary); record-DECL field via a dominating `fieldNameDecl` (typeDecl commits, so the fatal propagates). Pinned with the caret + teaching + NEITHER burial marker (Expecting: AND Other error messages:), plus the keyword×position fall-through matrix (if/then/else/elif/match/when/fun/let/in/type all still head their constructs) and a deep fuzz run. FINDINGS left: record-LITERAL field (`{ let = 1 }`) and keywords inside a parenthesised param pattern — both sit inside recordLit's field-vs-update commit-check [D:arm-commit], out of remit; neg-int (finding i) and the field-type mismatch (D) untouched, their own sessions. Zero message text moved; strict-spans deep run green. | PLAN-open-findings A+B; Tests "message domination" (A+B cases + matrix); e2e foldChain+param pins; LEXICON anchor-before-read + fatal-in-attempt; PROCESS corollary |
| anchor-residue-c | 2026-07-27 | C of PLAN-open-findings (neg-int overflow, run ALONE). The predicted-hard case was CLEANER than predicted and its premise was a CLAIM, not a fact (third such correction after A and the sweep): "parsed TWICE by negIntLit and prefix-minus" was WRONG — `negIntLit` is RANGE-ONLY (spaced steps `[10.. -1 ..1]`); general expressions use `negAtom` (prefix minus, `-e` → `0 - e`). REAL ROOT: for `let x = -N`, negAtom's operand `intLit` hits int-out-of-range, and negAtom's OWN `attempt` SWALLOWS the fatal (the residue's documented property — a fatal inside an attempt is not a fatal), then the furthest-error merge buries it at the `-`. FIX = the property applied, not the plan's three options (option 1 breaks `-5`/`f -1` since negIntLit is range-only; option 2 breaks range steps): narrow negAtom's `attempt` to the prefix DETECTION only (guards + `-` + notFollowedBy space), so the operand parses OUTSIDE it and its fatal propagates. This narrows an EXISTING attempt (not a new consumed-separator commit), and the STOP-condition did not trigger because the risk surface is BYTE-IDENTICAL: `a - 1`=4, `a-1`=4, `-5`, `[10.. -1 ..8]`=3, `f -1`=99 (application, spacing-sensitive) all unchanged before/after, pinned. Overflow now reports "int literal out of range" at 1:10 (the digits — the operand intLit's own anchor), clean (neither burial marker). Deep fuzz + oracle green. | PLAN-open-findings C; Tests "neg-int overflow dominates" (caret + risk-surface behavioral pins) [D:anchor-before-read] |
| row-provenance-mismatch | 2026-07-27 | D of PLAN-open-findings, the Session D residue closed: the cross-statement field-TYPE-mismatch sibling now anchors at the ACCESS like the no-field case. Session D positioned the NO-FIELD error at the access with the meet as a note; the sibling (right field, WRONG type, cross-statement — `t.count` forced to string while T.count is int) still reported at the MEET, an asymmetry worse than either alone (the user learns the good behavior from the name case, then gets the old one for the type case). DIAGNOSIS (the load-bearing question): the mismatch DOES flow through dischargeRow's Some-arm (`bind ctx env fspan (declTy) ft` → "expected int, got string" at fspan, translated to the meet), and the origin IS populated for the mismatch path (same field-access → RowOrigins mechanism as no-field). FIX: factored the origin-positioning (RowOrigins lookup + TypeError.Origin + the meet note, all from [D:row-provenance]) into a shared `atAccess` helper, applied to BOTH arms — the mismatch keeps its own "expected X, got Y" message re-anchored at the access. Cross-stmt mismatch moved 5:1 → 3:25 (the field access); WITHIN-statement mismatch byte-identical (no cross-stmt origin → no note); no-field unchanged; oracle re-run green (origins ride schemes, Session D's precedent). | PLAN-open-findings D; Tests "the TYPE-mismatch sibling also anchors at the access" (cross moves, within unchanged) [D:row-provenance] |
| keyword-slots-residue | 2026-07-27 | The last two keyword-domination slots (PLAN-keyword-slots-residue), both surfacing a keyword's teaching cleanly instead of buried. ITEM 2 (pattern binders): `patWord`'s keyword check now DOMINATES outside its own attempt (consume the word, failFatallyAtCol back) — a keyword is never a valid pattern, so this is safe everywhere and the fatal SURFACES where the context is committed: MATCH-ARM (past its arm-commit `|`), LAMBDA (past `fun`), curried PARAM — all clean; swallowed only for let-destructure (`let (rec) =`), whose SLetPat attempt (the plain-vs-destructure commit) encloses it → stated FINDING (wants a pattern-peeking guard or narrowing that commit, out of remit). Fall-through matrix (constructors, tuples, lists, wildcards, `when` guards, true/false) unaffected; deep fuzz green. ITEM 1 (record-literal field name): FIXED with NO stop — the domination lives OUTSIDE the [D:arm-commit] literal-vs-update commit-check: a `keywordFieldGuard` runs right after `{ ws`, peeking `<word> =` and fataling only on a keyword (decision inside the attempt so a real field backtracks unconsumed; `{` already committed the atom so the fatal escapes). `{ let = 1 }`/`{ in = 1 }` → the teaching at 1:11; valid records and `{ r with x = 2 }` updates run byte-identical. The plan PREDICTED item 1 might need the commit boundary; it did not — a guard-before-the-check sufficed. Message text unchanged; every fix pinned caret + teaching + neither burial marker; strict-spans green. FINDINGS still open: let-destructure keyword (this session) — the only keyword-domination slot left. | PLAN-keyword-slots-residue items 1+2; Tests "keyword in a pattern binder dominates" + the message-domination record-literal pins [D:anchor-before-read] |
| let-destructure-keyword | 2026-07-27 | The LAST keyword-domination slot closed (PLAN-let-destructure-keyword): `let (rec) = 1` (and `let (a, rec) =`) now surfaces the keyword teaching instead of burying at 1:5. FIX = option 1b (the user's addition, cheapest that works): finding a reserved word in an identifier slot between `let` and the top-level `=` is a LEXICAL question, not a structural one — extend `letKeywordGuard`'s bareword scan to collect words while SKIPPING pattern delimiters `( ) [ ] { } , ; _` (no pattern parse, no nesting logic), fatal at the first keyword. It stays OUTSIDE SLetPat's attempt (a stmtWith alternative), so the fatal escapes; the scan STOPS at `=`, so an RHS keyword is never a binder keyword. Option 2 (narrow SLetPat's attempt) was PREDICTED to stop and NOT attempted: the attempt IS the plain-vs-destructure-vs-letin discrimination (it reads the pattern's shape), so binderPat can't move outside it without the discrimination following — the boundary itself, not a nudgeable one. ONE in-session fix: the scan flagged `true`/`false` (both in the keyword set) — excluded them per patWord's rule (they are LITERAL patterns, a refutable-binder CHECK error, not a parse keyword error), caught by the refutable-binder pin. `let (rec)`→1:6, `let (a, rec)`→1:9, `let (x, in)`→1:9; the three let-shapes (plain/destructure/expr-letin), true/false patterns, and RHS keywords all unchanged; deep fuzz green. This closes message-domination to ZERO open keyword slots. | PLAN-let-destructure-keyword (1b); Tests "keyword in a pattern binder" (destructure cases + true/false + RHS-stop guards) [D:anchor-before-read] |
| lsp-root-resolution | 2026-07-27 | Two editor-parity fixes for a /btw report (tools/fuzz.weir: clean in VS Code, errors in Zed). ROOT: `ci/deep-lock.sh` is a relative-path command head; `Extern.exists` resolves `/`-containing heads via `File.Exists(Session.resolve prog)` = `Path.Combine(Cwd(), prog)` -- the SERVER's launch cwd, which the editor chooses. VS Code launched `weir lsp` with cwd = workspace root (path resolves -> command mode -> clean); Zed launched it elsewhere (path missing -> not external -> the line parses as an EXPRESSION -> `ci` unbound + a mis-parse). Reproduced by running `weir check` from /tmp. FIX 1: the LSP `initialize` handler now captures `rootUri` (or the first workspaceFolder) and `Session.setCwd`s the workspace root, so command resolution is DETERMINISTIC across editors -- matching VS Code and the CLI-from-repo-root. FIX 2 (the secondary leak the report exposed): the sibling sentinel [D:sibling-sentinel] appeared as the U+001F escape in FParsec's expecting-list because it is a valid seq separator; cleanParseDump scrubbed the raw char but FParsec renders the ESCAPE string. Two guards now: `seqSep` relabels the separator alternative as ';' (<?>) so the sentinel never enters the expected-set, and cleanParseDump also scrubs the U+001F escape as a catch-all (the topLet command-tail path). PINS: lsp-e2e drives a wrong-cwd server with a correct rootUri and expects 0 diagnostics on fuzz.weir; the no-leak unit pin now asserts NEITHER the raw char NOR the escape on a separator-listing dump. | lsp-e2e rootUri probe; Tests no-leak (both forms) [D:sibling-sentinel] |
| self-module | 2026-07-27 | Script/process introspection grouped under a `Self` module (uppercase, the module convention), and `pid` added — freeing the bare names `args`/`stdin`/`scriptPath` for users. Origin: dogfooding — a script needs its own PID and wrote `$(sh -c 'echo $PPID') |> Seq.head`, a shell-out for a value the runtime knows. NAME `Self` (D1): the deciding argument is what the module ACCUMULATES — every plausible member (pid/args/stdin/scriptPath, later startTime/tty/cwd) is a fact about the running thing; `Script` is slightly wrong (pid is a process fact), `Proc` collides with the internal spawn vocab and Proc.fs, `Runtime` is accurate but lifeless. MEMBER `scriptPath` not `path` (D1b): the qualifier DISAMBIGUATES (script's path vs weir's exe vs cwd — two are things a script wants), it is not stutter (`Self.selfPid` would stutter; `scriptPath` says WHICH path, which the module name does not); `sourcePath` rejected because `source` already means source-code in the docs, and the clincher — the name matches the BOUNDARY that governs it: these members are SCRIPT-only, so concept, restriction, and error all say 'script'. CLEAN BREAK (D2): bare names removed, no aliases — a stale bare `args` is an unbound-var error whose did-you-mean (the existing bare-name->module-member teacher, Check.fs) points at `Self.args`; the migration teaches itself, as the casing rename did. Caveat: `args` has real muscle memory (every example) — expect a few days of the teaching error; fine, phase-scoped [D:drop-reify-builtins], no retirement machinery. `pid` (int) = Environment.ProcessId, stable across reads (the property fuzz.weir's lock liveness depends on; a per-read query would be a subtle bug); command use is `$"{Self.pid}"` (argv is strings; the interpolation hole converts inline — the string-vs-int question was weighed, string's payoff was one `show` and interpolation erases even that, so int stayed). FEASIBILITY: module members resolve to values via mangled `Module.member` keys, so a value member is no different from a function member; injected per-run in baseEnvs (both envs + strict copy) as a Modules entry + mangled Values. THE RE-VERIFIED FINDING (proven with a pin, not a grep, per the plan): `Args.load` gates script-mode on the introspection presence — it was `Map.containsKey 'args' env.Values`, now `Map.containsKey 'Self' env.Modules` (it reads Session.ScriptArgs at eval, so this is purely the availability gate). Env.load unaffected. Corpus migrated: repo-report `match Self.args`, fuzz.weir deletes its `$PPID` workaround (`acquire $"{Self.pid}"`), SKILL/GUIDE/SEMANTICS. | Tests 'the Self module' (types + teaching) + argvEnv on the Self module; e2e Self.pid stable-positive-int + scriptPath migrated; SEMANTICS script-inputs entry |
| doc-comments | 2026-07-28 | `///` doc comments (PLAN-doc-comments half 1). Attach to the declaration IMMEDIATELY below (a contiguous run accumulates in order; a blank OR a plain `//` breaks it). STORED OUT-OF-BAND, keyed by the physical (line,col,len) of the documented NAME — never by name (shadowing / inner lets / duplicate field names). The deciding argument (user, at review): this is NOT a divergence from the plan's "AST carries them / typed tree mirrors" — it is BETTER on the project's invariant-by-architecture-over-pin criterion (5th instance: one-pipeline, emergent-bound, one-spawn, capture-views). Erasure becomes a property of the REPRESENTATION: Value/Eval/Check never see a doc, so byte-identical runtime is trivial and there is NOTHING TO PIN — a doc is metadata about a source LOCATION, not part of the program's meaning; threading it through every AST/typed-tree constructor would add a field no arm reads (the binder-spans churn was paid for a SEMANTIC reason — spans feed the resolver; docs have none). Hover: TYPE FIRST, then the doc, at all five positions (top-level let, `type` decl, record field, union case, inner let) — `declHover` renders the type-decl structure / a field's type / a union case's signature for the KType position; let + inner-let already carried types. Completion items carry the doc (name-keyed index — a completion item IS a name, last-wins on a shared name). Alignment is a LINT, not the attribute machinery: docs are INERT (dropped before assembly) so a misaligned one cannot mis-parse — the `>]`-dangle precedent (a misaligned entry surfaced a misleading RUNTIME error) is about PARSE-affecting lines and does not apply; `Script.docMisalignments` errors both ways (doc above a field, doc above a union case) from analyzeLines, so it surfaces in `weir check` AND the LSP. fmt canonicalizes each `///` to its following declaration's formatted anchor (transparent to assembly, so the safety re-check holds; idempotent). Coloring: a distinct `comment.line.documentation` scope in all three grammars (TextMate name, tree-sitter `#match ^///` last-wins, micro `comment.doc` region before `//` — RE2 needs no lookahead, so not exempt); inventory 24->25. PARKED: param docs (a `/// - param x:` convention is structured-doc design — a bigger question; params already show their types on hover). RESIDUE (stated, chosen not defaulted): if docs ever leave the LSP (a `weir doc` command), the same pass serves, but a typed consumer wanting "the doc for this resolved symbol" goes through the position key, not the node. Half-2 doc-test answer: keep the Doc opaque markdown; half 2 embeds ```weir fences inside `///` and extends `ci/skill-doc.sh` with a `///`-aware pass (reuses the fence + runner), making builtin examples the project's only rot-proof docs. | NOTES: "/// doc comments — half 1" |
| builtin-docs | 2026-07-28 | Builtin docs (PLAN-doc-comments half 2) — every builtin member gets a doc rendering on hover and in completion, reusing half 1's out-of-band representation: `Builtins.builtinDocs: Map<string, BuiltinDoc={Summary; Example option; Pointer option}>`, keyed as the name appears at a use site (`Seq.map`, bare `print`, `Env.load`, `from json`); `renderBuiltinDoc` joins summary/example/pointer, the LSP prepends the type (half 1's type-first layout), so the Summary never restates the signature. Value/Eval/Check never see it (same erasure-by-architecture). D1 DECIDED (a) EXECUTABLE — but by the mechanism the plan did not consider, which dissolves its (a)-vs-(b) tradeoff: the Example is registry DATA run through the SAME check+eval path by an Expecto test (`builtin docs: every doc example runs clean`), NOT bash parsing example lines out of F# string literals. That sidesteps the F#-literal escaping/multi-line cost that made the plan lean toward the markdown-mirror (b), keeps the example IN the hover (b showed only a pointer), and puts a rotted example on the build's critical path. Hover resolves the name via the typed node: TEVar for members/bare, and the boundary forms explicitly (TEEnvLoad->`Env.load`, TEArgsLoad->`Args.load`, TEFrom->`from json`/`from porcelain`, TETo->`to json`). Completion carries the doc off the same registry (name-keyed, in the handler; Complete.fs is compiled before Builtins). Both surfaces PROTOCOL-verified over the wire (initialize+didOpen+hover / +completion against `weir lsp`), not just the pure function. Filesystem/stdin/raising members (File.read/write/append, Env.fromFile/load, Args.load, Self.*, fail, exit) are summary-only — no self-contained runnable one-liner — which the template allows. COVERAGE (reported as a fraction): Seq 30/30, Str 19/19, Option 3/3, Env 6/6, Args 3/3, File 4/4, Path 6/6, Self 4/4, bare 7/7, boundary 5/5, reifiers 4/4, types 5/5 = FULL (~96/96; Result removed after this pass, [D:no-result]). The two follow-ups closed the same way the boundary forms were keyed: the reifiers desugar to `|completed`/`|completedEnv`/`|completedIn` (and the succeeded/orFailed/exitCoded families) which the typed tree carries as a TEVar at the surface span — `Builtins.reifierSurface` maps that internal key back to the surface name (`complete` etc.). Builtin TYPES are no expression node, so hover adds a word-at-cursor fallback: the bare name written at the cursor is looked up in builtinDocs (uppercase/distinctive, so no false hits). Documenting the types surfaced a finding — Result had a doc that had to apologize ("no builtin returns one") — which led to [D:no-result]. Laws in docs are quoted from the ledgers (force = memoize/reuse+timing; pmap = ordered/eager/first-error; Env.load = field law; Args.load = three shapes), never restated deeply (the LEXICON lesson). | Tests "builtin docs" (example-runner + hover type-first + boundary via TEEnvLoad); NOTES: "builtin docs — half 2" |
| no-result | 2026-07-28 | Removed the prelude `type Result<'a,'e> = Ok of 'a \| Error of 'e`. Surfaced by half 2: documenting the builtin types, Result's doc had to apologize ("you construct it; no builtin returns one") — the tell that a type ships without earning its place. Investigation: ZERO uses in weir scripts / examples / corpus / docs; NO builtin produces or consumes one; weir's error model needs it NOWHERE — expected absence is `Option` (the `try*` family: tryFind/tryHead/tryItem/tryToInt/Env.get/Args.value), command outcomes are `Completed`/exit-codes/`succeeds`/`orFail`, and programmer/unrecoverable errors RAISE (`fail`, `Seq.head []`, overflow) and propagate to exit. Result would be a recoverable-error-WITH-payload channel, and weir routes "recoverable"->Option, "has structure"->Completed/exceptions — no gap for it (contrast Option, which earns its place). Its ONLY real role was a TEST FIXTURE: one test ("Result infers across arms") exercised a 2-param generic union + cross-arm inference, and it was the only 2-param generic union in the tree. That coverage RELOCATED to a locally-declared `type Either<'a,'e> = Left of 'a \| Right of 'e` in the test env + ctorValues (the pattern `Proc` already uses), so a type does not ship solely to be a fixture. Fits the subtractive line (drop-command-builtins, drop-reify-builtins, sh removed, measures removed): a type nothing produces/consumes is dead weight, and the migration teaches itself (a stale `Ok`/`Error` is an unbound-constructor error). SEMANTICS prelude line updated (Option only). Reversible: reintroduce if a Result-returning family is ever designed (a `tryParse : string -> Result<int,string>` was the hypothetical, not built). | Prelude.fs (Option only); Tests "a 2-param generic union infers across arms" (Either) + the option-sweep pattern uses Some; SEMANTICS prelude entry |
| hover-silence | 2026-07-28 | A cursor on a keyword / operator / punctuation / whitespace / the wildcard `_` hovers as NOTHING (was: the enclosing node's type — `if`/`then`/`let` returned the if-expr's `int`). A wrong type on the most-hovered tokens teaches the user that hover LIES, and fires constantly as a reader sweeps a line; null is the honest answer (the helix false-alarm precedent: a null at a space is correct). `onSilentToken` runs BEFORE the enclosing-node fallback, scoped by what the cursor is ON, not what encloses it — so a nested construct can't defeat it. `true`/`false` are in Parser.keywords but are LITERALS, so they answer; identifiers and numbers answer. Group 3 of hover-completeness, shipped first (a guard, not a feature). | Tests "keywords / operators / wildcard are silent" (the null matrix + negatives); protocol pin (hover 'if' -> null) |
| hover-completeness | 2026-07-28 | Hover answers at REFERENCE, FIELD, and PATTERN sites, not just declarations (five dogfooding reports; half 1 wired declaration sites only). 1a: a usage / field / case reference resolves to its declaration via `definitionFor` and reads the `///` doc there — so `hoverType` moved BELOW `definitionFor`; shadowing free (innermost-wins). 1b (module-member hover) was ALREADY shipped in half 2 (`TEVar -> builtinDocs`), despite the plan naming it the half-2 prerequisite — half 2 did not wait on it. 1c: a field in a LITERAL `{ F = … }` is only a `TERecord` node, so it showed the record type — now resolves the FIELD's own type off the record def (field ACCESS `t.f` already worked); the field doc rides 1a. 2: a union case in a pattern is no expression node, so the constructor signature `C : Payload -> Union` is keyed off the WORD (skipped at the type decl, where declHover renders it); the payload BINDER is no node either, but its USES carry the type (`varUseType`) — ordered exact-TEVar-use, then use-of-the-word, then the enclosing node, and the KLet scheme fires ONLY on the binder name (so an unresolved position no longer inherits the statement's type). Nulls pinned as legitimate answers; >=1 case per group over the PROTOCOL. Docs stay out-of-band (half 1). | Tests "a usage shows its declaration's doc…" + "a pattern constructor shows its signature…"; protocol pins (usage doc / field-in-literal / constructor over the wire); NOTES "Hover completeness" (the matrix table) |
| over-apply-continuation | 2026-07-28 | The over-application error (`'f' takes at most N argument(s), but got M`) pointed at the HEAD and said nothing about the usual CAUSE — a next line indented DEEPER than the call is slurped as a continuation (the offside rule), so `makeRef … / <deeper> deleteBranch branch` reads as one 4-arg call. Dogfooding report: the squiggle under `makeRef` + "got 7" reads as makeRef's fault, not the indentation's. FIX: point the error AT the first extra argument (its span), and — when that argument sits on a different PHYSICAL line than the head (via `toPhys`, the logical->physical translator; absent in Complete/tests) — append "these are an indented continuation of the line above — a separate statement must start at that line's indent, not deeper". Single-line over-application (`double 1 2`) is unchanged in message (no translator disagreement) and now squiggles the extra arg. The two existing message pins assert TEXT only, so they hold. | Tests "over-application on an indented continuation…" (via analyzeLines, so the translator is live) + the two single-line message pins |
| annotated-signature | 2026-07-28 | Hover renders a member as the ANNOTATED DECLARATION FORM — `Str.isMatch (pattern: string) (subject: string) : bool` — instead of the bare arrow `string -> string -> bool`, so which argument is which is visible. FORM chosen for two reasons: it is VALID F# declaration syntax (claims nothing false) and it is the shape a user would WRITE when wrapping a member (weir being an F# sibling, showing members the way one DECLARES a member beats a bespoke display convention). REJECTED: (a) a `pattern: string -> …` pseudo-type — syntactically a LIE about weir's type language; someone would type it into source and get a parse error; (b) a man-page usage line — weaker, and not copy-pasteable. DEGRADE RULE (one rule): render the annotated form when parameter names are KNOWN, else the arrow. Known = builtins (names from a new `BuiltinDoc.Params` — a SEPARATE field, never parsed from prose, avoiding D1's F#-literal trap; a sample of 5 named, the full pass is half 2) and user declarations (names from the binder-span lambda chain — top-level + inner lets, via `formatSignature`/`lambdaParamNames`/`innerLetSig`). Not known = lambdas, expressions, partial applications, unnamed builtins -> arrow, EVERY fallback pinned. Zero-param value -> `Self.pid : int` (no empty parens). PRESENTATION vs COMPUTATION: the arrow `formatTy` is UNTOUCHED, so type ERROR text is byte-identical — the two renderings are one type system shown two ways, pinned (formatTy=arrow, formatSignature=annotated, + a real error's text unchanged). Completion carries the same rendering as the item `detail`. Half-2 amendment: the template gains the annotated signature and its writing pass must NAME EVERY PARAMETER (`subject` vs `stringToMatch` forces clarity); the signature stays declaration-shaped even for data-last members (`Seq.choose (f: 'a -> Option<'b>) (xs: seq<'a>)`), while the EXAMPLE shows the piped idiom (`xs |> Seq.choose f`) — signature says what it is, example says how it is written. | Types.formatSignature; Tests "annotated signature for named builtins…" + "presentation-only: formatTy stays the arrow" + updated snapshot/inc pins; 4 protocol pins over the wire |
| json-option | 2026-07-28 | `from json`/`to json` accept `Option<scalar>` fields (int/string/bool inner only). READ: a missing key AND an explicit `null` both read as `None` — the JSON format is missing-OR-null (unlike env's absence-only or argv's flag-presence), so both absences collapse to `None`; a present scalar is `Some`. A `null` in a REQUIRED field stays a parse error, and its message is the NAMED exception to no-message-changes — it teaches the fix (`field 'x' is null; declare it Option<int> to allow it`). WRITE (the one real fork, decided in-session): `to json` on `None` OMITS the key — a weir payload looks like the ecosystem's (gh/kubectl/docker inspect omit rather than emit null); since missing and null both read back as `None`, the omit roundtrips. REJECTED at check, naming the supported set: `Option<Option<_>>`, `Option` of a record or seq (the boundary needs a FLAT row). The two field-law lists (`jsonableRecord` for from json, `jsonableElem` for to json) UNIFIED into one `jsonFieldOk`/`jsonScalar` predicate — the duplication the maintenance sweep would have flagged, closed here. No third list: `from porcelain` is a fixed-shape `Change` adapter, not a scalar-list validator. Zero movement on non-Option rows (existing FileRow pins byte-identical). | Tests "from json: an Option field reads present as Some, missing AND null as None" + "to json omits a None field; the Option roundtrip holds" + "json boundary: null-in-required teaches Option; nested Option and Option-of-record reject"; SEMANTICS sortBy/from-json field-law note; SKILL from json block; Builtins from/to json docs |
| modules-v1 | 2026-07-29 | USER modules & imports, arc SESSION 1 (design: plans/DESIGN-modules-and-imports.md). Landed: the `module [Name]` marker (top-level, no `=`/body; reserved word) and `import "path" [as Name]` (literal-string path only; uppercase alias) as new `Stmt` cases; single-hop, CLI only. RESOLUTION is one function (`resolveImportPath` importing-file + path -> absolute normalized, symlinks UNRESOLVED per the Path.glob precedent); a missing import names the RESOLVED ABSOLUTE path. A module is checked in its OWN base env (`moduleBaseEnvs`: builtins-strict + prelude + Self, Self.scriptPath = its own path — PURE, so loading never disturbs the entry's process facts); its contributions merge as `Modules[alias]` (values + union ctors, so `alias.member` resolves the existing qualified way), `Types` (flat, PLAIN NAME so signatures/field-access/bare-literals all resolve with ZERO scheme-rewriting), and a new `TypeEnv.ModuleTypes: alias -> type-name set` (provenance for the qualified literal). TYPE REPRESENTATION — the load-bearing choice: plain-name keying (not qualified `Git.Ctx` keys) makes the common case (a shared record used in the module's own helper signatures) work with no rewriting; the cost is a SESSION-1 collision guard — an imported type name clashing with an existing type is a check error (cross-module same-name types are not yet distinguishable; qualified type names are a later session). NAMED / QUALIFIED RECORD LITERAL (`Ctx { .. }` / `Git.Ctx { .. }`, decision 7): NO new AST node and NO grammar change — the checker INTERCEPTS `EApp(typeName, ERecord)` (and `EApp(EField(module, type), ERecord)`), which is safe because a record type name is never a VALUE, so interception fires only where a bare application would already ERROR — `Some { .. }` (ctor applied to a record) is byte-identical (zero movement, pinned). Bare `{ .. }` field-set inference is unchanged; the named form disambiguates a two-record field-set collision. DECL-ONLY + WEAK PURITY [decision 10]: a module holds only `type`/`let`; a `let` whose typed RHS RUNS a command is rejected — detection walks eager positions and STOPS at lambdas (`runsCommandT`), so a paramless `let x = git …` (an ECmd) is rejected but a param-ful `let f r = git …` is a deferred function, allowed (bare builtin value refs like `ls` are lazy seqs, also allowed). Enforced BOTH on import (`loadModule`, first error) and on direct `weir check <module>` (decision 10: checkable in isolation). ERRORS, each named + own message: self-import ("a file cannot import itself"), not-a-module (names the file + fix), running-a-module ("a module declares; it does not run…", detected by the marker up front in `run`), nested-import (single-hop block), literal-only path, script-only (`-e`/REPL — no file to resolve against). Eval: `CImport` replays the module body in a fresh module venv (Self.scriptPath = module's; process facts ride from the entry), exposing bindings as `alias.member`; no caching (session 3). checkStatement gained an `ImportLoader` param (5 call sites; the real loader in run/check, `scriptOnlyImport` for -e/REPL, a blocking loader inside `loadModule`). SESSION 2 (multi-file diagnostics, decision 13 completed): a module-CONTENT error (a type error, decl-only or weak-purity violation inside the module) now reports at the module's OWN file:line:col, PLUS an "imported here" NOTE (a new `note` severity) at the import line in the importer; an import-STATEMENT error (self-import, missing file, not-a-module) still reports at the import line with no note. `loadModule` returns a structured `ImportError` (`File` option — Some=content error at the module site, None=statement error at the import line — + Line/Col/Message) instead of a baked string; `StmtDiag` gained `File`/`Note` fields; `analyzeLines` emits the module diagnostic under its own File and a second `note` diagnostic at the import line; `run` renders both; `check --json` already carried a `file` field per diagnostic (`writeDiag`), now populated per-file. Per-file translate holds by construction — a module's positions were already computed in the module's own `checkStatement` context (session 1). SESSION 3 (the graph): TRANSITIVE imports — a reached module's own imports resolve too (session-1's single-hop nested-import BLOCK is removed); `loadModuleCached` threads a `cache` (normalized abs path -> LoadedModule) so a DIAMOND's shared module is checked ONCE and re-used (re-aliased per import site via a new `LoadedModule.NaturalName`), and a `chain` (the DFS path of importing files) so a repeat is a CYCLE — a check error naming the loop by file name (`a.weir → b.weir → a.weir`), same detector as self-import, different rendering (decision 9), reported at the closing edge with the note at the entry's import. Path identity is the normalized absolute path (symlinks unresolved, session 1). NO RE-EXPORT (decision 3): a module exports only its OWN types (computed from its Body's `type` decls, not the env diff — which would leak transitively-imported types) and its own values (imported members live under `Modules[·]`, never `Values`, so they were already excluded); a module USES an imported module's types internally freely, but an importer sees a member mentioning a third module's type as opaque unless it also imports that module (decision 8's discouraged-but-legal case). Eval: `replayModule` evaluates a module's Body — including NESTED `CImport`s (exposing the nested module's members as `alias.member` for this module's own lets) — recursively; no eval-time caching (a diamond's shared module re-evaluates per importer, safe because modules are pure decl-only). SESSION 4 (the LSP, decision 14): PER-URI publishing — the server groups diagnostics by their `File` and publishes each under its own `file://` URI (a module's error lands on the module's file even if unopened; the "imported here" note on the importer), deduped by position+message across the direct and dependency analyses (richer span wins), and clears a file that went clean. BUFFER-OVER-DISK — a new `Script.importSourceOverride` (a `(path -> string list option) option ref`, the `Check.toPhys` pattern) makes `loadModuleCached` read an imported file through it; the server sets it to open-buffer-then-disk (an unsaved dependency checks against what the user sees). DEPENDENT INVALIDATION (option b) — implemented as `refreshAll`: on any didOpen/didChange/didClose it re-checks EVERY open document (so an importer re-checks when a dependency it reads changes) and republishes per URI; the reverse-EDGE selective form is the efficiency variant, deferred (re-check-all is correct and robust — no stale-graph bug). `note` severity maps to LSP 3 (Information); URIs convert via `System.Uri` (`uriToPath`/`pathToUri`), the entry now analyzed against its real path so imports resolve. SECURITY.md revised: the server reads client text PLUS import-reachable files (the old "never touches the filesystem" claim is now false). SESSION 5 (`Self.entryPath`, decision 12): `Self` splits — `scriptPath` is the FILE'S OWN path (a module sees its own; already true since session 1's `moduleBaseEnvs`), `entryPath` is the INVOKED script's, a PROCESS FACT like `args`/`stdin` (the same in every file), added to `selfMembers`, set by `baseEnvs` for the entry (where the two coincide), and carried into a module's replay via the `procFacts` list (not `moduleBaseEnvs`, which keeps the module's own `scriptPath`). No migration: every existing `Self.scriptPath` site means the same. Builtin docs state the asymmetry. DEFERRED still, STATED: the reverse-edge optimization, a failed-import "unbound alias" cascade suppression (pre-existing, appears in CLI too), multi-ERROR-per-module recovery, the decl-above-import ordering error (decision 5); `pub`/privacy (parked, trigger: distribution). ARC IMPLEMENTATION COMPLETE (sessions 1-5); the vocabulary grep (one-word-one-meaning "module") is the arc's closing check. Zero movement on single-file behavior (927 tests, only the keyword-completion inventory pin moved — module/import offered like let/type). | Tests "module marker parses…", "import parses…", "module and import are reserved words", "the named record literal constructs a specific type…", "a constructor applied to a bare record still applies (zero movement)"; ci/e2e.sh modules block (happy path, alias, qualified-literal disambiguation, and every named error); SEMANTICS "modules and imports"; SKILL modules block; plans/DESIGN-modules-and-imports.md; NOTES: "User modules and imports — the five-session arc" |
| repl-quality | 2026-07-29 | THE REPL, made good enough to EVALUATE weir in — a BOUNDED interactive scope (not "a login shell"). IN SCOPE (serve adoption, build on existing machinery, touch NO semantics): (1) COMPLETION — the REPL's Tab reuses `Complete.suggest` (one brain; Complete.fs MOVED after Extern+Builtins in the fsproj so it can reach the PATH cache + command-callable set — the "a shape Complete.fs cannot serve is a finding, extend it" rule). Four contexts: command HEADS (PATH executables + command-callable, offered at a statement head), filesystem PATHS (an explicit `/`-or-`~` word lists the dir, `~` expanded, trailing `/` on dirs; cwd files in argv position), module members/fields + keywords/bare-names (already served). Single-Tab completes the unique match else lists (readline convention, pre-existing). Completion NEVER executes — a directory read / cached PATH lookup only (pinned: no marker file). (2) Ctrl+R HISTORY SEARCH — fzf when on PATH (spawn with `FinderFlags`, feed history most-recent-first to its stdin, the selection REPLACES the whole line; exit 130 = cancel, line unchanged), a minimal built-in reverse substring search otherwise (Esc cancels) — behavior defined either way, never an "install fzf" message. DOGFOODING FIX (2026-07-30): weir glyphs ARE fzf extended-search operators (`^` prefix-anchor vs the force-PATH sigil — searching `^ls` EXCLUDED every `^ls …` entry; also `\|` OR, `$` suffix, `!` negation), so `fzfSearch` hard-wires `--no-extended` BEFORE the config flags — literal matching is CORRECTNESS for code lines, not style; fzf is last-flag-wins, so `finderFlags: ["--extended"]` restores the operators. Verified against real fzf 0.55.0 (repro + fix + override + live pty run); the committed pin is the stub's argv contract. (3) PERSISTENT HISTORY at `$XDG_STATE_HOME/weir/history` (STATE, not config), per-line append (no rewrite during a session — durable), consecutive-dup dedup (readline ignoredups), a 5000-line cap front-truncated ONCE at load, file `0600` (a REPL line can carry a secret). (4) an inert JSON CONFIG at `$XDG_CONFIG_HOME/weir/config.json` — keys `historySize`/`historyDedup`/`historyPath`/`finderFlags`, all INERT values; unknown keys REJECTED with did-you-mean (a typo silently doing nothing is the config's vacuous pin); FORBIDS code/aliases/prompt-commands/imports/env. FORMAT MEASURED, question CLOSED with numbers (2026-07-30 throwaway spike — branch deleted, nothing merged): baseline AOT binary 7,696,168 B / 5.4 ms startup median / 3 pre-existing trim warnings (FSharp.Core, FParsecCS). TOML via Tomlyn 2.10.1: VIABLE — 0 new AOT warnings on the `TomlReader` pull API (the readme's `Deserialize<TomlTable>` path IS reflection — IL3050/IL2026, and the package's buildTransitive flips reflection OFF under PublishAot so it would fail at runtime; the source-gen context is a C# generator, unusable from F#), +329.5 KB, startup unchanged (5.0 ms), best-in-class errors (`(1,16) : error : Unexpected token…` — line/col, no trace), keys enumerable with positions so did-you-mean plugs in. strict-YAML via YamlDotNet 18.1.0 node API: VIABLE WITH CAVEATS — 0 new warnings, +677.3 KB (2× TOML), startup unchanged, the error MESSAGE lacks position (hand-format from `YamlException.Start`), the subset IS enforceable (aliases resolve silently but detectably via non-empty `Anchor`; Norway-problem `no` arrives as a raw string until YOU convert). Measured subset, stated: scalars, block maps, block sequences, `#` comments — NOT anchors/aliases/multi-doc/flow-edges/coercion. VERDICT: JSON STANDS (zero bytes, already rejects unknowns); if a friendlier format is ever wanted, TOML via `TomlReader` is the one worth a user decision (half YAML's size, better errors, no subset to police) — the spike informs, never auto-adopts. Side-caution recorded: F# `printfn "%d"` CRASHES under AOT (MakeGenericMethod) — weir's Console.WriteLine+interpolation convention is load-bearing, not style. USER RULED (2026-07-30): TOML ADOPTED — the config is `config.toml` (Tomlyn 2.10.1, `TomlReader` pull API ONLY — the Deserialize path stays banned per the spike's trap), swapped in the pre-adoption amendment window (the JSON config shipped days earlier, zero users). Measured on weir itself: 0 new AOT warnings (3 = the pre-existing baseline), +324,928 B (7,696,168 → 8,021,096), startup unmoved (8 ms median). The posture CARRIES OVER and improves: unknown keys rejected with did-you-mean AND `config.toml:line:col`; `#` comments are the point; malformed → one readable line + defaults; scripts-never-read-it unchanged (the type still lives in the Repl module). A leftover `config.json` gets one plain sentence ("no longer read — the config is config.toml now"), never silence. **REVERSED, SAME DAY (2026-07-30): JSON RESTORED** — the user reconsidered the DEPENDENCY itself: Tomlyn joins the supply-chain/maintenance surface (weir's sole runtime dependency today is FParsec) for a 4-key file whose only real win was comments — zero-dependency outranks config comfort. The reversal cost nothing: the TOML commit never merged (reverted on its own branch), so TOML never reached main and no user ever held a `config.toml`. THE QUESTION IS NOW DOUBLY CLOSED — the spike's numbers (TOML viable, +330 KB, zero warnings) AND a tried-and-ruled-back data point both live in this row; the next "why not TOML?" reads a measurement and a reversal, not an opinion. The implementation knowledge (the TomlReader token walk, the Deserialize AOT trap) survives in the spike record and NOTES should it ever reopen. THE LOAD-BEARING PROPERTY: scripts NEVER read the config — the config type lives in the Repl module, so a script cannot reach it BY CONSTRUCTION (pinned: a malformed config + `weir script.weir` runs clean). OUT OF SCOPE, stated (the login-shell competition; each pushes toward the dynamism check-before-effects forbids, so not a matter of effort): job control, prompt customization beyond a static string, aliases, `cd -`/dirstack, keybinding config. In-session decisions: `echoElems`/`echoChars` config keys DROPPED (would be vacuous — the REPL renderer still uses Eval's constants; wiring them is on the still-lacking list). PARKED with a trigger: a config LIST of modules imported at REPL start (data pointing at declaration-only code — safe once distribution/`pub` lands); explicitly NOT an rc file. SECURITY.md gains the history-file line. | Tests "command heads: a command-callable builtin completes", "path completion: an explicit path lists the directory", "path completion never runs anything"; tests/repl/repl-quality.py (history XDG/dedup/0600, Ctrl+R fzf-stub + minimal fallback); ci/e2e.sh config block (reject-unknown, scripts-ignore-config); SEMANTICS "the REPL is for EVALUATING weir" bullet; NOTES: "REPL quality — good enough to evaluate weir in" |
| yaml-district | 2026-07-30 | YAML SESSION 2 of DESIGN-yaml-templates: the `yaml` DISTRICT — a CHECKED block literal. MECHANISM (the `!`-district precedent, upgraded): the assembler's third district species — a line ending in the word `yaml` (EXCEPT `to yaml`/`from yaml`, the adapters) arms it; block lines ride into the logical line VERBATIM with indentation RELATIVE to the block's first line, behind the sentinel with NO leading space (`JYamlLine`); the parser reconstructs the 2D structure from the sentinel-split tail, and fragment parses (splices, for-headers) run PADDED to their logical column so every span lands true and `translate` maps it physically — NO span-shift walk exists. `yaml` is NOT reserved: the district arm triggers on `yaml`+GLUED-sentinel, which user text cannot produce; the MACHINE-BOUNDARY GUARD (found the hard way: under `weir check` the assume-resolver made `yaml` a command head and the let-RHS command path swallowed the block) — a command head glued to the sentinel fails in `commandSegment`, since statement joins always SPACE the sentinel and only the yaml wrap glues it. TEMPLATE TYPING (`checkYamlTpl`, a new `TEYaml` typed node): value splices carry the LIFTABLE law (string/int/bool, `Yaml`, Option of one, seq of those — a seq splices as sequence items); a KEY splice (`$name:`) is string-typed; an unresolved template PARAMETER defers to the value-driven lift at eval — THE SORTBY POSTURE, stated (no type-class constraint exists; concrete violations check-error at the splice; polymorphic ones become a located runtime failure naming the law); literal duplicate keys are check errors; `for` binders bind via `binderShape`/`bindParams` (the lambda machinery) over the source's element type. EVAL (`evalYamlTpl`): value-driven lift (VStr→YStr…, Some unwraps, None OMITS its entry/item — the json-option omit in template form; a None ELEMENT of a spliced seq lifts YNull); template literal scalars type themselves (plain `3`→YInt, `true`→YBool, quoted→YStr, empty→YNull); `for` instantiates per element with `bindPattern`; RUNTIME duplicate keys (for-generated or key-spliced) are eval errors — invalid YAML never renders silently. Subtrees COMPOSE through functions (a district value splices into another district). THE FOR SPECIALIZATION (per PLAN-for-do's session-0 contract): no `do`, the body is the indented sub-template; sequence context yields items, mapping context yields entries (dynamic keys `for (k, v) in pairs` → `$k: $v` — the open-map answer); a block of only-for units infers its context from the body's shape. BOUNDED OUT, stated: mid-text `$` in scalars is LITERAL (compute with `$(…)`); full-line `#` comments inside districts (the run-path directive scan owns line-leading `#` — trailing ` #` works); block scalars (session 1's bound stands); editor grammars for the district landed as the arc's rider (NOTES: "yaml district editor grammars" — scanner state machine, TextMate while-region, micro exempt, REPL marker tint). THE README LINE, verbatim per the bless note: "you cannot write a YAML injection in weir, for the same reason you cannot write an argv injection" — the argv law generalizing to a second domain. | Tests "the yaml district assembles", "a command head glued to the sentinel never parses as a command", "district templates check", "district eval: splices lift, for instantiates, Option omits"; ci/e2e.sh district block (typed splices + for-keys + quoting + None-omission + roundtrip + splice-law on AOT); README the injection line; SEMANTICS "The yaml district"; SKILL + GUIDE (doc-tested); NOTES: "yaml session 2" |
| yaml-v1 | 2026-07-30 | YAML SESSION 1 of DESIGN-yaml-templates (blessed): the `Yaml` node union + `to yaml` (data path) + `from yaml T` — the typed-boundary family's TREE member. VALUE-DOMAIN FIRST (the bless note — answered by PROBE before the parser was written, then pinned): recursive unions + seq-of-tuple payloads DECLARE in weir source, so the union lives in the PRELUDE (`type Yaml = YStr \| YInt \| YBool \| YNull \| YSeq of seq<Yaml> \| YMap of seq<string * Yaml>` — the Option precedent: ctors/Show/class-laws free); Show RENDERS the recursion; Eq REFUSES it via the EXISTING no-seq rule with its own teaching text — no new rule, the machinery answered; no float case (weir has no float). THE OWNED PARSER (~300 lines, Yaml.fs — the spike's receipt: no YamlDotNet): the stated subset (scalars, block maps, block sequences incl. the compact `- key: v` k8s form, `#` comments incl. trailing, `---` multi-doc), an INTERNAL node tree carrying QUOTEDNESS + LINE POSITIONS (the public union carries neither — construction never needs them, conversion does), every rejection a teaching error WITH ITS LINE: anchors/aliases, tags, flow style, directives, complex keys, block scalars (session-1 bound; the quoted-\n spelling covers, block-scalar polish rides the district session), unclosed quotes, duplicate keys. THE SHAPE BRIDGE: eval has no env.Types (the [D:env-enums] precedent), so the checker packs a RESOLVED target tree (`Yaml.Shape`) into a new `TEFromYaml(tyName, shape)` typed node; `yamlShape`/`yamlableOut` are the recursive tree laws (scalars, nested monomorphic records, seq, seq<string * _> mappings, Option anywhere, Option<Option> rejected, declaration cycles guarded, `Yaml` nodes admitted on the OUT side only). READ SEMANTICS: `seq<string> -> seq<T>` one element per document; missing/null Option = None; null-in-REQUIRED teaches (`declare it Option<…>` — the json-option text); null where a collection sits = the EMPTY collection (the k8s `ports:` idiom); bool EXACTLY true/false and a QUOTED scalar is a string even at an int/bool field (quotedness is load-bearing — the Norway problem cannot fire by construction); extra keys ignored (from-json precedent). WRITE SEMANTICS: single value = one document, seq = `---`-separated documents, top-level pair-seq = ONE mapping document; record fields ALPHABETICAL (the VRecord law, as json) with YMap as the user-ordered escape; None field omits / None element renders `null` (the json-option split); THE QUOTING LAW (reverse-Norway, adversarially pinned incl. the bless note's additions): `"no"/"007"/"1e5"/"true"`-family, mid-line ` #`, `: `, leading specials, multiline (as `\n`-escaped double-quoted) all quote — a YAML reader cannot mis-type weir's output. LSP: go-to-def on the target type + hover doc key ride the TEFrom precedent; builtinDocs gains both adapters. One pre-existing pin moved legitimately (the unknown-format example WAS 'yaml'; now 'toml'). | Prelude Yaml union; Yaml.fs; Tests "yaml value-domain answers, pinned", "reverse-Norway quoting", "YMap preserves order", "yaml roundtrip", "multi-doc"; ci/e2e.sh yaml block (k8s tree, anchors-with-line, Norway-on-read, multi-doc, roundtrip on AOT); SEMANTICS "The yaml boundary"; SKILL; NOTES: "yaml session 1" |
| for-do | 2026-07-30 | `for`/`do` — THE GENERAL EFFECT LOOP (PLAN-for-do, blessed with four notes, each landed). The divergence frame decided it: F# HAS the form, declining it needs justification, and "we have Seq.iter" does not meet the divergence bar; plus shell muscle memory (`for f in *; do …; done` is THE shell loop) and the yaml district's `for` becoming a SPECIALIZATION instead of a one-place oddity. BOTH FORMS, ONE MECHANISM — desugar AT PARSE (the reifier precedent; the typed tree never sees `for`): `for <binderPat> in <expr> do <body>` ⇢ `xs \|> Seq.iter (fun p -> body)` (unit, eager, body unit via iter's type — the statement-rule error family for free), `[for p in xs -> e]` ⇢ `xs \|> Seq.map (fun p -> e) \|> Seq.force`. THE COMPREHENSION FINDING (the bless note said the finding matters more than the outcome): it IS the same path — the desugar BYPASSES EList entirely, so list-literal inference (the empty-list fresh var, element unification) is untouched, and `Seq.force` preserves the literal's eagerness contract; included, not parked. BARE COMMAND BODY (the bless addition, pinned explicitly): `for f in files do git add $f` — the body parses as a command chain (same fall-through as the statement classifier: known heads → expression body) and wraps as implicit `!(…)` (EPipe into print — streams, raises per iteration, unit); this is a `for`-SPECIFIC rule (`do` is a statement position; `then` bodies stay expression-only — probed, `if c then git status` still rejects). LAYOUT PINNED, NOT ANALOGIZED (the bless note): `do` at EOL opens an indented body block via `dangleOpensBlock` — with a WORD BOUNDARY (`t = "do" \|\| t.EndsWith " do"`), because the existing enders use bare EndsWith and `sudo` at EOL must not dangle (existing enders untouched — zero movement); `do !` arms a command district through the existing marker machinery (EndsWith " !"); assemble pin on the three-line body. THE KEYWORD SWEEP (the bless note): `do` grep = 38 prose occurrences (comments/messages), ZERO identifier-position uses of either word in pinned code/fixtures — reserving moved only the completion-inventory pin (both OFFERED, for starts / do continues, the if/then pairing). No guard syntax (a `when` clause = a third filtering spelling, refused). Eager, stated. THE LAW, stated once in SEMANTICS: pipelines transform; `for` effects. | Tests "for/do desugars to Seq.iter", "equivalence: both spellings", "a bare command body is implicit !(…)", "the comprehension desugars … bypassing EList", "for and do are reserved words; sudo at EOL does not dangle", "multiline body: the layout is the then-body rule, pinned"; ci/e2e.sh for/do block (command body, district, comprehension, statement-rule error) on the AOT binary; SEMANTICS the form + the law; GUIDE the shell-loop example (doc-tested); SKILL the idiom lines; NOTES: "for/do lands" |
| windows-viability | 2026-07-30 | THE WINDOWS SPIKE, Linux-measurable half (throwaway copy, deleted, nothing merged; the Windows-run half is a paste-and-run RUNBOOK in NOTES awaiting a real Windows host — every cell labeled MEASURED/SIZED/PREPARED/BLOCKED-HERE, never reasoned-as-measured). THE TWO FEARED ITEMS SHRANK UNDER MEASUREMENT: (1) the `\r\n` blast radius is NOT "the most surface" — stdout capture ALREADY handles CRLF/lone-CR and is oracle-pinned (Proc.fs scanLines; the capture-oracle pin), so the radius is **0 stdout pins**; only the stderr rule (deliberate `\n`-only, CR retained) holds **exactly 1 pin** — keep = 0 pins move + a Windows UX wart, strip-on-stderr-too = 1 pin moves (0 deliberate-`\r` fixtures anywhere else, grepped); the premise was retired by the representation session and nobody noticed. (2) tree-kill is NOT a POSIX-process-group reimplementation — the one kill site is BCL `p.Kill(entireProcessTree=true)` (Proc.fs:62), the same call on Windows; the open question narrowed to the orphaned-grandchild case (probe in the runbook) with job objects (~150 lines) only if the BCL walk fails. STRUCTURAL FACT, certain without a run: cross-OS NativeAOT is UNSUPPORTED (`error: Cross-OS native compilation is not supported`) — a Windows release requires a Windows build host; the managed win-x64 compile is CLEAN (no platform-conditional code errors). TIER-2 SIZINGS (sum well under ~100 lines, four arms): PATH resolver breaks today (':' split, no PATHEXT, no '\\' — Extern.fs, 25-40 lines); config/state dirs work-but-non-native (`C:\Users\x\.config` via the UserProfile fallthrough; native APPDATA arm 6-10 lines); history 0600 is a SILENT GAP on Windows (SetUnixFileMode try/with-swallowed → default ACLs, no error — ACL arm 20-30 lines + a SECURITY honesty line); env case-insensitivity ~0 lines (per-name GetEnvironmentVariable is OS-native-insensitive on Windows). VERDICT SO FAR: 3-4 small sessions + two bounded probes (conhost VT — REPL-only casualty; orphaned-grandchild), no blocker visible — but the verdict ISSUES only when the runbook's five sections run on real Windows (the spike's own bar). The `.cmd`/`.bat` BatBadBut hazard is a STATED NON-CLAIM regardless. SECOND PASS (a parallel deeper run, convergent on every shared number — the redundancy bought confidence plus one MISS): (1) **CRASH-ON-ARRIVAL FOUND**: the SIGWINCH registration (Repl.fs `PosixSignalRegistration.Create`) is UNGUARDED — `PlatformNotSupportedException` at interactive-REPL startup on Windows; `-e`/scripts/lsp/fmt route around it; the 2-line guard is ITEM ONE of any Windows session (not fixed now — nothing merges from a spike). (2) Refinements: product POSIX surface totals ~80-95 lines across EIGHT arms (three more small ones: completion `/` separators ~10, glob drive-letter roots ~6, 128+N signal-name cosmetics ~4); non-AOT self-contained win-x64 datum: 82 MB / 208 files (builds clean, the AOT contrast); suite denominator 922 blocks = 884 pure + 38 spawn/console (the lifecycle probes hardcode /bin/sh+pgrep and fail LOUDLY by design); the would-have-been stdout radius population: 24 runReal equality pins + 37-of-206 e2e expects on spawn captures — all immune by the existing strip; concrete Tier-2 consequence: `Extern.exists "fzf"` misses `fzf.exe`, so Ctrl+R silently degrades to the built-in search until the PATHEXT arm. Zero DllImport/termios/setsid/killpg/dev-paths in the product — the POSIX surface is exactly the eight arms. | NOTES: "The Windows spike — the Linux half" (carries the runbook verbatim, extended by the second pass) |
| repl-multiline | 2026-07-30 | THE REPL BUFFER BECOMES TWO-DIMENSIONAL — lines + a (row, col) cursor; the horizontal machinery (word nav, home/end, kill, Tab completion) applies per line unchanged. REPL-only, NO semantics: a submitted multiline entry runs through THE SAME `Script.assemble` a script uses, so identical lines mean identical statements (pinned both ways). ENTER-COMPLETENESS, the parser's own answer (not an approximation): submit iff the buffer assembles (no open brackets / pending bindings / dangling openers) AND no logical line's parse fails AT THE VERY END of its text — a mid-text failure SUBMITS and shows the error, so the user is never trapped adding newlines; a parse-fails-at-end (e.g. `match x with`) grows the buffer. THE ONE CARVE-OUT, found by the color probe HANGING on `let s = @"raw`: a line ending INSIDE A STRING also submits — weir strings are single-line (all four kinds), so more input can never close one and growing is a trap; detection reuses the ONE string-state machine ([D:one-scanner]: `stringScan` core, `foldOutsideStrings` + new `Script.endsInsideString` as wrappers — never a second quote machine in Repl.fs). Alt+Enter / Ctrl+J FORCE a newline even when complete (formatting — an entry stays ONE statement; Ctrl+J doubles as the deterministic pty spelling). KEYS (fixed, not configurable): Up at the FIRST line → history, Up elsewhere → cursor up (fish/ipython; Ctrl+R stays the explicit path); Down at the last line → history-forward ONLY while already browsing (histIdx < count — else Up could never be walked back), a fresh buffer's last line is a NO-OP (the plan's asymmetry, refined); Esc/Ctrl+C abandon the whole buffer; Backspace at column 0 joins the previous line, Ctrl+D at line end joins the next (EOF only on a fully empty buffer). PREREQUISITE THAT LANDED FIRST — history entries become LOGICAL statements: in-memory entries carry real newlines; ON DISK one entry per file line, backslash-ESCAPED (`\` -> `\\`, newline -> `\n`) — the file stays line-oriented and greppable, the cap counts lines = entries, 0600 unchanged; dedup compares WHOLE entries; a LEGACY entry containing a literal `\n` weir-string decodes with a real newline — accepted NOW, pre-adoption (amendment, not migration). SEARCH DISPLAY FORM: fzf matches per line, so an entry feeds as its lines joined with ` ⏎ ` (every line stays searchable — rejected first-line-plus-ellipsis, which hides match arms) and maps back to the full entry via a display->entry dictionary (identical displays imply identical text — lossless); the minimal fallback renders the same form. REDRAW is a REGION repaint: climb to the region top by the tracked cursor display-offset, `ESC[J`, repaint every line with its prompt (continuation prompt `"  ... "` — SAME WIDTH as `weir> `, so column math is uniform), reposition by display-row math; WRAP-aware (display rows = ceil((6+len)/width), the fill-exactly row leaves the terminal wrap-PENDING which the ceil and the \r\n emission agree about); growth scrolls naturally (all cursor moves are relative). RESIZE: full repaint on SIGWINCH via PosixSignalRegistration — BEST-EFFORT, stated (the climb to the top uses pre-resize wrap math). LIMITS, stated: a buffer taller than the terminal repaints only the visible portion (ESC[A clamps at screen top — fish shares the limit); no sticky column on Up/Down (clamp only); no auto-indent on the continuation line (the recommendation was match-previous-indent — deferred to the still-lacking list, the newline lands at column 0). The colorizer applies per line [D:repl-color]. Piped (non-tty) input NEVER enters the editor — untouched, pinned. | tests/repl/repl-multiline.py (Enter-completeness, Ctrl+J + Up-within-buffer edit, script twin, whole-entry recall, fzf ⏎-display + mapping, Esc abandon, wrap at widths 30 AND 80, piped untouched); SEMANTICS REPL bullet; GUIDE keybinding table; NOTES: "REPL multiline — the buffer becomes two-dimensional" |
| pipe-rhs-decides | 2026-07-29 | THE PIPE RULE — the RIGHT-HAND SIDE decides the glyph: `|` when the stage is a program or a reifier (command grammar), `|>` when it is a function. One table, symmetric, no exceptions: `cmd\|cmd`, `expr\|cmd` (the stdin feed), `cmd\|reifier` take `|`; `cmd\|fn`, `expr\|fn` take `|>`. THE ONE REMOVAL: `|` before an expression/function stage — `git rev-parse HEAD \| Seq.head` stops parsing; the spelling is `|>`. That removal makes the rule total and FIXES THE ASYMMETRY that produced the receipt (today you could pipe a command into a function with `|` but not a value — `[1;2;3] \| Seq.length` errored — a directional exception with no user-statable justification); now both halves error symmetrically. REIFIERS STAY `|` (the THIRD case, not an exception): a reifier terminates a command chain and produces a value — command grammar, not a function applied to output — so `git diff --quiet \| succeeds`, never `\|> succeeds`. ADAPTERS ARE FUNCTIONS → `|>` (`from json`/`from porcelain`/`to json` are `seq<_> -> seq<_>`; they already parse as expression stages, so the new rule requires `|>` for them automatically — no special-casing). IMPLEMENTATION: `pipeSepSpanned` captures the glyph + span; `foldChain` checks it against the RIGHT-hand seg kind (`requiredPipeOp`) and anchors the teaching error ON the glyph [D:anchor-before-read] — this catches the command-CHAIN forms (`cmd \| fn`, `cmd \|> cmd`, `cmd \|> reifier`). The value-headed operator form `expr \|> cmd` is caught by a post-parse walk (`pipeToCommand`, resolver-aware: an `EPipe` whose RHS is headed by an external command), anchored on the offending program. BOTH ERRORS: `cmd \| fn` → "'\|' chains commands; pipe expressions with '\|>'" (the existing hint, now firing in command mode too); `expr \|> cmd` → "'\|>' applies functions; feed a program with '\|'". Grammar files (tree-sitter/TextMate/micro) UNCHANGED — both glyphs stay operators lexically; the rule is semantic (which glyph is valid where), the mode boundary is the LSP's semantic-token job. PHASE NOTE: a breaking grammar change done PRE-publication — much cheaper now than after (the casing/JSON-rename precedent); this is a spelling change, byte-identical output everywhere. | Tests "'\|' before an expression stage is a parse error", "pipe accepts \|> in command mode too", + 10 migrated unit pins (NOTES: "the pipe rule"); ci/e2e.sh (13 pins migrated, 3 negative reject-tests kept); SEMANTICS command-grammar bullet (the rule, once); SKILL/README/GUIDE/examples/tools migrated (GUIDE caught a day late by ci/skill-doc.sh block 59 — quoted-arg pipes evaded the sweep grep; the `>`/`>>` redirect + composition TEACHING MESSAGES also named the retired spelling, fixed — NOTES records both lessons) |
| doc-help | 2026-07-28 | `--help` sources a flag's text from its `///` doc's FIRST line; `[<Doc>]` RETIRES from the attribute registry entirely (a stale `[<Doc "…">]` is now the ordinary unknown-attribute error, no retirement hint — phase-scoped per [D:drop-reify-builtins]). ONE SOURCE by construction (6th invariant-by-architecture instance after [D:doc-comments]'s five): hover and `--help` read the same `///`, so they cannot drift — the same reason [D:doc-comments] stored docs out-of-band, now paying a second consumer. First-line convention: line one is `--help`, the rest is hover-only; an empty first line yields NO help text (silence, not an error). The BRIDGE: docs are out-of-band (position-keyed, [D:doc-comments]), but `--help` derives at EVAL from the `RecordDef` that `TEArgsLoad` captured at CHECK — so the runner enriches `env.Types[decl].Docs` (a new check-time-erased `RecordDef.Docs: field->first-line`, erased like `Attrs`) in `Script.run`'s fold right after a `type` decl checks and BEFORE the following `Args.load` captures it; `fieldDocsFor` keys each field's doc by the physical span the field's anchor occupies. Only the run path needs it (hover reads docAttachments directly; check never renders `--help`). Scope: flags-record fields, BOTH tiers (shared + payload, merged in `scopeDef`), case-scoped help — the two-tier layout is unchanged. `Env.load` does NOT consume it (never did consume `[<Doc>]`). REJECTED: a `[<Help>]` rename (still two sources that can drift — the disease, not the cure). Bars: `--help` byte-identical for migrated sites; hover unchanged; the [D:doc-comments] alignment lint governs a misaligned migrated doc; zero behavior change beyond help sourcing. Migration was not a text swap — a `///` must sit DIRECTLY above its field (an attribute line between them breaks attachment; an inline `{ /// … }` is a trailing comment), so inline records reshaped to `[<attr>]` / `///` / field, Stroustrup. | NOTES: "/// feeds --help — the Doc attribute retires" |
| block-scalars | 2026-07-31 | LITERAL BLOCK SCALARS `|`/`|-` join the yaml subset — the ConfigMap workload (embedded scripts/configs), the gap yaml session 1 bounded out. NO CHOMPING POLICY EXISTS (the bless amendment): chomping is YAML's spelling for a distinction weir strings already have — `|` MEANS ends-with-exactly-one-newline, `|-` means ends-with-none, read and write agree by construction (the form follows the value, BOTH directions; always emitting `|` would break the no-op round trip on `|-` sources, and reading `|-` as `|` hands back a silently different string). Blockness is quotedness's sibling, recorded as the internal `NBlock` node case (an int/bool field errors on one — a block scalar is unambiguously a string); form preservation on round trip falls out of the render law, no carried flag. REJECTED with located teaching: folded `>`/`>-` (fold semantics are a genuine trap), `|+` (trailing-newline multiplicity the subset cannot express — a string ending in 2+ newlines FALLS BACK to the quoted-with-escapes spelling at render: valid, exact, round-trips; the never-drop-bytes argument forbids DROPPING, which the fallback does not do — corrected by the content-bytes audit, which retired the session's ERROR spelling), explicit indentation indicators `|2` (content indentation is detected), inline content on a header. Content is BYTES both sides: blank lines are newlines, ` #` is not a comment, more-indented lines keep their extra indentation; read-side content comes from the RAW lines (the filtered view drops blanks/comments) with an extent-consistency guard (a dedented `#` line that would strand deeper lines is an error, never a silent drop). THE CENTREPIECE, districts: block scalar content is LITERAL — no splices, no `for`, no interpretation (embedded scripts are full of `$VAR`; substituting would be the `sh -c` fixture bite writ large, silent and in production). One sentence: content is bytes; templated content splices as a whole value. The content lines are consumed as TEXT before the splice/`for` scanners — order pinned with adversaries ($name, $(expr), for-lines, key-shapes, blanks, deeper indents). Rejected: an opt-in "templated block" marker (inventing YAML syntax is what the subset discipline refuses). RIDERS the session forced: blank lines inside an ACTIVE yaml district now ride the sentinel as empty verbatim lines (bytes in block content, structure-transparent elsewhere — dropping them silently was the never-drop class); the run-path directive scan narrowed to COLUMN-0 (a directive is a statement-position thing; `#!/bin/sh` inside a block is content), which also made full-line `#` yaml comments inside districts structure-transparent — the parked bound, closed. The reverse-Norway multiline case MOVED deliberately: a multiline string is a block scalar, not a quoted one; the quoting law governs single-line strings only. fmt: block content rides the district relative-indent law (never re-indented). Grammars: the tree-sitter scanner gained block mode (whole content lines as text tokens — no splice/for/key scanning inside; serialize grew to 5 bytes); TextMate bounds content with a while-backreference to the header's indent (content scoped literal); micro stays district-exempt. | Tests "block scalars read/render/district…/fmt…" (4); e2e ConfigMap workload + header-position error + roundtrip; GUIDE ConfigMap block (doc-tested); SEMANTICS subset + quoting law + district bullet; NOTES "block scalars" |
| content-bytes | 2026-07-31 | THE "CONTENT IS BYTES" AUDIT — the class named after three collisions became seven: every layer that reasonably normalizes text is wrong inside a byte-exact region. FIXED (mechanical, this audit): `stripComment` + the comment-only filter vanished `//`/`///`-shaped block content (assemble now takes RAW lines and classifies/strips INTERNALLY — comment-only and code lines inside an ACTIVE yaml district join their raw bytes; structure still parses the stripped text, so `//` comments keep working outside districts and full-line `#`/`//` shapes stay structure-transparent inside them); the tab rejection reached inside blocks (narrowed: a tab after the space indentation inside an active yaml district is CONTENT); the sentinel split and fmt trimmed trailing whitespace (both keep it; fmt strips only the space indentation so a content-leading tab survives); the doc-attachment pass, the doc-align lint, and fmt's canonicalizeDocs treated `///`-shaped content as doc syntax (all three skip via the shared `districtContentMask` — content is never doc syntax, never re-anchored, never linted). CRLF DECIDED: a source file's line ending is not data — normalized at read (ReadAllLines), so a CRLF-saved file behaves byte-identically; pinned in the fixture. THE STANDING GUARD: the hostile-byte fixture (ci/e2e.sh) — one block scalar carrying blank/spaces-only/`#`/`#!`/`//`/`///`/tab/`---`/trailing-ws/splice-lookalike/for/deeper/dedent/unbalanced-bracket lines, byte-exact through check AND run, plus the CRLF twin and fmt value-preservation; printf-generated because a checked-in literal invites the mangling it guards against. STATED NORMALIZATIONS (value-preserving, not mangles): fmt renders a whitespace-only content line as empty (both spellings are a blank line); mid-line ` #` on district STRUCTURE lines was data while the read side stripped it — RECLASSIFIED at review from accepted asymmetry to weir-vs-weir divergence and fixed by [D:district-hash]. R1 CORRECTED: 2+ trailing newlines fall back to quoted-with-escapes (valid, exact, round-trips) — the session's ERROR spelling retired; never-drop-bytes forbids dropping, which the fallback does not do. FINDINGS (sized, not fixed): the fuzzer grammar generates no yaml districts (coverage gap; its reindent transform is uniform so rel-offsets are safe by construction when it does); docAttachments inside BANG districts unmasked-but-benign (comment-stripped at run). Denominator: 16 sites examined, 10 touching content, 7 mangling (all fixed). | ci/e2e.sh hostile-byte fixture (byte-exact run + CRLF + fmt); Tests block-scalar pins re-run green; PROCESS "Content is bytes"; NOTES "the content-bytes audit" |
| district-hash | 2026-07-31 | MID-LINE `#` IN DISTRICTS: THE TWO PATHS AGREE — the content-bytes audit recorded "mid-line ` #` on district structure lines is data while the read side strips it" as an accepted asymmetry; re-read at review it is a weir-vs-weir DIVERGENCE with a determined fix direction: YAML's own rule (a `#` preceded by whitespace begins a comment) says the read side is right and the district was wrong. The bite was in the OUTPUT: the reverse-Norway law faithfully quoted the polluted value, so `to yaml` emitted `image: "nginx:latest # pinned by ops"` — silently wrong generated YAML, uncatchable from inside (the weir→weir round trip was self-consistent; the divergence was against text PASTED into a district, and paste-a-manifest is the district's pitch). Nothing is lost: a comment never reached the output as a comment (districts build nodes); it used to survive attached to the wrong value, now it disappears, which is what comments do. THE FIVE-CASE TABLE (stated once, SEMANTICS): full-line structure = comment; whitespace-preceded mid-line structure = comment (THIS decision); `a#b` glued = scalar; quoted `"a # b"` = data; `$(…)` hole interior = expression text; block-scalar content = bytes. ONE MACHINE, TWO FACES: the yaml comment scanner (Yaml.commentCutAt) parameterized — the plain face is YAML's lexical rule (read side, unchanged); the district face also skips $() holes, whose interior follows weir string rules — no second quote machine. `for` headers take the same cut. BONUS the rule bought: trailing comments after splices (`key: $name # note`) now parse (they errored before). Corpus sweep: ZERO occurrences of mid-line # in committed district sources — no pin movement anywhere. Grammars: the tree-sitter scanner stops yaml_text at a whitespace-preceded `#` (internal hash_line paints the comment; quotes/holes/block mode excluded); TextMate's existing district comment rule verified on-engine and the value catch-all widened with a lookbehind so glued `a#b` stays one data token; micro exempt. The fuzzer's missing yaml-district production is now in GRAMMAR.md's stated denominator. | Tests "district mid-line #: the five cases"; ci/e2e.sh the pasted-manifest acceptance (`image: nginx:latest` unquoted, quoted-# preserved); SEMANTICS the # table; GRAMMAR.md the denominator line; NOTES "mid-line #" |
| contracts-spine | 2026-08-01 | EXTERNAL CONTRACTS, THE SPINE — one concept, three customers (schemas now; command signatures and remote modules inherit it): a VENDORED, PINNED artifact that constrains what the CHECKER accepts and contributes NOTHING at run time. The four properties, each load-bearing: vendored (checked in under `.weir/`, never fetched during check), pinned (exact identity + sha256, NO RANGES ever — pairwise comparisons, not a graph), check-time only (deleting every contract leaves every script byte-identical — THE pin, e2e), declared not discovered. THE RULINGS (PLAN-contracts-spine, written before building): `.weir/` kind directories + `lock.json` at its root; the command family RE-RULED pre-merge (PLAN-restore-rename): `weir add <kind>` / `weir restore` / `weir verify` — `vendor` named the STORAGE STRATEGY not the act (backwards for a verb); `restore` is .NET's word for exactly this operation, right AND familiar for weir's audience. THE SPLIT'S REASONING: `add` is KIND-AWARE because acquisition genuinely differs (schema=url fetch, sig=GENERATE from the installed binary — absorbing the signatures design's separate `sig generate` verb into the subcommand, one acquisition entry point — module=clone at a ref); `restore`/`verify` operate on the LOCKFILE and are kind-agnostic BY CONSTRUCTION (every entry is source+hash+path). Kind names: `sig` (matches .weir/sigs/), `schema`, `module` (NOT `import` — that is the consuming STATEMENT, not the artifact). Alternatives recorded closed: fetch (sounds partial), sync (vaguer), install (implies system mutation), pull (git-tainted), noun-scoped `weir contracts …` (rejected at two commands; TRIGGER recorded: a third contracts command, or `verify` going ambiguous when signatures land, reopens it). THE LOCKFILE IS THE MANIFEST — deliberate: manifests exist to hold RANGES a resolver pins; contracts have no ranges, so the lock states everything (`add` writes, `restore` reads, `verify` checks). The checker tells restore from add apart BY THE LOCK: entry-but-no-file teaches `weir restore`, no entry teaches `weir add schema <url> --as <name>` — both pinned; the lock is Utf8JsonWriter/JsonDocument (reflection-free), ABSENT until the first fetch, and IS the standing record of intent (ruling 9 — `vendor add <name> <url>` writes it, bare `vendor` restores hash-verified; no separate manifest); fetch failures each named (unreachable host / non-200 / restore-hash-mismatch; redirects followed, the DECLARED url kept); discovery walks up to the FIRST `.weir/`, stops there, bounded by `.git` (dir or file — worktrees) and root, errors naming both ends; `verify` two-arm shaped (the hash arm today, the signature arm slots beside it) reporting ABSENT vs MODIFIED distinctly, exit 1 on either. `weir check` never fetches/spawns/touches the network — pinned (unreachable lock URL + vendored file = fine; missing file teaches `weir vendor`). weir's FIRST network code (HttpClient, explicit commands only). The type-provider answer, stated once (LEXICON): contracts are type providers minus the execution and plus the pinning. | Tests discovery-walk + subset pins; ci/e2e.sh contracts battery (vendor/verify lifecycle vs a local server, property 3, never-fetch); SECURITY.md the non-claim; SEMANTICS "External contracts"; plans/DESIGN-external-contracts.md + PLAN-contracts-spine.md |
| yaml-schemas | 2026-08-01 | YAML SCHEMAS — the spine's first customer, chosen BECAUSE it exercises every part (signatures need no vendoring; building the fetch machinery without a customer is what the design warns against). DECLARATION: `yaml schema=<name>` on the district marker line — per district, author-named, never inferred (`kind` is a Kubernetes convention; inference would teach weir one ecosystem and break declared-not-discovered). THE SUBSET, corpus-measured (six real k8s standalone-strict schemas, 17KB–714KB): IN — type (string|array-of-strings), properties, required, items, additionalProperties (false|schema — 906/106 corpus uses), enum, oneOf over SCALAR alternatives only (all 143 corpus occurrences are IntOrString); annotations accepted (description/format/title/$schema/x-*); REJECTED with keyword+JSON-path teaching — $ref/$defs (the teaching names the STANDALONE variants: refs inlined at publish, so remote-$ref is MOOT), composition (allOf/anyOf/not/if-then: zero corpus uses), patternProperties, const, the constraint family (pattern/minimum/…: zero corpus uses — each joins when a corpus needs it). VALIDATION: structural always (unknown fields WITH did-you-mean at the KEY's own span — the typed template gained spans; missing required at the map's span; misplaced nesting falls out), value where types permit (splice checks by weir type incl Option-unwrap and seq-elements; enum/pattern on splices unchecked, stated); for-entries and key splices RELAX unknown/required for their map (dynamic keys may supply). Gates CHECK and RUN both (check-before-effects); the LSP inherits via analyzeLines. POLISH RULINGS (PLAN-schema-polish): messages carry FIELD+PATH always (root suffix-free — the shallow pins kept their text); enums cap at 6 with an honest remainder, a ONE-element enum states its value plainly (the unconditional-ellipsis bug: k8s kind rendered 'ConfigMap…'); the `/null` in integer/null KEPT (k8s marks most fields nullable; hiding it makes a legal `replicas: null` unexplainable) — not re-litigated; THE STRICT-VARIANT DEPENDENCY IS LOAD-BEARING (unknown-field checking IS additionalProperties:false — plain -standalone is silently inert, so `add schema` warns when a schema closes nothing, the vacuous-pin class); `schema` is MARKER-LOCAL, never a keyword (would reserve the identifier) — the marker context completes `schema=` and the vendored names, the predicate shared via Parser (one classifier, three consumers). THE FIND: the machine-boundary guard needed its marker+schema face — `yaml schema=x` glued to the sentinel parsed as a COMMAND under check (head "yaml", arg glued); the head guard now refuses ` schema=<name>`+sentinel too, and the sibling-sentinel acceptance pin's fixture was corrected to the assembler-real SPACED join it always should have used (fixture-realism, the audit's class). | Tests subset + discovery pins (955); ci/e2e.sh: apiVerison did-you-mean + misplaced nesting vs the REAL committed configmap schema, property 3, never-fetch; tests/fixtures/configmap-v1.json (17KB, published corpus); SEMANTICS "YAML schemas"; SKILL mention |
| log-module | 2026-08-01 | THE `Log` MODULE — levelled diagnostics that respect the pipeline. THE LAW (not a default): every member writes to STDERR unconditionally — stdout is DATA (what pipes carry, what $() captures), so there is no stream knob, no Log.toStdout, and stdout is BYTE-IDENTICAL at every level (THE pin, e2e headline). Level control: `WEIR_LOG` (trace|debug|info|warn|off), read ONCE at startup, default `info` (ruled — info useful without ceremony, debug/trace opt-in); invalid values are a loud startup error naming the levels (reject-don't-guess), exit 2 before anything runs; the NO_COLOR-precedent posture stated — ambient, but it changes what is PRINTED, never what the script COMPUTES (pinned). Composition with children is free: WEIR_LOG rides the env sigil (`!e`/`$e`) like any var — e2e-pinned, nothing built. `Log.error` DROPPED (ruled), the module's best boundary: a filterable error means WEIR_LOG=off suppresses the one message a failing user needs; every framework answers with "error never filters", which makes the level vacuous for that member — weir already has the channels: filterable=Log.*, unconditional=printerr, stop=fail; `warn` is the TOP of the filterable range and the docs pre-empt the missing member. NO structured logging (to json is the structured answer — recorded so field-arguments argue against an entry). No timestamps (ts/journald own that; recommendation adopted). EAGER ARGUMENTS ruled option 1+2: the plain members are the API and evaluate their strings regardless (weir has no lazy positions — the language does not pretend otherwise), and the four `With` twins (`Log.debugWith (fun () -> …)`) run the thunk ONLY when the level passes (side-effect-proven pin) — the Option.defaultWith spelling, four thin wrappers. Format: `LEVEL message`, level tinted via the SHARED Color palette when stderr is a tty (Color moved Script→Types so Builtins reaches it; Script aliases, Repl/Program repointed); plain form pinned for harnesses. | ci/e2e.sh Log battery (byte-identity, levels, off-silence, invalid, plain-when-piped, thunk laziness both ways, env-sigil child, -e); repl-color.py prompt probe; SKILL + GUIDE (the three-channel table, the eager caveat); NOTES "the Log module" |
| windows-v1 | 2026-08-02 | WINDOWS v1 SESSION 1 (make it work; tree-kill is session 2). THE GATE: `Extern` splits PATH on `Path.PathSeparator` (the no-`#if` model) and resolves bare names through PATHEXT on Windows (name-as-given always wins first; POSIX byte-identical — the ext list is empty there). `PosixSignalRegistration` (SIGWINCH repaint) guarded off on Windows — resize repaint absent, REPL starts. PATH MEMBERS ARE PLATFORM-NATIVE, ruled: they manipulate filesystem paths, so the platform's own spelling is their job (the `System.IO.Path` contract weir already inherits); INPUT STAYS LIBERAL (forward slashes work everywhere — Windows APIs accept them), OUTPUT IS NATIVE. The accepted cost, stated: a script string-comparing a computed path against a literal, or embedding one in json/yaml, is platform-dependent — the script author's problem; `Path.combine`/`Path.dir` are the safe spellings. The ALTERNATIVE recorded, not just rejected: POSIX-normalized output (Go's `path` posture) is deterministic cross-platform at the cost of showing users paths their OS does not write; `Path.toPosix` is the escape hatch if a portability receipt arrives. A `Path` TYPE (the PathBuf posture) is PARKED with a trigger — language-scale here (every stringly boundary needs a visible conversion; four boundary field laws grow a case); trigger: a real bug from platform-dependent path strings, not a test expectation. THE STAMP MOVED INTO THE BUILD (`WeirStamp` MSBuild target in Weir.fsproj): every publish path (`dotnet publish`, a future .ps1, CI) stamps identically — short hash + porcelain-dirty + nogit, verified honest across INCREMENTAL rebuilds (HEAD move and dirty flip both refresh); publish.sh is publish-and-copy. CONFIG/STATE on Windows: `%APPDATA%` / `%LOCALAPPDATA%` (no XDG); THE 0600 QUESTION answered by INHERITANCE, stated not silent: Windows has no chmod, the history file inherits the profile ACLs that already deny other non-admin users — SECURITY.md carries the platform split; no AccessControl dependency (zero-dep outranks). ENV CASE: every lookup rides `Environment.GetEnvironmentVariable`, which is case-insensitive on Windows BY THE OS — nothing to build, pinned both-ways (the pin asserts each platform's own rule). The suite: 39 POSIX fixtures marked `skipOnWindows` (sh/coreutils/absolute POSIX paths — marked, not shimmed; shim candidates listed in NOTES for when the CI matrix can verify), temp paths ride `weirPath` (forward slashes into weir source — the `\U` escape trap), expected paths ride `platformPath`. CLI teaching arms (the rider): `--e` did-you-means `-e`; `-e` names its arity (the Windows shell-splitting trap); `--help`/`-h` now exist (stdout, exit 0). Windows arms are VERIFIED-BY-HAND until the CI matrix (github migration) — stated. Story: NOTES "Windows v1 session 1". |
| windows-s2 | 2026-08-02 | WINDOWS v1 SESSION 2 — the runbook's second pass. THE CORRECTION FIRST: tree-kill WORKS on Windows (Ctrl+C reaches grandchildren, natural exit clean) — the spike's three contrary findings all came from ONE fixture flaw: `Start-Process` launches DETACHED, so every conclusion measured PowerShell's detachment, not weir's kill (the lesson is in PROCESS beside the pgrep one — instruments that lie). JOB OBJECTS DOWNGRADED to a PARKED robustness item, not a correctness fix: the only remaining gap is a HARD kill (TerminateProcess — no user-mode cleanup runs), and POSIX has the same hole under SIGKILL modulo the process group — a PARITY question, not a Windows deficiency; trigger to reopen: a receipt where a hard-killed weir's orphans cause real trouble. PATHEXT: honour the FULL environment list, DECIDED — weir's PATH resolution should mean what the platform's means (a bare name resolving differently in weir than in cmd is its own surprise; .VBS/.MSC/.CPL are the platform's choice); session 1's code already read the env — session 2 PINS it (custom-extension probe + .bat probe). CreateProcess runs batch files DIRECTLY, so no cmd /c synthesis exists anywhere — same argv vector as any program; the batch NON-CLAIM in SECURITY: a batch interpreter re-parses its command line (BatBadBut), so word integrity holds up to the hand-off (native executables verbatim — the runbook's injection probe). THE LEADING-SPACE REPL BUG was a LINUX REGRESSION, not Windows-specific — introduced when repl-multiline adopted the script assembler (whose indentation model makes a first line's indent a continuation of nothing); the rule: a leading-space FIRST line has no statement above to continue, so the entry DEDENTS by the first line's indent, relative structure preserved (a pasted block keeps its shape), applied identically in bufferComplete and submission (the two must agree or Enter's verdict and the meaning split); pty-pinned both halves. ^print fixture DIAGNOSED WRONG, colorizer right: Windows System32 ships a legacy print.exe, so the verdict flip was CORRECT — fixture now uses ^show (a binding absent from every platform's PATH). Suite: the 11 → fixed (2 Env.fromFile weirPath sites, Path.combine platformPath, head/positive-controls/path-completion marked, env-sigil chain closed by the parse shims); sort.exe REMOVED from the shim list (System32 ships a real one that tests RUN — an empty shadow would break it). THE REPL-PLAY FINDINGS (same branch): blank-line ESCAPE — Enter on an empty final line closes a PENDING buffer (error shows, input kept; Ctrl+C was the only, input-losing, way out) — the general protection over every uncompletable state; Alt+Enter/Ctrl+J deliberate blanks never trigger it; mid-buffer-Enter (finding 3) DOES NOT REPRODUCE on Linux (Down-then-Enter submits) — hypothesis: terminal ESC-prefix Alt misdetection after arrow keys; the escape un-traps the symptom regardless; Shift+Enter documented NOT bindable (terminals send plain Enter). Story: NOTES "Windows v1 session 2". |
| windows-s3 | 2026-08-02 | WINDOWS v1 SESSION 3 — the hand-run's findings (Windows effectively green: 918/963 + 43 stated skips at the second pass). THE LSP CRASH: System.Uri refuses a bare C:\ path (one-letter "scheme") — pathToUri died on the first Windows refresh and took the server with it, 5x then give-up. Fixed BOTH DIRECTIONS hand-rolled (a one-way fix is the mirror bug surviving): backslashes→/, per-segment percent-encoding, drive lowercase on the wire (VS Code convention) / UPPER on the way back (platform canonical), file:///+empty host; ACCEPTANCE = the round-trip pin (path→uri→path identity, both platforms, spaces+non-ASCII, encoded drive colon accepted). THE ROBUSTNESS HALF, its own finding: an unhandled exception in a request path KILLED the server — now a per-DOC guard in refreshAll (one bad document logs and skips, the rest still publish) plus a request-level backstop; lsp-e2e pins survival (bad didOpen, next request answers). Coverage-gap note: the safety review's LSP pass tested malformed FRAMING, not malformed PATHS — this was the miss. THE COLORIZER was the dedent's UNENUMERATED THIRD CONSUMER: head verdicts ran on the raw indented text while completeness/submission ran dedented — one dedentEntry now feeds all three (verdicts on the dedented text, painted behind the typed prefix); the pin is a TRIPLE (executes + completes + paints), the pair pin let it through; the general lesson is in PROCESS (a normalisation's consumers are enumerated, not remembered). The two sweep misses: `into` marked skipOnWindows with the PRODUCT GAP STATED (into IS the sh -c path; its Windows shell would be cmd /c — a real small feature awaiting a receipt, a stated gap beats a blind shim); the NINTH weirPath site fixed and the mechanical GetTempFileName/GetTempPath grep confirms no tenth (the one unwrapped survivor is the shim dir itself — a native PATH entry, deliberate). Blank-line escape's KEEPS-THE-INPUT half pinned (the escaped buffer submits and shows its error — the property that outranks the leading-space fix). Docs: Windows Terminal claims left-Alt+Enter (fullscreen) — Ctrl+J/right-Alt spelled out in GUIDE + README. The 2D-editor probe pass (wrap/arrows/resize/Ctrl+R) stays a HAND-RUN checklist in NOTES — resize handling is absent by construction (SIGWINCH guarded off). Story: NOTES "Windows v1 session 3". |
| lsp-uri-spelling | 2026-08-03 | THE SQUIGGLE BLINK (Windows report: diagnostics flash once and clear) — a URI-SPELLING SPLIT: clients spell URIs their own way (VS Code percent-encodes the drive colon, c%3A) and the server re-derived publish URIs via pathToUri, so ONE document became TWO strings — the diagnostic landed on the server's spelling and the every-open-doc-publishes-empty pass landed 0 diags on the CLIENT's spelling, which the client applied last: blink. REPRODUCED ON LINUX with a percent-encoded letter (file:///tmp/%77eir…), no Windows machine needed. THE RULE: a publish rides the CLIENT'S OWN URI STRING whenever the diagnosed file is an open doc (path -> client-uri map built per refresh); pathToUri is only for files the client never named (modules). Pinned in lsp-e2e: the encoded spelling gets EXACTLY ONE publish, under that exact string, and none under a re-derived one. `weir lsp --debug` added (tolerated argv): method dispatch + every publish (uri + count) to stderr — editors surface server stderr, so the next blink-class mystery is a log read. Session lesson, re-learned: an AD-HOC probe bypassed the freshness gate (assert_fresh) and a short-circuited publish (grep -c exits 1 on zero) left yesterday's binary under test — the "fix didn't work" was a stale binary, again; ad-hoc probes assert freshness or they lie. Story: NOTES "the squiggle blink". |
| bare-comma-match | 2026-08-03 | BARE-COMMA TUPLES IN MATCH — the gap closed (rider; origin: two findings from writing the portable showcase). The asymmetry was real: [D:bare-comma] decided commas build tuples in binder and RHS positions, then `match a, b with` and `| Some d, _ ->` refused — not a stated divergence, a gap. BOTH fixES are entry-point swaps producing ALREADY-CHECKED nodes: the scrutinee now parses `commaExpr` (the let-RHS's own production; `with` is reserved, so it terminates the chain), the arm now parses `commaPats` (the binder positions' own production; `when`/`->` are not commas, so the guard sits outside the tuple by construction). Zero checker surface, zero precedence or commit-point changes — no stop-and-report fired. The risk surface pinned: command-mode commas byte-identical (existing e2e pin), `f a, b` STAYS `(f a), b` (the knowingly-imported footgun, now pinned so the fix cannot move it), `[a, b]` stays one tuple element, the bare `for k, v in` binder ALREADY WORKED (binderPat was commaPats all along — confirmed and pinned), guard-outside-tuple pinned. LEDGER OUTCOME: CLOSED — no divergence row needed; both shapes are oracle rows verdict Same (158). SEMANTICS states the rule once: bare commas build tuples in BINDER, RHS, SCRUTINEE, and PATTERN positions — one rule, four positions, no exceptions. The showcase's parenthesized spellings stay (still correct; no corpus churn in the rider). Story: NOTES "bare commas, four positions". |
| add-validates | 2026-08-03 | `weir add` VALIDATES BEFORE IT WRITES — the principle for every contract kind, present and future (origin: live use pointed a schema URL at a GitHub BLOB page and add saved the HTML and LOCKED it — the one trustworthy artifact re-materializing garbage on every fresh clone). THE RULE: add validates the artifact against everything the checker will later require and writes NOTHING if it cannot; an artifact that passes add and fails at check has already broken the lock. SIX GATES in order, nothing persists until all pass: content-type (reported, not gating alone); JSON parse — the failure names what came back AND offers the REWRITTEN raw URL when the host is github.com/gitlab.com (the blob-page mistake is the most likely user error and the fix is a URL edit the user may not know how to construct; `rawUrlHint` exported, unit-pinned incl. gitlab subgroups); schema-shaped (top-level object with ≥1 of $schema/type/properties/$defs — someone's package.json rejects here); THE SUBSET CHECK AT ADD TIME, not at first use (an out-of-subset schema never reaches the lock; the old inert-warning's `with _ -> true` swallow — unparseable sails through — is exactly the disease, removed); the inert-schema warning (warn-and-proceed, unchanged); ONLY THEN file + lock entry together, with a delete-on-failure guard. Program.fs no longer pre-creates .weir/ — a failed add leaves the tree BYTE-IDENTICAL including no empty .weir (the strongest e2e pin: HTML/non-schema/out-of-subset all fail with no .weir at all). No repo residue from the incident (the user's local .weir carries it; `weir verify` flags, re-`add` heals). BONUS, the finding worth the line: the schema validator's FIRST UNPROMPTED CATCH was the showcase's own district — `annotation` (typo, did-you-mean nailed it) and replicas/rollout (Deployment concepts on a Service); the district now describes a real Service, and the showcase RUNS IN E2E as a build gate (a composition test disguised as a document must fail builds when invalid). Story: NOTES "add validates before it writes". |
| encoding-law | 2026-08-03 | HASHING, BASE64, AND THE ENCODING LAW (the gap audit's session A, receipts-only half). THE LAW, stated in SEMANTICS as the prerequisite it is (verified ABSENT before the members landed — behaviour existed, the law did not): weir encodes and decodes UTF-8 at EVERY boundary — what gets read, written, hashed, and base64'd is UTF-8 bytes; about BOUNDARIES, not representation; BOM detection where a stream may declare otherwise (the capture rules, pinned); strict contexts reject invalid UTF-8 as an ERROR — corruption must not wear a success. MEMBERS: `Str.sha256` (lowercase hex = sha256sum parity, the tool it replaces; SHA-256 ONLY — md5 is broken and offering it invites use, sha1 deprecated, sha512 receipt-less; more on receipt, so "why no md5" argues against a standing entry); `Str.toBase64` (ONE unwrapped line — GNU base64's 76-column MIME wrap and its -w0 tax simply do not exist here, a genuine improvement over the shell spelling, stated in docs); `Str.fromBase64` raises on malformed input AND on valid-base64-of-non-text (a PNG's bytes through GetString would be U+FFFD corruption wearing a success); `Str.tryFromBase64` = None for BOTH cases (X asserts, tryX asks). Liberal in: unpadded standard base64 accepted (padded emitted, the one stated default). THE MINER MIGRATION IS THE ACCEPTANCE: corpus-mine.weir dropped `| sha256sum` — THE CORPUS IS COREUTILS-FREE — and reproduces its numbers exactly (base: extracted=5 unique=3 kept=0 before and after); filename hashes change by the feed-wrote-a-trailing-newline delta, stated, partition unaffected (dedup is by content). PARKED with triggers: URL-SAFE alphabet (-_, unpadded) — trigger: a script decoding a JWT payload; naming will make the choice visible, never a silent default flip. `File.sha256`/`File.toBase64` — both blocked on the byte-type gap (File.read is seq<string>, line-oriented, would corrupt binary), stated ONCE so the gap is legible; trigger: a receipt wanting a binary file's digest/encoding (cert/image into a manifest); NOTE the eventual shape — a byte-oriented File member that never becomes a weir VALUE (digest in, string out) can serve the receipt WITHOUT a byte type, so the park is not gated on a language addition. GUIDE gains the Secret-data idiom (base64 into a yaml district — the receipt's home). Story: NOTES "hashing, base64, and the encoding law". |
| interior-arming | 2026-08-03 | COMMANDS AS INTERIOR STATEMENTS — the arming rule, unified (semantics change, no users to migrate; prerequisite for the within-scopes design). THE RULE, one carve-out: a command line in interior-statement position is an EFFECT (streams, raises, unit — the for/do implicit-!() desugar, GENERALIZED); final/value position stays CAPTURE-typed; when the context demands unit of the final position (if bodies, iterated lambdas) the CHECK DIRECTION arms it (armTail pre-pass on if-then + a check-mode ESeq propagation + the commandish-under-TUnit rule — needed because the plan's own if-example arms its FINAL line, which fold-position alone cannot see). Top-level becomes an INSTANCE of the interior row — the subtraction is the argument. THE CARVE-OUT, stated and pinned: a script's body is a statement sequence, never a block with a value — every top-level command is non-final by construction; the last line still streams and raises. Parse side: seqExpr elements try the let-RHS chain grammar (tagged; arming decided at fold where finality is known; exitCode-discard keeps the for/do fatal); PLAIN PARENS STAY EXPRESSION TERRITORY (a thread-local gate — the first cut leaked phantom commands into `(scriptPath |> …)` and ate the Self teaching; lambda BODIES re-enable through parens because `Seq.iter (fun f -> git add $f)` is the idiom). THE REIFIER GAP diagnosed as predicted (the tuple-comma class): reifierEnd's follower set lacked the statement separator, demoting `| orFail "m"` to a bareword stage as an interior statement — one alternative added. For/do's single-chain body yields to the sequence when a sibling follows (it was stranding siblings outside the binder's scope — found by the enumeration probe). Raise timings pinned both ways (armed = immediate, tail never runs; capture = at force). Sharp case 4 DECIDED no warning: a discarded-would-be-capture was never bound, and the armed effect is what the line SAYS on the new reading. FLIPPED PINS, named: the sibling-sentinel ACCEPTANCE shape (armed EPipe), the command-first-body rejection (unit + e2e twins — now checks clean; the at-the-head quality property moved to a non-command fixture). THE ! DISTRICT'S JUSTIFICATION, enumerated: if-then bodies (now redundant), for do ! multi-line (now redundant — seqExpr serves), top-level runs (redundant-ish; retains the command-ONLY territory guarantee), and the ENV variants !ev (NOT redundant — nothing else distributes an overlay until within-env). Verdict REPORTED not acted: plain ! is a deletable-candidate for its own bless after the rule settles; !ev survives narrowed. Corpus reads FINE under the new rule (the showcase's districts still work; no churn per the bar — an idioms sweep may later simplify). Story: NOTES "commands as interior statements". |
| within-scopes | 2026-08-03 | `within` — SCOPED RESOURCES, session 1 (kind `tmp`). THE KEYWORD-OVER-SIGIL REASONING, on file so the sigil question stays closed: the sigil space was walked twice — `$`-as-marker collides with splice-application, `!`-as-marker is categorically wrong (effect-and-discard; scopes PRODUCE), the paren/brace variants break the district family or promise false delimitation; the structural fact is that $/! encode a TOTAL two-way value/effect distinction, so there is no third glyph because there is no third CATEGORY — only new KINDS, and kinds want names. `within` verified essentially unclaimed (searched: no mainstream language reserves it; SQL's WITHIN GROUP is a clause; SystemVerilog/EPL niches). THE FORM: `within <kind> <binder>` + block, an ordinary EXPRESSION — the body is a plain expression block (blocks yield, commands arm — the two prerequisite sessions), so mode-from-position needs NO rule: expression position yields, statement position rides the existing discard demand, and armTail reaches through scopes/let-ins so a commandish scope tail arms in statement position (the one addition the position table needed). `tmp`: fresh unique dir (GetTempPath + guid, weir-tmp- prefix), binder = plain string path, removed in eval's try/finally on normal AND raise exits (the raise-path is the headline pin); NESTED scopes = two dirs, inner finally first. INTERRUPT VERDICT, verified not assumed: SIGINT terminates without managed cleanup — the dir SURVIVES Ctrl+C; STATED as the gap (the OS sweeper's problem, the job-objects-park reasoning; the prefix keeps leftovers identifiable) rather than engineered around. THE ASSEMBLER learned isWithinHead (the yaml-marker precedent: a lexical classifier — the head ends with arbitrary args, so the dangle keys on the keyword, optionally behind `let name =`); the head SPACE-joins its first body statement (the then-convention), siblings arrive sentineled. The binder rides bindings-beat-PATH (withPatNames — the family's FIFTH site, pinned with the perverse `within tmp git`). `within` reserved (domination teaching automatic), OFFERED in completion (the for/do precedent, inventory pin updated), painted in all three grammars (tree-sitter ENGINE-VERIFIED: keyword node, no ERROR) + the REPL tint (Parser.keywords-driven, free). ORDER NOTE: within landed BEFORE session A's tempRoot/newTempDir members — the member follows within's spelling when it lands. `!ev` untouched (session 3's business); no corpus churn. Story: NOTES "within tmp". |
| within-cd-env | 2026-08-04 | `within cd` AND `within env` — sessions 2+3, plus the observed sigil table. THE ASYMMETRY IS THE FORM'S POINT: tmp PRODUCES (binder), cd/env CONSUME (one ATOM argument — never a greedy expr, which would swallow the space-joined first statement; parenthesize compounds); EWithin generalized to kind * binder option * arg option * body. `cd`: the Cwd axis predicted three times (spawn-spec park, scopes-vs-arguments ruling, the worker fork) — resolves against the current cwd so nested relative scopes COMPOSE (a/b pinned), errors AT ENTRY naming the resolved absolute path (block never runs, pinned), restores in try/finally; the interrupt gap inherits tmp's verdict but is HARMLESS here (the process is gone; nothing observes its cwd) — stated because the distinction makes tmp's gap the real one; inside pmap workers the scope nests in the worker's forked cwd (pinned — the one place two cwd mechanisms meet). `env`: an AMBIENT overlay stack in Session (same locality discipline as cwd: root for the main thread, fork-on-first-push for workers, cleared at exitWorker); every spawn applies ambient layers OUTER-FIRST under any explicit sigil env, so inner and explicit keys win at Proc's last-wins application — collision pinned (inner wins, outer keys survive); weir's own Env.load untouched (a scope around pure work is legal-and-pointless, allowed). `!env`'s FATE, reported not acted: the LINE-END !ev district variant is fully covered by within env (retire-candidate at its own bless, unblocking the !-district deletion likewise reported by the arming session); the $e()/!e() SIGIL forms RETAIN — a single-command env overlay and any FRAGMENT-position use are not expressible by a block form while parens-spanning-lines stay parked (census: bicep-deploy ×4, git-subrepo ×1, 13 e2e pin refs). THE SIGIL TABLE, probed row by row (SEMANTICS carries it): the draft rule DIED on observation twice — the if-condition takes NEITHER bare nor $()-piped chains (bind the verdict first), and record fields refuse bare commands despite being whole value-slots; the supported rule: bare commands live at STATEMENT positions and let-RHS/block-final values, wrappers are MANDATORY in every expression fragment, with the unbound-variable-on-the-head teaching as the observed error. Observed asymmetry recorded, no action: interior-statement $() seq-unit-errors while top-level $() arms (armTail reaches statements, not interior elements). SIZED FINDING, pre-existing: a COMMAND line as the first statement after DEDENTING from a nested block mis-joins (within: silent argv absorption; block-lets: parse error at the next line) — the assembler's dedent reprocessing needs its own session; pins structure around it (post-scope observations placed before nested blocks). Story: NOTES "within cd, within env". |
| district-retirement | 2026-08-04 | THE `!` DISTRICT RETIRES — five movements, one session. M1: the line-end `!` and `!name` districts GONE (both unblocked: the arming rule ate the mode gate, `within env` ate the overlay); the retired spelling is a TEACHING error checked up front over every non-content line (a documented feature's removal is the one case a reader has a right to be confused; yaml bodies are bytes, never read); the $e()/!e() SIGILS STAY — fragment/single-command uses have no block spelling (the retention statement, so the next sweep does not propose them). Migrated, each named in NOTES: showcase ×3, jira-branch ×1, git-subrepo ×6 (+2 !()-tail unit assertions where groups ENDED function bodies — the district made every line unit; the migration must keep the type), bicep ×2 (+1 tail), fuzz.weir ×1, GUIDE ×2 blocks, 10 e2e blocks, 17 unit pins (the Command-district group retired to teaching pins; offside/semantic-token/child-env fixtures retargeted to the arming spelling — expectations matched actuals on first run); the FUZZ production RETARGETED not deleted (bare groups; ExplicitDistrict transform = the bare-vs-!() arming equivalence); GRAMMAR.md CAN/CANNOT diffed, 5 stale rows fixed; three grammars lose the marker rule (inventory 25→24 matched, Zed sync green, tree-sitter regenerated + engine-verified 0 ERROR). M2 THE DEDENT FLOOR — the silent swallow (legal-parse-wrong-meaning) is DEAD: a line dedenting below the open block that aligns with no enclosing level ERRORS with a located teaching instead of space-joining as argv (exemptions: open lambdas/brackets/parens — the suite found exactly one legit shape); the CORRECT-JOIN goal deferred WITH ITS SIZE: a stack of open statement levels in Pend (today: LastIndent + closedHead only), its own session; four shapes pinned (within/block-let/for/if). The floor BIT during M1's own migration (post-scope lets absorbed as argv) — the bug demonstrating itself while being fixed. M3 THE WRAP-IT HINT: an applied unbound lowercase head gets "wrap it: $(git …)" beside did-you-mean (fires at the application spine head; module-homes/retired/scriptPath teachings keep their arms); pinned, plain unbound unchanged. M4: record fields NO with the reason IN the table row (newline-bounded RHS + the dedent-swallow class multiplied); if-conditions REPORTED not fixed — inline wants a then-stop argv context + reifier follower + acceptance gate (more than the tuple-comma swap); bind-first is the GUIDE-documented canonical spelling. M5 DIRECTION B: `$()` means CAPTURE everywhere — the new ECapture marker node (erased at check, invisible to armTail by construction) removes the top-level arming; both discard variants gain the drop-the-wrapper clause ("or drop the $( ) to run it as a command" — the wrap-it principle inverted, fired only when the discarded expr IS a $()-command); direction A (arm interior $() too) REJECTED with the reason recorded: $()'s entire job is to assert capture, and arming it discards what the wrapper asked for; corpus migration: ZERO $()-statements existed. The interior exitCode discard kept its tailored teaching via a check-side spine test (was the district cell's text). Story: NOTES "the district retires". |
| gap-a-remainder | 2026-08-04 | GAP AUDIT SESSION A, THE REMAINDER (all receipt-cited): Seq.windowed (LAZY per the family with a pull-count pin — first-1 over infinite pulls the window, never the world; SHORT source = the EMPTY seq, F#'s rule — a partial final window is a different function; n <= 0 RAISES naming the constraint, the head-on-empty posture), Seq.last/tryLast (the X/tryX rule's newest instance — last ASSERTS with the family's exact message shape "last: empty sequence", tryLast asks; BOTH force the whole source by necessity, the forcing family's fifth customer, stated in SKILL), Option.iter (None runs NOTHING — side-effect-absence pinned, the Log.With shape), Option.orElse (RULED fallback-FIRST, F#'s order, so it pipes data-last — `opt |> Option.orElse fallback`, the piped spelling pinned; stays in Option where defaultValue unwraps, both docs carry the distinction; the fallback is an ordinary EAGER argument and an orElseWith twin is PARKED on the defaultWith precedent — the pattern exists, the receipt has not arrived). Path.tempRoot + Path.newTempDir — TWO members ruled over one flag doing two jobs: tempRoot is the pure QUERY (named so it does not read like *a* temp dir; no trailing separator, platform-native), newTempDir CREATES with the side effect visible in the name and follows `within tmp`'s spelling exactly (weir-tmp- prefix + guid — within landed first, the member matches it); cleanup is the CALLER's or the OS's with the SIGNPOST in the doc (use `within tmp` for removal on scope exit; newTempDir when the directory outlives the block) and the shared SIGINT caveat. THE FILESYSTEM-MEMBERS GAP, reported not fixed: File = read/write/append/exists ONLY (no delete/copy/move); there is NO Dir module at all — newTempDir is now weir's only directory-creating operation, an odd shape; the gap audit's receipt pass inventoried File/Path but only receipts drove recommendations, so the family gap went unstated — a filesystem-members audit is its own item. Story: NOTES "gap audit session A, the remainder". |
| fs-members | 2026-08-04 | THE FILESYSTEM FAMILY — File completed (delete/copy/move/size), Dir created (create/exists/delete/deleteAll/list/move); NO receipts required, ruled: a missing family generates none (nobody attempts what obviously is not there — the gap audit's own stated limit). THE RULINGS: (1) copy/move take (src, dst) — the universal convention, NOT data-last, deliberately: neither argument is "the data", so the pipe-position rule does not bite, and inverting a universal convention to satisfy a rule that does not apply is the worse trade. (2) DESTINATIONS REFUSE TO OVERWRITE (raise naming the path) — silent overwrite is data loss and weir rejects-not-guesses; the overwriting spelling is an explicit File.delete first; a copyOver/flag is parked WITH this reasoning. (3) Dir.delete REFUSES a non-empty directory, naming Dir.deleteAll — the recursive one is a SEPARATE, visibly-named member whose doc says what it does in its first line (shipped: scripts building scratch trees need it and rm -rf is worse in every way). (4) Dir.list = FULL paths (a bare name forces every caller to re-join), files AND directories (filter with Seq.where + exists; listFiles/listDirs parked), NOT recursive (Path.glob "**" is that spelling — the pointer stated so nobody adds a flag), SORTED (the glob precedent — two listing operations must not disagree), EAGER (bounded; stated, so no pull-count pin owed). (5) Dir.create = mkdir -p, IDEMPOTENT — the one deliberate exception to (2), distinguished in writing: an existing directory IS create's post-condition, where an existing copy destination is data the caller did not ask to destroy. (6) File.size in bytes, raises on absence (the plain name asserts; trySize parked). Every path resolves against the session cwd (the within-cd/worker seam covered by construction). PINNED: the four error shapes each naming their path; idempotent-create; sorted-full-path-both-kinds listing; the glob-then-delete composition; and the WITHIN TMP DOUBLE-DELETE — a block that deleteAlls its own binder exits clean (the scope's finally already tolerated a vanished dir; now pinned as a product, not an accident). The showcase's File.delete judgment row CLOSED (real cleanup, the acceptance). Story: NOTES "the filesystem family". |
| duration | 2026-08-04 | DURATION — time as a type, decimals only at the edges. THE STORAGE LAW: integer milliseconds, no float anywhere in the implementation (parse/render are digit manipulation); this converts the no-floats decline from gap to position — the divergence row now points here. LITERALS single-unit only (`500ms/30s/2m/1h`), expression position; command-position `30s` stays an argv WORD (the sharpest pin — `sleep 30s`/`timeout 30s` pass text through). FOUR teaching rejections at the lexer: decimal-with-unit (names the ms form), bare decimal (no float literals), `d` (a day is not a fixed length — write 24h; the no-`d` park), compound `1m30s` (text, not a literal — add units or Duration.parse; landed unplanned when the doc-runner hit the confusing application error). THE CLOSED ALGEBRA: +/- Dur×Dur, */÷ Dur×int (ms truncation), comparisons Dur×Dur; Dur÷Dur REJECTED naming `Duration.toMs d1 / Duration.toMs d2` (a ratio has no unit); negatives legal; nothing converts implicitly. THE CLASS WIDENING: TDur joins Eq/Show/Ord — the first widening since the class set closed (sortBy takes duration keys; the Ord "EXACTLY" claim in SEMANTICS annotated). SHOW = the Go shape (largest-unit-first, no zero components, one sign, decimals only below seconds, zero is `0s`) and parse round-trips it BOTH directions, pinned; sub-ms text errors naming the floor. MODULE: ms/s/m/h/toMs/parse/tryParse + `Duration.sleep` — module-qualified RULED (bare `sleep` would shadow coreutils and flip `sleep 5`; integer-ticks Thread.Sleep, no float). BOUNDARIES: Args.load/Env.load parse duration text, `[<Default 30s>]` legal (ADur attr literal), --help renders the Show shape; JSON rejected with a teaching naming Duration.toMs [reasoning superseded 2026-08-05 by [D:unit-types-wire]: NON-REPRESENTABLE, not awaiting a receipt]; interp holes and command splices STAY scalar-exact — `$"took {show elapsed}"` (reported, not widened: unplanned semantics). | Types.fs formatDuration/parseDurationMs; Tests "Duration [D:duration]" (10); e2e duration block; SEMANTICS "Duration — time as a type"; GUIDE "Time: the timeout flag idiom"; SKILL bullet; divergences no-floats pointer; NOTES: "duration" |
| interp-show | 2026-08-04 | INTERPOLATION HOLES CONSULT `Show`, NOT A LIST (rider; origin = the duration session's report). DIAGNOSIS: the arm ENUMERATED — checkScalarSplice hardcoded str/int/bool and the deferred-var discharge (resolvePendingSplices) repeated the same list; neither consulted the class one `demand` call away — the same-policy-twice shape, and Duration's Show admission exposed the disagreement. THE FIX: one `spliceAdmit` law shared by the eager check and the discharge; the HOLE arm demands Cls.Show with a tailored Describe ("interpolation holes render what show renders; … functions never render") — a clean substitution, the class exposed everything needed. THE PROPERTY (Size confirms when it lands): a new Show-admissible type needs NO edit here — the hole side asks the class. CONSEQUENT FLIPS, deliberate: records/seqs/unit now RENDER in holes (show's form; two pins retargeted to functions, unit's exclusion pin became an admission pin, the update-in-hole rejection became a render pin); the bare top-level string hole stays RAW (value, not quoted rendering — eval renders VStr raw, everything else via formatValue). THE SPLICE QUESTION DECIDED SEPARATELY, outcome (1) SPLIT: command-argument splices do NOT inherit — scalar-exact stands (an argv word is a bet on the child's parser: sleep 1h30m works on GNU, fails elsewhere — the word-integrity posture), with a Duration arm teaching the deliberate spellings ("a Duration's argv form depends on the program; pass Duration.toMs d or show d deliberately"); both spellings check-pinned clean. Deferred-hole defaulting UNTOUCHED (unresolved var still defaults to string — [D:splice-default-last] order preserved; pipe-into-lambda pins green). | Check.fs SpliceSite/spliceAdmit; Eval.fs hole renderer; Tests "Interpolation holes consult Show" (5) + 4 retargeted; e2e duration block +2; SEMANTICS splice-family split; SKILL/GUIDE; NOTES: "interp-show" |
| floats | 2026-08-04 | FLOATS, FINITE-ONLY — session 1 of 2 (analysis: plans/PLAN-floats-cost.md; the "shells don't have floats" argument retired — capability peers zsh/pwsh/nushell all float; the other argument died with Duration.toS). THE LAW: checkedFloat raises on any non-finite result (the checkedInt precedent, Eval.fs) — overflow, x/0.0, 0.0/0.0 each raise NAMING what happened; NaN/Infinity unrepresentable, every boundary parse rejects their text, -0.0 normalizes at construction. LITERALS 0.5/1.0/1e5/1.5e-3; digits required BOTH sides of the point (1. and .5 are located teachings — .5 collides with the accessor glyph); 1..10 stays a range; 2e backtracks to ident; 1e999 rejects at the literal; prefix minus FOLDS into float AND Duration literals (the 0 - e desugar would mix types — fixed the pre-existing -30s gap in passing). NO TOWER, no implicit widening: 3/2=1 unchanged, 3.0/2.0=1.5, mixed = located error naming Float.ofInt (the one deliberate footgun, accepted in the bless). CLASSES: Show admitted (holes/print follow — the interp-show ZERO-EDIT property CONFIRMED, before Size: $"{pct}" rendered with no interpolation change); Ord admitted (total BECAUSE finite — first Ord-not-Eq type, stated in SEMANTICS); Eq EXCLUDED — ==/distinct/contains on floats teach "use Float.near a b eps, or compare after Float.round" (representation is the trap finite-only cannot fix, and weir does not vouch for it). RENDERING shortest-round-trippable, integral floats keep .0 (show 1.0 = "1.0" — a float never renders as an int); parse↔show pinned both directions (the exact spelling: Float.near … 0.0, since == is excluded). MODULE Float.ofInt/toInt(truncating, range-checked)/round(away-from-zero)/abs/near/parse/tryParse — NEVER flattens to bare aliases (toInt would shadow Str's; the Option precedent). Duration.toS lands float-returning and LOSSLESS (2500ms→2.5 — the declined-as-int member, now honest); Duration's parse/render path stays integer (re-verified). BOUNDARIES (json/yaml/Args/Env) = session 2, independently blessable; float fields reject with today's messages — nothing half-admitted. Fuzz float production DEFERRED to session 2 with the reason stated: the generator's typed int/str core wires literals into CCmp "==", which floats cannot join — a design change, not an add. Oracle: 3 pins (+Same float arithmetic, +Same mixed-rejection both sides, +Diverges equality — the oracle REFUTED the first spelling: F# rejects == too, pinT with = landed); divergence row no-floats REWRITTEN as floats-finite-only, status different. | Types.fs formatFloat/parseFloat; Parser intLit/floatExponent/dotFloatTeaching/negAtom fold; Check tyHasFloat/floatEqTeaching + class/binop arms; Eval checkedFloat; Builtins Float module; Tests "Floats, finite-only" (12); oracle 161; SEMANTICS "Floats — finite-only"; GUIDE "Rates and percentages"; SKILL bullet; NOTES: "floats session 1" |
| trailing-comments | 2026-08-04 | TRAILING COMMENTS — `let x = 5 // note` parses (origin: the showcase refresh's finding; its "BY NECESSITY" conclusion AMENDED in NOTES — true of the pre-pass, overstated about the language). THE RULE, three regions stated once in SEMANTICS: inside any string form = data (holes included — the one-scanner keeps holes in the string extent, so holes-don't-host-comments is true by construction; $"{1 // 2}" is a located parse error); command argv GLUED = data (http://a, --format=a//b — the nuget receipt's rule unchanged); anywhere else whitespace-preceded = comment to EOL. OPTION B RULED for command lines (yaml's whitespace-preceded # law — ONE convention twice): `git clone $url // the mirror` comments; the stated cost: a bare unquoted `//` argv word reads as a comment, quote it ("//") — both spellings pinned. IMPLEMENTATION: stripComment WAS the rule already (whitespace-preceded, one-scanner, URL receipt in its own comment) — the assembler now applies it per PHYSICAL line before joining, yaml content masked out (BYTES) and comment-ONLY lines excluded (their class carries transparency a blanked line loses — blankSinceHead); -e strips the same; the REPL already did (divergence closed from the script side); the grammars were AHEAD (micro's region already (^|[ \t])//; zero grammar diffs). stripComment now TrimEnds ON CUT (agreement + district-head EndsWith; 5 pins retargeted; the colorizer paints from the // so alignment gaps stay uncolored). THE MASK ORDERING BUG, fuzzer-caught on first deep look: a commented district HEAD line broke head detection (mask ran on raw) — the cut runs twice, find-regions then strip-excluded; commented-head pinned (opens the district, body stays bytes). Trailing /// = ordinary comment, docs attachment-only. fmt PRESERVES verbatim (respace reattaches the tail; qualifyFile's Substring reattachment predated the feature; match reflow carries the comment) + idempotent, e2e-pinned. NEW FUZZ METAMORPHIC: append-trailing-to-random-code-lines is output-neutral (mask-aware, blanks excluded). | Script.fs assemble pre-strip + stripComment TrimEnd-on-cut + colorizeRepl; Program.fs -e; Fuzz appendTrailing; Tests "Trailing comments" (7) + 5 retargets; e2e trailing block; SEMANTICS three-region table; SKILL/GUIDE; NOTES: "trailing comments" + the amendment |
| floats-boundaries | 2026-08-04 | FLOATS SESSION 2 — the four boundaries + the fuzzer (the S1 deferrals, both landed with their stated reasons resolved). THE INTEGER-INTO-FLOAT RULING: a JSON/YAML integer-shaped number WIDENS into a float field ({"rate": 3} reads 3.0) — A PARSE, NOT WEIR ARITHMETIC: no-implicit-widening governs the language's operators, not what a boundary reads from a wire format with one number type (stated so it does not look like an exception); the reverse (decimal into int) TEACHES "declare it float" — the pre-existing behavior was a RAW FormatException leak ("One of the identified items was in an invalid format"), so "today's message" was not pinned, it was replaced (deviation recorded). to json: 1.0 emits 1.0 via formatFloat as a raw JSON number (the show shape; schema validity against integer changes CORRECTLY); invalid tokens IMPOSSIBLE by construction (finite-only — no value exists to serialize). SCHEMA SEAM CONFIRMED CLEAN as the report predicted: kindOk untouched (integer ⊆ number was already there); tyKind gained float→number, literalKind gained unquoted-float→number (symmetric with the int precedent — an unquoted 1.5 at a string schema now errors like unquoted 5 always did). YAML: the float/float-string PAIR pinned as a pair (1.5 unquoted / "1.5" quoted, round-trip both directions — the reverse-Norway law confirmed, not redesigned); .inf/.nan REJECTED with the finite-only teaching (the session's sharpest pin); THE YFLOAT NODE ADDED to the prelude union — required, not optional: the district tree is TYPED Yaml, so a float splice without a case would be unsound, AND district float literals previously self-typed YStr and rendered QUOTED (cpu: 1.5 emitting "1.5" — wrong for any manifest); unquoted float-shaped literals now self-type YFloat (the int-normalization precedent; parseFloat refuses non-finite so nan-text stays string). ARGS/ENV: Float.parse shape at both, [<Default 0.5>] (attrArgLit gained its own float lexing — the Duration session's recorded gotcha, hit as predicted), --help renders via formatFloat, round-trip pinned. FUZZ: SFloat production (top-level, exact-QUARTER literals so renders stay deterministic, + - * only — division excluded, a zero divisor would raise; optional Ord comparison print) — the Eq exclusion's cost STATED in GRAMMAR.md's denominator (first class decision to shape the generator: floats never join CCmp "=="; ==-free but NOT comparison-free); CAN/CANNOT diffed; 30k fresh-seed cases green FIRST RUN (the expect-red prediction did not fire — exact quarters dodged rendering flakiness). Showcase gained --rate 0.5 (+ a background agent's five review fixes folded in). Member names here predate the same-branch rename row below. | Check/Contracts/Argv/Eval/Parser/Yaml/Prelude per plan; Tests "Floats at the boundaries" (6) + 4 message retargets; fuzz SFloat + GRAMMAR.md; SEMANTICS boundary section; SKILL/GUIDE; showcase; NOTES: "floats session 2" |
| duration-rename | 2026-08-04 | `Duration.toS`/`toMs` → `toSeconds`/`toMillis` (rider, pure rename — toSeconds was already float-returning from [D:floats], so name and type land in one branch). RULED: spelled out, `to` prefix KEPT — it marks direction against the constructor family (`Duration.ms` constructs, `toMillis` inverts); house convention is Str.toLower/toInt, so the abbreviations were the outliers. Alternatives closed: seconds/millis (constructor collision), inSeconds/inMillis (fine second choice), s/ms (literal-suffix ambiguity). NO alias, no shim, NO hint machinery (user ruled: the plain unknown-member error suffices — didYouMean stays distance-≤2; a prefix widening was landed and reverted the same hour, unshipped). Three teaching messages renamed (the ÷ ratio, the JSON park, the argv splice). | Builtins members+docs; Check 3 messages; Tests 7 pins moved; e2e 3 lines; SEMANTICS/GUIDE/SKILL/showcase |
| dedent-join | 2026-08-04 | THE DEDENT CORRECT-JOIN — the district-retirement session's deferred M2 upgrade, landed. THE ENUMERATION FIRST (the bless's own gate): statements after `within` and `for` bodies at INTERIOR levels errored (the floor) — after block-lets, if/match, and at col-0 they already joined; even within-in-within could not continue its outer body; corpus clean only because the migration CONTORTED around it (git-subrepo's moved lets). Verdict: not nearly empty — proceed. THE MECHANISM IS SMALLER THAN THE PLAN'S SKETCH: no statement-level stack was added — NO new stack at all. The plan worried the fix might read as a fifth alignment stack; the landed fix is two more MEMBERS of the existing compound machinery (OpensCompound: within/for heads beside if/match — one classifier expression). The insight: a dedent-join alone cannot work in sentinel-flattened text (the parser cannot see columns, so a within body would absorb the sibling as its next statement); the block must be PAREN-WRAPPED at close, which is exactly what compounds do — within/for needed the same close-and-wrap, and if/match's machinery already carried levels, alignment, spans, and deepest-first pops. AMBIGUITY RULING: innermost wins — inherited from the compound stack's existing pop discipline, not newly decided. Let-prefixed heads (let r = within …) stay Lets-owned, the if/match convention. THE FLOOR STAYS for unmatched columns (pinned located); the git-subrepo swallow shape now JOINS with its binding proven (never argv — pinned by VALUE); the exemption class (lambdas/brackets/parens) unchanged incl. the compound lambda-inside-within case; the four alignment stacks UNTOUCHED (Brackets/PipeGroups/Compounds/Lambdas — the census trigger must not fire: Compounds gained members, not a sibling). Single-line within/for heads ride the same tolerance single-line ifs always had. Interior blocks ending in commands still owe !()-tails (the migration convention — a session probe re-found it; not this session's business). 30k strict-span deep cases green; fmt is identity on every newly-joining shape. | Script.fs classifyPiece OpensCompound (+2 members); Tests "The dedent correct-join" (6); e2e dedent block; SEMANTICS dedent-resolution rule; NOTES: "the dedent correct-join" |
| tasks-underneath | 2026-08-04 | PARALLEL FAN-OUT FOR I/O-BOUND ARMS — two sessions, one branch. THE FINDING: Parallel.For sized workers by ProcessorCount, assuming CPU-bound arms; weir's arms spawn-and-wait (I/O-bound by domain), so 100 blocking arms on 8 cores ran ~13 rounds with the pool's ~500ms-per-thread injection limping behind. SESSION 1 (the risk, isolated): session state REKEYED ThreadLocal→AsyncLocal (cwd + env overlay stack) — scopes follow the LOGICAL context, which is also correct under plain threads (per-arm enter/exit makes pool reuse safe); AOT-cleanliness PROBED not assumed (3 publish warnings = the pre-existing baseline, zero new); ZERO pin movement (1028/1028 + the load-bearing within-cd-in-piter pin + env locality pins, all live-probed on AOT too). SESSION 2: the runner is a dedicated-thread worker pool (TaskCreationOptions.LongRunning — each ACTIVE arm owns a thread, sidestepping the injection heuristic) bounded by THE CEILING: 64, stated — resource protection, not CPU sizing; Seq.pmapWith/piterWith set it explicitly (degree<1 raises naming the constraint); tasks-not-F#-async per the bless (AOT bite history, BCL cancellation shape, one-way interop tax). CONTRACTS: input order preserved (pinned unchanged); error contract RE-RULED mildly and deliberately: every arm RUNS even after a failure (Parallel.For skipped unstarted iterations — "all arms complete" is now literally true) and the first error BY INPUT ORDER rethrows (was nondeterministic first-observed) — deterministic, pinned by distinguishable messages. RE-PINNED AT THE CEILING: within-cd-in-piter at 100 concurrent scope forks (unit) + 100 arms each SPAWNING a child weir on AOT in ~470ms with order and parent cwd held (e2e); tree-kill is per-child mechanism (concurrency-independent), the concurrent-children half covered by the spawn probe. 128×100ms blocking arms complete in ~2 rounds (wall-clock pinned <700ms). Duration.sleep negative-raise had ALREADY landed (the dedent session's rider); the granularity line (~15ms Windows / ~1ms Linux — a small sleep is a floor, not a promise) added to its doc. UNBLOCKED, recorded not built: Seq.pfirst (Task.WhenAny + CTS + tree-kill), retry/poll (cancellable waits); Duration.sleep stays Thread.Sleep (a sibling, not a dependency — no reason surfaced). | Session.fs AsyncLocal; Builtins runParallelWith/ceiling/pmapWith/piterWith + docs; Tests "Parallel fan-out" (5); e2e tasks block; SEMANTICS "Parallel fan-out"; SKILL; NOTES: "tasks underneath" |
| retry-poll | 2026-08-04 | RETRY/POLL — the bounded loops, one session, the framing pre-settled (two-segment compound + key=value desugaring over a record; REJECTED: within-shaped — a scope's body runs ONCE, retry's runs repeatedly, conflating them re-makes the mode-gate-vs-scope confusion; REJECTED: plain combinators — the predicate needs the body's value, forcing two lambdas or Option-folding at the shape people write most). THE SEVEN RULINGS: (1) TWO records, not one-with-ignored-keys (poll interval=1s attempts=3 must not typecheck): Retry{attempts=5; delay=1s; timeout=None}, Poll{timeout=1m; interval=1s} — declared in the PRELUDE, the types ARE the reference; Retry.defaults/Poll.defaults are module members. (2) body = block yielding 'a; BOTH forms return the successful attempt's value — they differ by BOUND (attempts vs time), not return. (3) exhaustion RAISES naming attempts+elapsed (fail-fast, the Seq.head posture); tryRetry PARKED on X/tryX (receipt: a script that genuinely continues). (4) keys attempts/delay/timeout= (retry), timeout/interval= (poll); UNBOUNDED IS UNREPRESENTABLE, not refused — no key spells forever, attempts=0 raises the floor; backoff= PARKED (additive later by construction — the key form is why parking beats guessing its shape); duplicate keys = located parse error ON the second spelling; order free. EMPTY HEAD IS NOT A FORM: with no keys, ONE atom is consumed as the options value (the within-cd argument rule — same consumption, same teaching class). (5) until binds in a SEGMENT HEADER (for k,v precedent), never lambda syntax; a bool body IS the predicate — until on top of one is REJECTED naming both the fix and the alternative (negate in the body); the four-row table pinned as FOUR cases (the N-tuple lesson); attempt-aware predicates PARKED value-only (a second binder is additive — for k,v again). (6) the wait is CANCELLABLE from the start: timeout= cancels a pending delay (wall-clock pinned: poll timeout=80ms interval=10s exhausts in ms, not 10s); Duration.sleep stays Thread.Sleep — different mechanisms, difference unobservable from weir. RAISES PROPAGATE — retry retries on the predicate, never exceptions (catching would swallow bugs; the reifiers make failure data). (7) options records+defaults live in Retry/Poll modules; Seq.pfirst stays a Seq member when it lands (a race, not a retry). GRAMMAR: retry/poll heads = OpensCompound members 5 and 6 (STILL no stack); until = an ElseHead-class extender with two carve-outs (skips the let-no-body guard; never reads as a pipe) + col-0 routing (the col-0 `|` arm precedent — the bless's own example demands it); keywords retry/poll/until reserved+offered (the inventory tripwire fired exactly as designed and was decided). NOT-DOING: while (an unbounded loop wearing a keyword), general recursion (pipelines transform), try/catch (raises propagate — error-handling-as-value stands), [<Default>] at record literals (the resting-point reasoning is for values from OUTSIDE), unbounded retry (unrepresentable). | Ast ERetry; Parser retryExpr both-OPPs + keywords; Script until routing + 2 compound members; Check TERetry + the 4-row rules; Eval loop w/ cancellable delay; Prelude 2 records; Builtins defaults+docs; Tests "retry / poll" (9); fuzz SRetryPoll + GRAMMAR.md; 3 grammars + ts regen (the showcase's ONE pre-existing ERROR is the README's {{braces}} nit — entry refreshed, no duplicate filed; retry sample 0); e2e block; SEMANTICS/GUIDE/SKILL; NOTES: "retry/poll" |
| command-signatures | 2026-08-04 | COMMAND SIGNATURES — contracts customer two, both blessed sessions on one branch. THE TWO VERIFIES HELD: Contracts.verify WAS two-arm-shaped (kind-dispatched loop, the hash arm labeled as such — the version arm slotted in, no refactor); the #sig walk IS findWeirDir (the schema kind's machinery, second consumer); #sig is a head DIRECTIVE (the #loose family), nowhere near a marker line — the fourth-face guard is N/A, confirmed. THE GENERATED-ENTRY RULING (the ninth ruling's edge, extended not contradicted): Url slot holds "generated:<source>" (help/completion-fish/fish-file), the entry carries the file hash AND the verbatim --version (denormalized from the file, hash-protected against drift, so verify needs no weir parser); restore NEVER regenerates — present = hash-confirmed, absent = an error naming `weir add sig` (a checked-in signature must not depend on the restoring machine; option (b) rejected as the lock's purpose inverted). THE MODEL SIMPLIFICATION: a signature is an ordinary weir MODULE (loadModuleCached gives parsing, decl-only+weak-purity, and cycle guards free) — `let version`, optional `let exhaustive = true` (the declared-field spelling won), surface = the type named `Cmd` BY NAME (greppable; generation always writes it). Positionals RETURNED with the justification stated loudly: the drop's receipt was weir's OWN CLIs mimicking contracts; foreign CLIs have operands and a signature DESCRIBES theirs — [<Positional>] is registered, derives no flag, inert elsewhere (2 archaeology pins retargeted). Shorts are EXPLICIT [<Short>] only — derivation is weir's convention, a foreign tool's shorts are facts. L2 held: unknown flags only (subcommand-miss disables the line's checking; bare -- ends flags; -<digit> is an operand); partial WARNS / exhaustive ERRORS, did-you-mean + declared-by line. CHECK NEVER SPAWNS (marker-fixture pinned, unit + e2e); property 3 re-pinned (byte-identical output ± contracts). NON-INHERITANCE is vacuously strong: modules are decl-only so commands cannot appear in them, and a module's own #sig is rejected outright (v1 — modules cannot declare signatures at all, stated). GENERATION (session 2): three sources in fidelity order, flat surface v1 (subcommand split = the invited hand edit), validate-before-write (the add-validates principle — a generator emitting invalid weir errors immediately, nothing persists; caught its own first bug: the probe importing itself), bool fields uniformly (L2 needs no value types). REAL-TOOL HIT RATE, reported honestly: dotnet 10 flags via --help; git and python3 refuse cleanly to write-by-hand (their help shapes defeat the scraper); no fish-emitting tools in the container — the fixture pins the pipeline, the fish sources await a kubectl-shaped receipt. | Contracts.fs Version field + version/restore arms + toolVersionOutput (fenced to verify/add); Script.fs SigDecl/loadSigs/sigCmdDiagnostics/SigGen + scriptBody peel; Check.fs Positional; Program.fs add sig; Tests "Command signatures" (7) + 2 retargets; e2e sig block; SEMANTICS/GUIDE/SECURITY non-claim; NOTES: "command signatures" |
| size | 2026-08-05 | SIZE — bytes as a type, the Duration pattern COPIED (the session's brief), one session. THE ARGUMENT THAT UNPARKED IT: show renders a TYPE — File.size : int rendered 1610612736 correctly-and-uselessly; File.size : Size renders 1.5 GiB with no formatter member to name, document, or maintain; the earlier "base-2 footgun" park was a LITERAL problem show does not have. LITERALS binary-only (B/KiB/MiB/GiB/TiB, integer, single-unit): MB/KB/GB/TB REJECTED with the session's most contestable teaching — 10^n in SI, 2^n in common usage, ls -lh/df -h/Windows all disagree, weir refuses to guess; decimal and compound literals teach (the Duration wording shapes reused; the digit-follow lexer fix inherited by the MERGED suffix table — one choice serves both unit types, case distinguishing MiB from minutes). THE SI-IN-PARSE ASYMMETRY, ruled as recommended: Size.parse accepts "1MB" as 10^6 — parse reads what someone else WROTE (the json integer-widens shape), a literal is weir's own code; sub-byte precision rejects (the sub-ms mirror — noted: this makes parse strict on approximations like "1.3GiB"; a receipt reopens). ALGEBRA closed, Size/Size names BOTH alternatives (toBytes ratio, Float.ofInt fraction); negatives legal; int64 bytes (~8 EiB ceiling, stated). CLASSES Eq/Show/Ord — the hole rendered with ZERO interpolation edits, THE PROPERTY'S SECOND CONFIRMATION (floats were the first); print excludes Size (the Duration precedent — holes are the spelling). FILE.SIZE RETURNS SIZE — the one intended break, legible ("expected Size, got int"); exactly ONE suite pin moved (the Ord message listing key types). WHERE THE COPY DID NOT FIT, the report's finding for whatever unit type comes third: (1) show is a truncated REPORT, not an exact encoding — base-1024 decimals do not terminate, so Duration's total value↔text round-trip is unavailable; the law is text→value→text plus representable values, and toBytes is the exact exit; (2) parse accepts foreign SI units — Duration had no ambiguous units to rule on; (3) the JSON park copied but YAML needed its OWN park — NOT because Duration predates yaml (it does not: yaml 07-30, Duration 08-04; the first claim here was wrong and a user question caught it) but because the Duration bless parked JSON explicitly and never mentioned yaml, whose GENERIC field law caught TDur untailored; the follow-up gave Duration the matching tailored park (both name their toX exit). Boundaries: Args/Env parse (attrArgLit's THIRD reminder — the separate tiny lexer again), [<Default 10MiB>] e2e, --help renders the show form; JSON/YAML rejected [reasoning superseded 2026-08-05 by [D:unit-types-wire]: NON-REPRESENTABLE, not awaiting a receipt]. Command-mode 10MiB = argv word by construction (inherited, pinned parse-level + AOT). | Types formatSize/parseSize; Parser merged suffix table + teachings; Check/Eval/Argv/Builtins per the Duration map; Tests "Size" (11) + 1 retarget; e2e block; SEMANTICS "Size — bytes as a type"; SKILL/GUIDE; NOTES: "size" |
| showcase-covers | 2026-08-05 | THE SHOWCASE COVERS EVERYTHING + THE STALE SWEEP. HALF ONE, hit counts per the denominator rule: File.size stale claims 0/0/0/0/0 (GUIDE/SKILL/COMING-FROM/SEMANTICS/README — the Size session's own doc pass held), toS/toMs 0 (the rename agent swept), floats-absent 0; the coming-from header re-dated with the re-sweep noted. HALF TWO: the tour hosts examples/showcase-lib.weir (decl-only module, /// docs, qualified-always import, the Lib.Ctx qualified literal crossing the boundary), examples/.weir/ COMMITTED (the real ConfigMap schema reused from tests/fixtures + lock.json with the yannh provenance URL — CI offline; a fresh-tree copy CHECKS with no restore and no network, e2e-pinned) with a SECOND district (schema=k8s-configmap — the Service keeps its three demos, per the recommendation), and a HAND-WRITTEN partial git signature (git refuses to scrape — its own session's finding; hand-written-partial IS the ruling demoed; no lock entry: hand-written sigs are SOURCE, nothing to restore, no version pin — git versions legitimately vary); the caught-typo demo is a comment naming the e2e proof (the tour must run clean). ADDED BEYOND THE BLESS, floor-not-ceiling: a retry demo (2 lines — the header's honesty demanded demonstrated-or-excused and there was no excuse). TWO FINDINGS THE TOUR ITSELF CAUGHT: (1) THE RETRY-DESUGAR CAPTURE — the tour's own `Verdict = … | Retry of string * int` constructor captured the key=value desugar's `Retry.defaults` ("only records have fields; this has type string * int -> Verdict"): the sugar is SHADOWABLE by any user name Retry/Poll — the F#-type-name-capture class the update-path row dodged; showcase case renamed Again, the language seam RECORDED OPEN (a non-capturable module reference is the fix shape, own session). (2) THE REIFIER SIG GAP, fixed in-session: reified commands (git … | succeeds — the MOST idiomatic shape) desugar their ECmd into the |succeeded-family builtin spine, so signature checking missed them entirely; sigCmdDiagnostics now recovers (prog, argv) from the spine (prog = the LAST string — orFail's msg precedes it), all four reifiers pinned. Windows walk: path work is Path.Combine under the platform-native ruling; STATED as the hand-run item (no Windows CI). One fixture bug en route: the e2e typo probe tripped weir's own discard rule — the silent set-e death pattern, mine this time. | examples/showcase-lib.weir + .weir tree; showcase edits ×7; Script.fs reified-spine recovery; Tests +1 reified pin; e2e showcase-tree block; COMING-FROM date; NOTES: "the showcase covers everything" |
| unit-types-wire | 2026-08-05 | UNIT TYPES ARE NON-REPRESENTABLE AT THE WIRE BOUNDARIES (rider; docs + park reasoning only, ZERO behaviour change). WHAT IT REPLACES: the Duration and Size json/yaml parks recorded "the encoding is undecided and wants a receipt" (ms-int vs ISO-8601; bytes-int vs string) — a TEMPORARY framing that invited periodic re-litigation and was already strained by the SI-in-parse asymmetry looking like a pro-parsing argument. THE REASON THAT HOLDS: json/yaml have no duration or size type — whatever weir emitted would be an INVENTED encoding the consumer must learn ("1m30s" is weir-specific; 90500 loses the unit), and a read-side field law would apply weir's format SILENTLY on the author's behalf, asserting the document's writer used it, which weir cannot know; the parse asymmetry's own principle (a parse reads what someone else wrote — invoked BY the author) argues FOR the rejection, not against. RECEIPT TRIGGER DROPPED, explicitly: nothing a receipt would settle — this is not awaiting evidence. THE GENERAL RULE for a third unit type, filed beside the Size misfits: a unit type's wire representation is the CONSUMER's convention, not weir's — the boundary rejects, the author converts (toX into an int field, or show for a string). ALL EIGHT direction-cells read and confirmed tailored (2 types × 2 boundaries × 2 directions, live-probed — each names its exit). TWO FINDINGS reported, not folded (the zero-change bar): (1) all four message TEXTS say "no … convention YET" — the word "yet" is the receipt framing embedded in behaviour; retiring it moves four pins, a separate rider if wanted; (2) a FIFTH cell exists: a unit-type DISTRICT SPLICE rejects with the GENERIC yaml-boundary wall AND leaks a synthetic internal name ("type '__hole1 cannot cross the yaml boundary") — untailored plus a cosmetic bug, its own fix. ARCHAEOLOGY carried per the bless: Duration's yaml cell was originally the generic law (the bless omitted yaml; JSON was parked explicitly), the follow-up tailored it — and the first explanation of that gap ("yaml predates Duration") was WRONG (yaml 07-30 < Duration 08-04), caught by a user question; both the sequence and the failed explanation are on record. | SEMANTICS shared statement (both types, both directions, one place); duration + size rows' park clauses superseded-marked; NOTES size-entry general rule + rider entry |
