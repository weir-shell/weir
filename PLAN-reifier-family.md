# weir — mini-plan: the reifier family completes (`| exitCode`, orFail streams)

Status: BLESSED (user 2026-07-24, advisor pass folded). One session.
Origin: the family's missing cell bit twice — jira-branch filed
"graceful-cancel has no spelling"; fuzz.weir hit the streaming half.

(Blessed plan text as delivered; the executed decisions below.)

## Session report (2026-07-24)

- ARCHAEOLOGY VERDICT: accident-grade. The only orFail pin was
  parse-shape (the message desugar); the silence lived in an in-code
  comment (capture-and-replay riding complete's plumbing), un-pinned;
  the flagship's two sites were --quiet commands, exactly as the plan
  predicted. Fixed: orFail rides the new streaming spawn
  (Proc.streamCode — stdout relays live, stderr inherits). The
  implied claim became provable and is pinned behaviorally in e2e:
  bare `!(cmd)` ≡ `cmd | orFail msg`, stream-byte-identical.
- `| exitCode` landed: marker + fold arm (exitCoded/exitCodedEnv,
  the family desugar verbatim), builtins on the streaming spawn,
  int-typed, never raises. Single-segment family rule holds (pinned
  at statement level; the let-RHS chain rejects mid-chain stages
  earlier with the bare-pipe hint, family-uniformly — noted).
- The conflict cells landed as parse-time teaching errors, all four
  pinned: $() (capture conflict → points at `| complete`), !()
  (discard hint), bare statement (the set+e muscle-memory hint),
  district lines (inherit the bang ruling through the wrap desugar —
  the verify-cell resolved: the wrap makes it the !() cell).
- The match-on-130 idiom is taught (GUIDE) and pinned (e2e); env
  variants follow the completed precedent (exitCodedEnv pinned in
  expression position with the overlay applied).
- fuzz.weir un-workarounds: the district line carries
  `| orFail $"deep fuzz FAILED — reproduce with: …"` — streams AND
  decides; both NOTES-agent frictions closed (graceful-cancel's
  selection half stays complete's, said explicitly). The flagship's
  diff-files guard swapped --quiet for --exit-code as the living
  example.
- The semantic-token walk gained the new spine names (exitCode
  chains keep their command coloring).
- The LAW is stated once in SEMANTICS (output goes where the meaning
  goes); GUIDE reorganized around the four-cell table; GRAMMAR.md
  denominator notes the family stays outside the fuzzer's shapes.
